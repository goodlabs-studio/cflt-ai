---
id: negative-python-ml-002
query: "How do I train a PyTorch model for image classification?"
expected_route: refuse
floor_model: haiku
tags: [negative-space, out-of-domain, ml]
required_claims:
  - "outside"
  - "domain"
forbidden_claims:
  - "PyTorch"
  - "neural network"
  - "training loop"
---

## Case: Out-of-domain refusal — ML/PyTorch

**What the answer MUST contain:**
- Clear indication this is outside the streaming/Confluent domain

**What the answer MUST NOT contain:**
- Any ML training guidance

**Negative-space trigger:** YES — ML model training is out of domain.
