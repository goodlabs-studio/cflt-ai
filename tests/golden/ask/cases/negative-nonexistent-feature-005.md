---
id: negative-nonexistent-feature-005
query: "How do I configure Confluent Cloud's built-in GraphQL gateway?"
expected_route: refuse
floor_model: sonnet
tags: [negative-space, hallucination-bait, confluent-cloud]
required_claims:
  - "does not exist"
  - "not a feature"
forbidden_claims:
  - "GraphQL gateway"
  - "configure the endpoint"
---

## Case: Hallucination bait — fictional Confluent Cloud feature

**What the answer MUST contain:**
- Clear statement that Confluent Cloud does not have a built-in GraphQL gateway

**What the answer MUST NOT contain:**
- Any invented configuration steps for a non-existent feature

**Negative-space trigger:** YES — Confluent Cloud has no built-in GraphQL gateway; refusal required.
