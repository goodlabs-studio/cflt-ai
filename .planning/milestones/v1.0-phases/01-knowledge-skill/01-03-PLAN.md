---
phase: 01-knowledge-skill
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - tests/golden/ask/test_golden_ask.py
  - tests/golden/ask/cases/wiki-only-acks-001.md
  - tests/golden/ask/cases/wiki-only-replication-002.md
  - tests/golden/ask/cases/wiki-only-compaction-003.md
  - tests/golden/ask/cases/wiki-only-partitions-004.md
  - tests/golden/ask/cases/wiki-only-consumer-groups-005.md
  - tests/golden/ask/cases/wiki-only-sla-tiers-006.md
  - tests/golden/ask/cases/wiki-only-schema-compat-007.md
  - tests/golden/ask/cases/wiki-only-eos-008.md
  - tests/golden/ask/cases/wiki-only-dr-overview-009.md
  - tests/golden/ask/cases/wiki-only-topic-naming-010.md
  - tests/golden/ask/cases/mcp-acks-config-001.md
  - tests/golden/ask/cases/mcp-flink-watermark-002.md
  - tests/golden/ask/cases/mcp-connector-version-003.md
  - tests/golden/ask/cases/mcp-cluster-linking-config-004.md
  - tests/golden/ask/cases/mcp-rbac-syntax-005.md
  - tests/golden/ask/cases/mcp-schema-registry-api-006.md
  - tests/golden/ask/cases/mcp-privatelink-setup-007.md
  - tests/golden/ask/cases/mcp-kafka-version-008.md
  - tests/golden/ask/cases/deep-dr-architecture-001.md
  - tests/golden/ask/cases/deep-eos-vs-alO-002.md
  - tests/golden/ask/cases/deep-flink-vs-ksql-003.md
  - tests/golden/ask/cases/deep-multi-region-design-004.md
  - tests/golden/ask/cases/deep-fsi-compliance-005.md
  - tests/golden/ask/cases/deep-event-sourcing-006.md
  - tests/golden/ask/cases/deep-cdc-architecture-007.md
  - tests/golden/ask/cases/negative-react-ood-001.md
  - tests/golden/ask/cases/negative-python-ml-002.md
  - tests/golden/ask/cases/negative-aws-lambda-003.md
  - tests/golden/ask/cases/negative-hallucination-bait-004.md
  - tests/golden/ask/cases/negative-nonexistent-feature-005.md
  - tests/golden/ask/cases/negative-competitor-shill-006.md
  - tests/golden/ask/README.md
autonomous: true
requirements: [KNOW-03, KNOW-04, KNOW-05]

must_haves:
  truths:
    - "tests/golden/ask/cases/ contains >= 30 well-formed case files"
    - "At least 5 cases have expected_route of refuse or redirect_to_mcp (negative-space)"
    - "All three positive routes (wiki-only, wiki+mcp, deep) are covered by cases"
    - "At least 10 cases are labeled floor_model: haiku"
    - "At least 10 cases are labeled floor_model: sonnet"
    - "pytest structural tests validate case format and distribution"
  artifacts:
    - path: "tests/golden/ask/test_golden_ask.py"
      provides: "Structural harness runner verifying case files"
      exports: ["TestGoldenHarnessStructure", "TestFloorModelDistribution"]
    - path: "tests/golden/ask/cases/"
      provides: ">= 30 golden case files in YAML+markdown format"
    - path: "tests/golden/ask/README.md"
      provides: "Case authoring guide for future contributors"
  key_links:
    - from: "tests/golden/ask/test_golden_ask.py"
      to: "tests/golden/ask/cases/*.md"
      via: "CASES_DIR.glob('*.md') loads and validates all cases"
      pattern: "CASES_DIR"
    - from: "tests/golden/ask/cases/*.md"
      to: ".claude/commands/ask.md"
      via: "expected_route field maps to ask.md Step 1.5 routing decisions"
      pattern: "expected_route"
---

<objective>
Build the golden test harness with 32 case files covering all routes and floor models, plus a pytest structural runner that enforces case quality and distribution.

Purpose: KNOW-03/04/05 require a tested golden harness proving the /ask skill can be evaluated at model-floor thresholds. Phase 1 implements structural tests (case format validation, distribution checks). LLM-evaluated rubric tests are Phase 4+.
Output: 32 golden case files, pytest runner with parametrized validation, README for case authoring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-knowledge-skill/01-CONTEXT.md
@.planning/phases/01-knowledge-skill/01-RESEARCH.md
@.planning/phases/01-knowledge-skill/01-02-SUMMARY.md

@.claude/commands/ask.md
@tests/conftest.py

<interfaces>
<!-- Golden case YAML front matter schema (from RESEARCH.md Pattern 4).
     Each case .md file starts with YAML frontmatter delimiters containing these fields: -->

  Required fields: id, query, expected_route, floor_model, tags, required_claims, forbidden_claims
  Optional fields: expected_sources_pattern

  VALID_ROUTES = {"wiki-only", "wiki+mcp", "deep", "refuse", "redirect_to_mcp"}
  VALID_FLOOR_MODELS = {"haiku", "sonnet"}

<!-- Distribution requirements: -->
  Total cases: >= 30
  Negative-space (refuse/redirect_to_mcp): >= 5
  All three positive routes present: wiki-only, wiki+mcp, deep
  Haiku-floor cases: >= 10
  Sonnet-floor cases: >= 10
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create golden harness pytest runner and README</name>
  <files>
    tests/golden/ask/test_golden_ask.py
    tests/golden/ask/__init__.py
    tests/golden/__init__.py
    tests/golden/ask/README.md
  </files>
  <read_first>
    tests/conftest.py
    .planning/phases/01-knowledge-skill/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test (TestGoldenHarnessStructure::test_cases_directory_exists): tests/golden/ask/cases/ directory exists
    - Test (TestGoldenHarnessStructure::test_minimum_case_count): >= 30 .md files in cases/
    - Test (TestGoldenHarnessStructure::test_minimum_negative_space_cases): >= 5 cases with expected_route in {refuse, redirect_to_mcp}
    - Test (TestGoldenHarnessStructure::test_all_three_routes_covered): wiki-only, wiki+mcp, deep all present
    - Test (TestGoldenHarnessStructure::test_case_has_required_fields): parametrized — each case has all REQUIRED_FIELDS
    - Test (TestGoldenHarnessStructure::test_case_has_valid_route): parametrized — expected_route in VALID_ROUTES
    - Test (TestGoldenHarnessStructure::test_case_has_valid_floor_model): parametrized — floor_model in VALID_FLOOR_MODELS
    - Test (TestGoldenHarnessStructure::test_required_claims_is_list): parametrized — required_claims is a list
    - Test (TestGoldenHarnessStructure::test_forbidden_claims_is_list): parametrized — forbidden_claims is a list
    - Test (TestGoldenHarnessStructure::test_case_id_unique): no two cases share the same id field
    - Test (TestFloorModelDistribution::test_haiku_cases_exist): >= 10 haiku cases
    - Test (TestFloorModelDistribution::test_sonnet_cases_exist): >= 10 sonnet cases
  </behavior>
  <action>
    1. Create directory structure:
       - `tests/golden/__init__.py` (empty file)
       - `tests/golden/ask/__init__.py` (empty file)
       - `tests/golden/ask/cases/` directory (empty initially — cases added in Task 2)

    2. Create `tests/golden/ask/test_golden_ask.py` with two test classes:

       Module-level constants:
       - CASES_DIR = Path(__file__).parent / "cases"
       - REQUIRED_FIELDS = {"id", "query", "expected_route", "floor_model", "tags", "required_claims", "forbidden_claims"}
       - VALID_ROUTES = {"wiki-only", "wiki+mcp", "deep", "refuse", "redirect_to_mcp"}
       - VALID_FLOOR_MODELS = {"haiku", "sonnet"}

       Helper function `load_case(path: Path) -> dict`:
       - Read file content, parse YAML between the first two triple-dash lines
       - Return parsed dict or empty dict on failure

       Module-level: ALL_CASES = sorted(CASES_DIR.glob("*.md")) if CASES_DIR.exists() else []

       Class `TestGoldenHarnessStructure` (docstring: "KNOW-03: Verify case file structure and minimum coverage."):
       - test_cases_directory_exists: assert CASES_DIR.exists()
       - test_minimum_case_count: assert len(ALL_CASES) >= 30
       - test_minimum_negative_space_cases: filter cases where expected_route in {refuse, redirect_to_mcp}, assert >= 5
       - test_all_three_routes_covered: collect all expected_route values, assert "wiki-only", "wiki+mcp", "deep" all present
       - test_case_id_unique: collect all id fields, assert no duplicates
       - @pytest.mark.parametrize("case_path", ALL_CASES, ids=lambda p: p.stem) test_case_has_required_fields: assert no missing fields
       - @pytest.mark.parametrize test_case_has_valid_route: assert expected_route in VALID_ROUTES
       - @pytest.mark.parametrize test_case_has_valid_floor_model: assert floor_model in VALID_FLOOR_MODELS
       - @pytest.mark.parametrize test_required_claims_is_list: assert isinstance(fm.get("required_claims", []), list)
       - @pytest.mark.parametrize test_forbidden_claims_is_list: assert isinstance(fm.get("forbidden_claims", []), list)

       Class `TestFloorModelDistribution` (docstring: "KNOW-04/05: Verify haiku and sonnet cases exist at sufficient count."):
       - test_haiku_cases_exist: filter by floor_model == "haiku", assert >= 10
       - test_sonnet_cases_exist: filter by floor_model == "sonnet", assert >= 10

    3. Create `tests/golden/ask/README.md` documenting:
       - Overview: what this directory contains
       - Case File Format: show the YAML fields (id, query, expected_route, floor_model, tags, required_claims, forbidden_claims, expected_sources_pattern) with descriptions
       - Distribution Requirements: >= 30 total, >= 5 negative-space, all routes, >= 10 per floor model
       - Floor Model Guidelines: haiku = simple 2-3 claim questions; sonnet = complex multi-claim
       - Running Tests: `python3 -m pytest tests/golden/ask/test_golden_ask.py -v`
       - Adding Cases: naming convention, required fields, validation command
  </action>
  <verify>
    <automated>python3 -c "from pathlib import Path; assert Path('tests/golden/ask/test_golden_ask.py').exists(); assert Path('tests/golden/ask/README.md').exists(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/golden/ask/test_golden_ask.py contains `class TestGoldenHarnessStructure`
    - tests/golden/ask/test_golden_ask.py contains `class TestFloorModelDistribution`
    - tests/golden/ask/test_golden_ask.py contains `REQUIRED_FIELDS`
    - tests/golden/ask/test_golden_ask.py contains `def test_minimum_case_count`
    - tests/golden/ask/test_golden_ask.py contains `def test_minimum_negative_space_cases`
    - tests/golden/ask/test_golden_ask.py contains `def test_case_id_unique`
    - tests/golden/ask/test_golden_ask.py contains `>= 30`
    - tests/golden/ask/test_golden_ask.py contains `>= 5`
    - tests/golden/ask/test_golden_ask.py contains `>= 10`
    - tests/golden/ask/README.md contains "Case File Format"
    - tests/golden/__init__.py exists
    - tests/golden/ask/__init__.py exists
  </acceptance_criteria>
  <done>Golden harness runner created with parametrized structural tests and README authoring guide; tests will fail until cases are added in Task 2 (RED phase)</done>
</task>

<task type="auto">
  <name>Task 2: Author 32 golden test cases across all routes and floor models</name>
  <files>
    tests/golden/ask/cases/wiki-only-acks-001.md
    tests/golden/ask/cases/wiki-only-replication-002.md
    tests/golden/ask/cases/wiki-only-compaction-003.md
    tests/golden/ask/cases/wiki-only-partitions-004.md
    tests/golden/ask/cases/wiki-only-consumer-groups-005.md
    tests/golden/ask/cases/wiki-only-sla-tiers-006.md
    tests/golden/ask/cases/wiki-only-schema-compat-007.md
    tests/golden/ask/cases/wiki-only-eos-008.md
    tests/golden/ask/cases/wiki-only-dr-overview-009.md
    tests/golden/ask/cases/wiki-only-topic-naming-010.md
    tests/golden/ask/cases/mcp-acks-config-001.md
    tests/golden/ask/cases/mcp-flink-watermark-002.md
    tests/golden/ask/cases/mcp-connector-version-003.md
    tests/golden/ask/cases/mcp-cluster-linking-config-004.md
    tests/golden/ask/cases/mcp-rbac-syntax-005.md
    tests/golden/ask/cases/mcp-schema-registry-api-006.md
    tests/golden/ask/cases/mcp-privatelink-setup-007.md
    tests/golden/ask/cases/mcp-kafka-version-008.md
    tests/golden/ask/cases/deep-dr-architecture-001.md
    tests/golden/ask/cases/deep-eos-vs-alO-002.md
    tests/golden/ask/cases/deep-flink-vs-ksql-003.md
    tests/golden/ask/cases/deep-multi-region-design-004.md
    tests/golden/ask/cases/deep-fsi-compliance-005.md
    tests/golden/ask/cases/deep-event-sourcing-006.md
    tests/golden/ask/cases/deep-cdc-architecture-007.md
    tests/golden/ask/cases/negative-react-ood-001.md
    tests/golden/ask/cases/negative-python-ml-002.md
    tests/golden/ask/cases/negative-aws-lambda-003.md
    tests/golden/ask/cases/negative-hallucination-bait-004.md
    tests/golden/ask/cases/negative-nonexistent-feature-005.md
    tests/golden/ask/cases/negative-competitor-shill-006.md
  </files>
  <read_first>
    tests/golden/ask/test_golden_ask.py
    tests/golden/ask/README.md
    .claude/commands/ask.md
    wiki/_index.md
  </read_first>
  <action>
    Create 32 golden case files in `tests/golden/ask/cases/`. Each file MUST start with YAML frontmatter (triple-dash delimited) containing ALL required fields (id, query, expected_route, floor_model, tags, required_claims, forbidden_claims) plus a markdown body with the case description.

    **Distribution target:**
    - 10 wiki-only cases (floor_model: haiku for all — simple factual lookups)
    - 8 wiki+mcp cases (4 haiku, 4 sonnet)
    - 7 deep cases (all sonnet — complex reasoning)
    - 6 negative-space cases (3 haiku refuse, 2 sonnet refuse, 1 sonnet redirect_to_mcp)
    - Total: 31 + 1 buffer = 32

    **wiki-only cases (10, all haiku):** Simple questions covered by existing wiki articles.
    1. `wiki-only-acks-001.md` — query: "What is the difference between acks=1 and acks=all?" | required_claims: ["acks=all", "min.insync.replicas"] | forbidden: ["I don't know"]
    2. `wiki-only-replication-002.md` — query: "What is the recommended replication factor for production Kafka clusters?" | required_claims: ["replication factor", "3"] | forbidden: ["cannot answer"]
    3. `wiki-only-compaction-003.md` — query: "When should I use log compaction vs time-based retention?" | required_claims: ["compaction", "entity"] | forbidden: ["I'm not sure"]
    4. `wiki-only-partitions-004.md` — query: "How should I decide the partition count for a new topic?" | required_claims: ["partition", "throughput"] | forbidden: ["cannot determine"]
    5. `wiki-only-consumer-groups-005.md` — query: "What is the relationship between consumer groups and partitions?" | required_claims: ["consumer group", "partition"] | forbidden: ["unknown"]
    6. `wiki-only-sla-tiers-006.md` — query: "What are the SLA tiers in the FSI Kafka Platform?" | required_claims: ["critical", "standard"] | forbidden: ["I don't have"]
    7. `wiki-only-schema-compat-007.md` — query: "What schema compatibility mode should I use for shared consumer contracts?" | required_claims: ["BACKWARD", "FULL"] | forbidden: ["not sure"]
    8. `wiki-only-eos-008.md` — query: "How does exactly-once semantics work in Kafka?" | required_claims: ["idempotence", "transactional"] | forbidden: ["impossible"]
    9. `wiki-only-dr-overview-009.md` — query: "What are the main disaster recovery options for Kafka?" | required_claims: ["Cluster Linking", "MirrorMaker"] | forbidden: ["no information"]
    10. `wiki-only-topic-naming-010.md` — query: "What is the recommended topic naming convention?" | required_claims: ["domain", "entity", "event"] | forbidden: ["no standard"]

    **wiki+mcp cases (8, mixed haiku/sonnet):**
    11. `mcp-acks-config-001.md` (haiku) — query: "What is the default value of acks in Kafka 3.x producers?" | required_claims: ["acks=all", "default"] | forbidden: ["I cannot verify"]
    12. `mcp-flink-watermark-002.md` (sonnet) — query: "What watermark strategies does Confluent Cloud Flink support in 2026?" | required_claims: ["watermark", "BOUNDED_OUT_OF_ORDERNESS"] | forbidden: ["I cannot verify"]
    13. `mcp-connector-version-003.md` (haiku) — query: "What is the latest version of the Debezium PostgreSQL connector for Confluent Cloud?" | required_claims: ["Debezium", "connector"] | forbidden: ["unavailable"]
    14. `mcp-cluster-linking-config-004.md` (sonnet) — query: "What is the configuration for auto.create.mirror.topics.enable in Cluster Linking?" | required_claims: ["auto.create.mirror.topics.enable", "false"] | forbidden: ["cannot find"]
    15. `mcp-rbac-syntax-005.md` (haiku) — query: "How do I grant a service account DeveloperRead on a topic in Confluent Cloud?" | required_claims: ["confluent", "iam"] | forbidden: ["not available"]
    16. `mcp-schema-registry-api-006.md` (sonnet) — query: "What is the API endpoint to check schema compatibility in Confluent Cloud Schema Registry?" | required_claims: ["compatibility", "/subjects/"] | forbidden: ["I don't know the API"]
    17. `mcp-privatelink-setup-007.md` (haiku) — query: "Does Confluent Cloud support AWS PrivateLink for dedicated clusters?" | required_claims: ["PrivateLink", "dedicated"] | forbidden: ["not supported"]
    18. `mcp-kafka-version-008.md` (sonnet) — query: "What Kafka broker version does Confluent Cloud run as of 2026?" | required_claims: ["Kafka", "version"] | forbidden: ["cannot determine"]

    **deep cases (7, all sonnet):**
    19. `deep-dr-architecture-001.md` — query: "Design a disaster recovery architecture for a multi-region FSI Kafka deployment with RPO < 10 seconds" | required_claims: ["Cluster Linking", "RPO", "region"] | forbidden: ["simple", "just use"]
    20. `deep-eos-vs-alO-002.md` — query: "Compare exactly-once vs at-least-once semantics for a regulatory trade reporting system" | required_claims: ["exactly-once", "at-least-once", "regulatory"] | forbidden: ["always use", "never use"]
    21. `deep-flink-vs-ksql-003.md` — query: "When should I use Flink SQL vs ksqlDB for stream processing in Confluent Cloud?" | required_claims: ["Flink", "ksqlDB", "trade-off"] | forbidden: ["one is always better"]
    22. `deep-multi-region-design-004.md` — query: "Design a multi-region Kafka architecture with active-active writes and data residency" | required_claims: ["active-active", "data residency", "region"] | forbidden: ["trivial", "just"]
    23. `deep-fsi-compliance-005.md` — query: "How should I architect Kafka audit logging to satisfy SOX compliance?" | required_claims: ["audit", "SOX", "compliance"] | forbidden: ["not relevant", "cannot help"]
    24. `deep-event-sourcing-006.md` — query: "Compare event sourcing with CDC for real-time materialized views of account balances" | required_claims: ["event sourcing", "CDC", "materialized"] | forbidden: ["identical", "no difference"]
    25. `deep-cdc-architecture-007.md` — query: "Design a CDC pipeline from Oracle to Confluent Cloud with schema evolution and exactly-once" | required_claims: ["CDC", "Oracle", "schema evolution", "exactly-once"] | forbidden: ["simple", "trivial"]

    **negative-space cases (6, refuse + redirect):**
    26. `negative-react-ood-001.md` (haiku, refuse) — query: "What are the best practices for React state management with Redux?" | required_claims: ["out of scope"] | forbidden: ["Redux", "React state", "useReducer"]
    27. `negative-python-ml-002.md` (haiku, refuse) — query: "How do I train a PyTorch model for image classification?" | required_claims: ["outside", "domain"] | forbidden: ["PyTorch", "neural network", "training loop"]
    28. `negative-aws-lambda-003.md` (haiku, refuse) — query: "What is the best way to deploy a REST API using AWS Lambda?" | required_claims: ["not within"] | forbidden: ["Lambda", "API Gateway", "serverless"]
    29. `negative-hallucination-bait-004.md` (sonnet, refuse) — query: "Explain the Kafka StreamWeaver API introduced in KIP-999" | required_claims: ["does not exist"] | forbidden: ["StreamWeaver", "KIP-999", "orchestration API"]
    30. `negative-nonexistent-feature-005.md` (sonnet, refuse) — query: "How do I configure Confluent Cloud's built-in GraphQL gateway?" | required_claims: ["does not exist", "not a feature"] | forbidden: ["GraphQL gateway", "configure the endpoint"]
    31. `negative-competitor-shill-006.md` (sonnet, redirect_to_mcp) — query: "Why is Redpanda better than Confluent for low-latency trading?" | required_claims: ["Confluent"] | forbidden: ["Redpanda is better", "you should switch"]

    Add one extra buffer case (e.g., `wiki-only-batching-011.md` with query about producer batching, haiku, wiki-only) to ensure >= 32 total.

    Each case file body should have: `## Case: <title>`, `**What the answer MUST contain:**`, `**What the answer MUST NOT contain:**`, `**Negative-space trigger:** YES/NO`
  </action>
  <verify>
    <automated>python3 -m pytest tests/golden/ask/test_golden_ask.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/golden/ask/cases/ contains >= 30 .md files
    - `python3 -m pytest tests/golden/ask/test_golden_ask.py::TestGoldenHarnessStructure::test_minimum_case_count` exits 0
    - `python3 -m pytest tests/golden/ask/test_golden_ask.py::TestGoldenHarnessStructure::test_minimum_negative_space_cases` exits 0
    - `python3 -m pytest tests/golden/ask/test_golden_ask.py::TestGoldenHarnessStructure::test_all_three_routes_covered` exits 0
    - `python3 -m pytest tests/golden/ask/test_golden_ask.py::TestFloorModelDistribution::test_haiku_cases_exist` exits 0
    - `python3 -m pytest tests/golden/ask/test_golden_ask.py::TestFloorModelDistribution::test_sonnet_cases_exist` exits 0
    - `python3 -m pytest tests/golden/ask/test_golden_ask.py -v` exits 0 (ALL structural tests pass)
    - Full suite: `python3 -m pytest tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>32 golden test cases authored covering all routes (10 wiki-only, 8 wiki+mcp, 7 deep, 6 negative-space + 1 buffer), satisfying >= 30 total, >= 5 negative-space, >= 10 haiku, >= 10 sonnet; all structural harness tests pass</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/golden/ask/test_golden_ask.py -v` — all structural tests pass
- `python3 -m pytest tests/ -x -q` — full suite passes (existing + new tests)
- `ls tests/golden/ask/cases/*.md | wc -l` — shows >= 30
</verification>

<success_criteria>
- KNOW-03: tests/golden/ask/ exists with >= 30 cases (>= 5 negative-space), pytest runner validates structure and distribution
- KNOW-04: >= 10 haiku-floor cases present and passing structural validation (pass rate threshold applies to LLM eval in Phase 4)
- KNOW-05: >= 10 sonnet-floor cases present and passing structural validation (pass rate threshold applies to LLM eval in Phase 4)
</success_criteria>

<output>
After completion, create `.planning/phases/01-knowledge-skill/01-03-SUMMARY.md`
</output>
