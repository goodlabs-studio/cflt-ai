---
phase: 00-foundation
plan: 04
type: execute
wave: 2
depends_on: ["00-02"]
files_modified:
  - wiki/activity/README.md
  - wiki/activity/2026-04.md
  - raw/repos/fsi-dsp/docs/adr/009-linuxone-kafka-offload.md
autonomous: true
requirements: [WIKI-02, WIKI-01]

must_haves:
  truths:
    - "wiki/activity/ directory exists with README.md and a seed log file"
    - "Activity log entry format supports overlay-scoped path recording"
    - "ADR-009 exists in fsi-dsp documenting LinuxONE as preferred compute for z/OS Kafka offload"
  artifacts:
    - path: "wiki/activity/README.md"
      provides: "Activity log format specification and retention rules"
      contains: "Append-only audit trail"
    - path: "wiki/activity/2026-04.md"
      provides: "Seed activity log with initial entries"
      contains: "**Overlay:**"
    - path: "raw/repos/fsi-dsp/docs/adr/009-linuxone-kafka-offload.md"
      provides: "ADR-009: LinuxONE for z/OS Kafka offload"
      contains: "Status: Accepted"
  key_links:
    - from: "wiki/activity/2026-04.md"
      to: "canon/industry/fsi/overrides.yaml"
      via: "Overlay field in activity log entries"
      pattern: "Overlay.*fsi"
---

<objective>
Create the wiki activity log directory with seed file and format specification. Author ADR-009 (LinuxONE for z/OS Kafka offload) in fsi-dsp — a prerequisite for WIKI-01 wiki article ingestion in Plan 05.

Purpose: Activity log enables immutable audit trail of all skill invocations per overlay-scoped path (WIKI-02). ADR-009 is the source material for LinuxONE wiki article compilation (WIKI-01 partial).
Output: wiki/activity/ directory with seed, ADR-009 in fsi-dsp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation/00-RESEARCH.md
@raw/repos/fsi-dsp/docs/adr/000-template.md

<interfaces>
<!-- From Plan 02: MANIFEST.yaml references adr/009 -->
MANIFEST.yaml (created by Plan 00-02) includes:
```yaml
  - id: "adr/009"
    type: adr
    name: "LinuxONE for z/OS Kafka offload"
    path: "docs/adr/009-linuxone-kafka-offload.md"
    status: proposed
```
This plan creates the actual ADR file that MANIFEST.yaml's adr/009 entry points to.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wiki activity log directory and seed file</name>
  <files>wiki/activity/README.md, wiki/activity/2026-04.md</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md
    - wiki/_index.md
  </read_first>
  <action>
Create `wiki/activity/` directory with two files:

**wiki/activity/README.md:**
```markdown
# Activity Log

Append-only audit trail of skill invocations.
One file per calendar month: `YYYY-MM.md`.
Each entry records: skill invoked, overlay active, inputs, outputs, timestamp.

Never edit entries retroactively. Archive (do not delete) files older than 12 months.

## Entry Format

```
## YYYY-MM-DDTHH:MM:SSZ
**Skill:** /{skill-name}
**Overlay:** {base | fsi | customer/{name} | engagement/{name}}
**Input:** {path or description}
**Output:** {path or "none"}
**Canon stack:** {layers active, e.g., "base + fsi"}
```

## Rules

1. Entries are append-only — never modify a past entry
2. The `Overlay` field records which canon overlay layer was active during the invocation
3. One file per calendar month; create `YYYY-MM.md` when the first entry in a new month occurs
4. Files older than 12 months should be archived to `wiki/activity/archive/`, not deleted
5. The `Canon stack` field records all active layers (e.g., "base + fsi + customer/acme")
```

**wiki/activity/2026-04.md:**
```markdown
# Activity Log — 2026-04

## 2026-04-28T00:00:00Z
**Skill:** /gsd:map-codebase
**Overlay:** base
**Input:** cflt-ai repository (initial codebase mapping)
**Output:** .planning/codebase/*.md
**Canon stack:** base

## 2026-04-28T00:30:00Z
**Skill:** /gsd:plan-phase
**Overlay:** base + fsi
**Input:** Phase 0 Foundation planning
**Output:** .planning/phases/00-foundation/00-*-PLAN.md
**Canon stack:** base + fsi
```

These are seed entries documenting the work that has already been done in this project setup phase. Future skill invocations will append new entries following the same format.
  </action>
  <verify>
    <automated>cd /Users/jhogan/cflt-ai && python3 -c "
from pathlib import Path
assert Path('wiki/activity').is_dir(), 'wiki/activity does not exist'
assert Path('wiki/activity/README.md').is_file(), 'README.md missing'
assert Path('wiki/activity/2026-04.md').is_file(), '2026-04.md missing'
content = Path('wiki/activity/README.md').read_text()
assert 'Append-only audit trail' in content, 'Missing description'
assert '**Overlay:**' in content, 'Missing overlay field'
seed = Path('wiki/activity/2026-04.md').read_text()
assert '**Skill:**' in seed, 'Missing skill field'
assert '**Canon stack:**' in seed, 'Missing canon stack field'
print('Activity log validated OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - wiki/activity/ directory exists
    - wiki/activity/README.md contains "Append-only audit trail"
    - wiki/activity/README.md contains "**Overlay:**"
    - wiki/activity/README.md contains "**Canon stack:**"
    - wiki/activity/README.md contains "Never edit entries retroactively"
    - wiki/activity/2026-04.md contains "**Skill:** /gsd:map-codebase"
    - wiki/activity/2026-04.md contains "**Overlay:** base"
    - wiki/activity/2026-04.md contains "**Canon stack:** base + fsi"
  </acceptance_criteria>
  <done>wiki/activity/ directory is live with README.md format spec and 2026-04.md seed file. Activity log supports overlay-scoped path recording.</done>
</task>

<task type="auto">
  <name>Task 2: Author ADR-009 — LinuxONE for z/OS Kafka offload</name>
  <files>raw/repos/fsi-dsp/docs/adr/009-linuxone-kafka-offload.md</files>
  <read_first>
    - raw/repos/fsi-dsp/docs/adr/000-template.md
    - raw/repos/fsi-dsp/docs/adr/001-avro-over-protobuf.md
    - CLAUDE.md
    - .planning/phases/00-foundation/00-RESEARCH.md
  </read_first>
  <action>
Author `raw/repos/fsi-dsp/docs/adr/009-linuxone-kafka-offload.md` following the ADR template:

```markdown
# ADR-009: LinuxONE as Preferred Compute for z/OS Kafka Offload

**Status:** Accepted
**Date:** 2026-04-28
**Author:** Jeremy Hogan

## Context

Financial institutions running mainframe workloads on IBM z/OS need to integrate
legacy transaction systems with modern event-driven architectures built on Apache Kafka.
Two patterns exist for bridging z/OS data into Kafka:

1. **Direct z/OS Connect**: Kafka clients run on z/OS itself, limited by zIIP engine
   capacity, JVM tuning constraints, and license cost per MSU.
2. **Offload to Linux**: Kafka producers/consumers run on a Linux partition adjacent
   to the z/OS LPAR, connected via HiperSockets or internal coupling facility links.

IBM LinuxONE (Emperor/Rockhopper III+) provides a native Linux environment on the
same physical frame as z/OS, offering sub-millisecond latency to z/OS data via
HiperSockets while running standard Linux workloads (Kafka clients, Flink, Schema
Registry) without mainframe license overhead.

The canonical bridge pattern for z/OS-to-Kafka integration is the IBM MQ Source
Connector: z/OS applications write to MQ queues (a well-understood mainframe pattern),
and the Kafka Connect MQ Source Connector on LinuxONE consumes from those queues and
produces to Kafka topics.

## Decision

We adopt IBM LinuxONE as the preferred compute platform for z/OS Kafka offload
workloads in FSI engagements. The bridge pattern is:

```
z/OS Application -> IBM MQ Queue -> MQ Source Connector (LinuxONE) -> Kafka Topic
```

Key specifics:

- **Compute**: IBM LinuxONE Emperor 4 or Rockhopper III+ running RHEL 8.x/9.x or
  Ubuntu 22.04 LTS
- **Network**: HiperSockets for z/OS-to-LinuxONE communication (sub-millisecond,
  no external network hop)
- **Bridge**: Confluent-certified IBM MQ Source Connector for Kafka Connect,
  running in distributed mode on LinuxONE
- **Serialization**: Avro with Schema Registry (per ADR-001); schemas registered
  at the connector level
- **Security**: FIPS 140-2 validated cryptographic modules on LinuxONE; mTLS between
  Kafka clients and brokers (per ADR-006)
- **DR**: LinuxONE instances in both primary and DR regions; Cluster Linking
  replicates topics cross-region (per ADR-005)

## Consequences

**Easier:**
- Mainframe teams continue writing to MQ (no application changes on z/OS)
- Sub-millisecond bridge latency suitable for `market_data` and `risk` SLA tiers
- LinuxONE inherits z/OS availability (99.999%) without mainframe software license cost
- FIPS compliance is native; no additional certification work for crypto modules
- Standard Linux tooling (Ansible, Terraform, Docker) works on LinuxONE

**Harder:**
- LinuxONE hardware procurement has long lead times (6-12 months)
- Teams unfamiliar with mainframe infrastructure need LinuxONE-specific runbooks
- MQ Source Connector requires MQ queue manager configuration on z/OS side
  (coordination with mainframe team)
- HiperSockets configuration requires IBM z/VM or PR/SM setup expertise
- Not all Confluent components are certified for s390x architecture; verify
  connector and client versions before deployment

**Mitigations:**
- Provide pre-built Ansible roles for LinuxONE Kafka Connect deployment
  (future: `role/linuxone_connect`)
- Document MQ queue manager setup in partnership with IBM mainframe practice
- Maintain a LinuxONE compatibility matrix for Confluent component versions
```
  </action>
  <verify>
    <automated>cd /Users/jhogan/cflt-ai && python3 -c "
from pathlib import Path
adr = Path('raw/repos/fsi-dsp/docs/adr/009-linuxone-kafka-offload.md')
assert adr.is_file(), 'ADR-009 does not exist'
content = adr.read_text()
assert 'Status: Accepted' in content or 'Status:** Accepted' in content, 'Missing accepted status'
assert 'LinuxONE' in content, 'Missing LinuxONE reference'
assert 'MQ Source Connector' in content, 'Missing MQ bridge pattern'
assert 'HiperSockets' in content, 'Missing HiperSockets reference'
assert 'FIPS' in content, 'Missing FIPS reference'
print('ADR-009 validated OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - raw/repos/fsi-dsp/docs/adr/009-linuxone-kafka-offload.md exists
    - ADR-009 contains "Status:** Accepted"
    - ADR-009 contains "LinuxONE" in the title
    - ADR-009 contains "IBM MQ Source Connector" as the bridge pattern
    - ADR-009 contains "HiperSockets" for z/OS-to-LinuxONE connectivity
    - ADR-009 contains "FIPS 140-2" for compliance
    - ADR-009 references ADR-001, ADR-005, and ADR-006 for Avro, Cluster Linking, and auth
    - ADR-009 follows the 000-template.md structure (Context, Decision, Consequences)
  </acceptance_criteria>
  <done>ADR-009 authored in fsi-dsp documenting LinuxONE as preferred compute for z/OS Kafka offload with MQ Source Connector bridge pattern, FIPS compliance, and DR integration.</done>
</task>

</tasks>

<verification>
1. `ls wiki/activity/` shows README.md and 2026-04.md
2. `cat wiki/activity/2026-04.md` shows properly formatted seed entries with Overlay and Canon stack fields
3. `cat raw/repos/fsi-dsp/docs/adr/009-linuxone-kafka-offload.md` shows complete ADR with Context, Decision, Consequences
</verification>

<success_criteria>
- wiki/activity/ directory is live with format spec and seed log (WIKI-02)
- ADR-009 authored in fsi-dsp (WIKI-01 prerequisite — article compilation in Plan 05)
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-04-SUMMARY.md`
</output>
