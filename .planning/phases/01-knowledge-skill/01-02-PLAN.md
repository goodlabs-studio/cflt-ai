---
phase: 01-knowledge-skill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ask.md
  - .claude/commands/wiki/recommend.md
  - wiki/_queue.md
autonomous: true
requirements: [KNOW-01, KNOW-02, WIKI-05]

must_haves:
  truths:
    - "Running /ask with --mode ephemeral produces an answer without writing files"
    - "Running /ask with --mode report writes output to outputs/reports/"
    - "Running /ask with --mode reconsolidate writes back to wiki"
    - "/wiki:recommend dispatches to /ask --mode reconsolidate"
    - "The triage classifier routes queries to wiki-only, wiki+MCP, or deep based on heuristics"
    - "--force-route flag bypasses the classifier"
    - "Wiki misses append an auto-stub entry to wiki/_queue.md"
  artifacts:
    - path: ".claude/commands/ask.md"
      provides: "Unified /ask skill with mode routing, triage classifier, and auto-stub logic"
      contains: "Step 1.5"
    - path: ".claude/commands/wiki/recommend.md"
      provides: "Thin alias dispatching to /ask --mode reconsolidate"
      min_lines: 5
    - path: "wiki/_queue.md"
      provides: "Auto-Stubs section for coverage gap tracking"
      contains: "## Auto-Stubs"
  key_links:
    - from: ".claude/commands/ask.md"
      to: "tools/wiki-search.py"
      via: "Step 1.5 classifier runs wiki-search for hit scoring"
      pattern: "wiki-search"
    - from: ".claude/commands/ask.md"
      to: "wiki/_queue.md"
      via: "auto-stub append on wiki miss"
      pattern: "_queue.md"
    - from: ".claude/commands/wiki/recommend.md"
      to: ".claude/commands/ask.md"
      via: "alias dispatch with --mode reconsolidate"
      pattern: "reconsolidate"
---

<objective>
Consolidate /ask and /wiki:recommend into a single unified skill with --mode routing, triage classifier, and auto-stub coverage gap tracking.

Purpose: Three personas (IC/SE, Embedded SA, SRE) all use /ask with a mode flag instead of remembering separate commands. The triage classifier ensures queries are routed optimally (wiki-only for covered topics, wiki+MCP for config validation, deep for architecture synthesis). Auto-stub ensures wiki coverage gaps are never lost.
Output: Rewritten ask.md skill, thin recommend.md alias, and _queue.md with Auto-Stubs section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-knowledge-skill/01-CONTEXT.md
@.planning/phases/01-knowledge-skill/01-RESEARCH.md

@.claude/commands/ask.md
@.claude/commands/wiki/recommend.md
@.claude/commands/wiki/references/article-format.md
@.claude/commands/wiki/references/quality-standards.md
@wiki/_queue.md
@tools/wiki-search.py

<interfaces>
<!-- Current ask.md structure (to extend, not replace): -->
- Step 1: Understand the question
- Step 2: Search the wiki
- Step 3: Apply Confluent Canon
- Step 4: Validate against MCP sources
- Step 5: Produce response

<!-- Current recommend.md structure (7 steps to consolidate): -->
- Step 1: Recall (search wiki)
- Step 2: Draft (write report)
- Step 3: Verify (check claims via MCP)
- Step 4: Correct (apply fixes)
- Step 5: Write report (to outputs/reports/)
- Step 6: Reconsolidate (write back to wiki)
- Step 7: Print summary

<!-- wiki/_queue.md existing sections: -->
- ## Stubs to Create
- ## Articles to Expand
- ## Unverified Claims to Resolve
- ## Lint Findings
- ## Candidate Articles (from lint / Q&A sessions)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ask.md with mode routing, triage classifier, decay check, and auto-stub</name>
  <files>.claude/commands/ask.md</files>
  <read_first>
    .claude/commands/ask.md
    .claude/commands/wiki/recommend.md
    .claude/commands/wiki/references/quality-standards.md
    wiki/_queue.md
  </read_first>
  <action>
    Rewrite `.claude/commands/ask.md` to be the unified knowledge skill. The new structure preserves the existing 5-step process but inserts new steps and adds mode-conditional behavior. The full file content should be:

    **Header section:**
    ```
    # /ask — Unified Confluent Knowledge Skill

    You are answering a Confluent/Kafka/Flink question using the cflt-ai knowledge system.
    The user will paste a config snippet, architecture question, error message, or document excerpt.

    ## Input

    $ARGUMENTS
    ```

    **Step 1: Parse arguments** (NEW — replaces old "Understand the question"):
    ```
    ## Step 1: Parse arguments

    Parse `$ARGUMENTS`:
    - Extract `--mode` value: `ephemeral` (default) | `report` | `reconsolidate`
    - Extract `--force-route` value if present: `wiki` | `mcp` | `deep`
    - Remaining text after flags is the query.
    - If no `--mode` is provided, default to `ephemeral`.
    - If `--mode` value is unrecognized, stop and print:
      `Error: unknown --mode value. Valid: ephemeral | report | reconsolidate`
    - If `--force-route` value is unrecognized, stop and print:
      `Error: unknown --force-route value. Valid: wiki | mcp | deep`
    ```

    **Step 1.5: Route the query** (NEW — triage classifier per CONTEXT.md):
    ```
    ## Step 1.5: Route the query

    If `--force-route` was specified, use that route and skip to the appropriate step. Otherwise:

    1. Search wiki using `Grep` and `Glob` across `wiki/concepts/` and `wiki/patterns/` for query terms.
    2. Count how many distinct articles match (have relevant content for the query).
    3. Apply routing rules:
       - **wiki-only**: 3+ wiki articles match AND query does not contain version numbers,
         config property names (dotted like `acks.all`), connector-specific syntax, or
         "does X support Y" phrasing
       - **wiki+MCP**: Query contains version numbers, config keys (dotted names), API names,
         or "does X support Y" phrasing; OR fewer than 3 wiki articles match but topic is
         clearly in-domain
       - **deep**: Query is multi-topic ("compare X and Y for Z use case"),
         architecture-design ("design me a..."), or trade-off analysis ("when should I use X vs Y for...")
       - **refuse**: Query is clearly out-of-domain (not Confluent/Kafka/Flink/Schema Registry/streaming/FSI data platform related)
    4. Log the route decision: `[ROUTE: <route>]`

    Routing determines which steps execute:
    - **wiki-only**: Steps 2, 3, 5 (skip MCP validation)
    - **wiki+MCP**: Steps 2, 3, 4, 5 (full pipeline)
    - **deep**: Steps 2, 3, 4, 5 with extended analysis (multi-source synthesis, explicit trade-off tables)
    - **refuse**: Skip to Step 5 with a polite refusal explaining this is outside the Confluent/streaming domain
    ```

    **Step 2: Search the wiki** (EXTENDED with decay check and auto-stub):
    ```
    ## Step 2: Search the wiki

    Search the wiki directory for relevant articles:
    - Use `Grep` and `Glob` to find matching articles in `wiki/concepts/` and `wiki/patterns/`
    - Check `wiki/_index.md` for relevant cross-references
    - Read the most relevant articles (up to 3-4)

    **Decay check (WIKI-04):** For each article read, check its `last_validated` field in front matter.
    If `last_validated` is more than 90 days ago AND `confidence: high`, note this in the response
    as: "Note: [article] has not been revalidated since [date] — confidence may have degraded."

    **Auto-stub on miss (WIKI-05):** If NO wiki articles match the query (zero relevant results):
    1. Extract a topic slug from the query: lowercase, remove non-alphanumeric except spaces/hyphens, take first 5 words, join with hyphens
    2. Read `wiki/_queue.md`
    3. Check if ANY existing line under `## Auto-Stubs` contains the primary keyword from the query
    4. If not already present, append to `wiki/_queue.md` under the `## Auto-Stubs` section:
       ```
       - [ ] <!-- auto-stub: <slug> --> wiki/concepts/<slug>.md — Auto-queued from /ask
             Query: "<original query>" | Date: <YYYY-MM-DD> | Mode: <mode>
       ```
    5. Continue answering from canon + MCP even though wiki had no hit.
    ```

    **Step 3: Apply Confluent Canon** (unchanged from current):
    ```
    ## Step 3: Apply Confluent Canon

    Cross-reference the question against the canonical defaults in CLAUDE.md:
    - Cluster/topic design, producer/consumer config, Schema Registry, Flink SQL, Cluster Linking, security
    - FSI overlay if the context is financial services
    - Flag any deviations from canon in the input
    ```

    **Step 4: Validate against MCP sources** (unchanged logic, skipped for wiki-only route):
    ```
    ## Step 4: Validate against MCP sources

    *Skip this step if route is wiki-only.*

    For claims that need live verification:
    - Use `confluent-docs` MCP for config syntax, API references, version-specific behavior
    - Use `context7` MCP for architecture patterns and best practices
    - Note what was confirmed, what was corrected, and what couldn't be verified
    ```

    **Step 5: Produce response** (mode-conditional output):
    ```
    ## Step 5: Produce response

    Output depends on both the route and the mode:

    ### All modes — base response structure:

    ---

    ### Answer

    [Direct, opinionated answer. Lead with the recommendation. Explain trade-offs where relevant.]
    [If route was "refuse": polite refusal explaining this is outside the Confluent/streaming domain.]
    [If route was "deep": include explicit trade-off table and multi-source synthesis.]

    ### Route

    `[ROUTE: <route>]`

    ### Wiki Sources Consulted

    - `wiki/concepts/relevant-article.md` — [one-line summary of what was used]
    - (or "No wiki articles matched this query" if auto-stub was triggered)

    ### MCP Validation

    | Claim | Source | Result |
    |-------|--------|--------|
    | [specific claim checked] | confluent-docs / context7 | Confirmed / Corrected / Unverifiable |

    (Omit this section if route was wiki-only)

    ### Canon Compliance

    [Note any deviations from Confluent Canon defaults. If the input follows canon, say so briefly.]

    ---

    ### Mode-specific behavior AFTER producing the response:

    **ephemeral (default):** Do NOT write any files. Response is terminal.

    **report:**
    1. Ensure `outputs/reports/` directory exists (create if missing)
    2. Save the response to `outputs/reports/<slug>.md` where slug is derived from the query
    3. Add a validation footer: `*Validated against Confluent docs via MCP (YYYY-MM-DD). N claims checked, M corrected, K unverifiable.*`
    4. Tell the user: "Report saved to outputs/reports/<slug>.md"

    **reconsolidate:**
    1. Do everything in "report" mode above
    2. Additionally, check if MCP validation revealed corrections to existing wiki articles:
       - If yes: update those wiki articles now, bump their `last_updated` and `last_validated` fields
    3. If topic gaps were found (wiki miss), they're already in _queue.md via auto-stub
    4. Update `wiki/_index.md` if any article was modified
    5. Tell the user a summary: report path, claims checked, corrections applied, articles updated, stubs queued
    ```

    **Rules section** (updated):
    ```
    ## Rules

    - Be direct and opinionated. This is a peer conversation with Confluent SEs.
    - Mode determines write behavior, NOT MCP call behavior. All modes can call MCP if route requires it.
    - The triage classifier is advisory — if you're uncertain about the route, prefer wiki+MCP over wiki-only (err toward more validation).
    - If MCP servers are unavailable, answer from wiki + canon and note the gap.
    - Keep the answer concise. Depth where it matters, brevity everywhere else.
    - For deep route: invest more tokens in analysis, comparison tables, and trade-off reasoning.
    - Auto-stub fires on ALL modes (including ephemeral) — coverage gaps are always tracked.
    ```
  </action>
  <verify>
    <automated>grep -c "Step 1.5" .claude/commands/ask.md && grep -c "force-route" .claude/commands/ask.md && grep -c "auto-stub" .claude/commands/ask.md && grep -c "reconsolidate" .claude/commands/ask.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/ask.md contains "## Step 1: Parse arguments"
    - .claude/commands/ask.md contains "--mode" and "ephemeral | report | reconsolidate"
    - .claude/commands/ask.md contains "## Step 1.5: Route the query"
    - .claude/commands/ask.md contains "--force-route"
    - .claude/commands/ask.md contains "wiki-only" and "wiki+MCP" and "deep" and "refuse"
    - .claude/commands/ask.md contains "## Auto-Stubs"
    - .claude/commands/ask.md contains "auto-stub:" (the HTML comment dedup marker format)
    - .claude/commands/ask.md contains "Decay check (WIKI-04)"
    - .claude/commands/ask.md contains "## Step 5: Produce response" with mode-conditional sections
    - .claude/commands/ask.md contains "[ROUTE:" log format
    - .claude/commands/ask.md does NOT contain "Do NOT write back to the wiki" (old rule removed)
  </acceptance_criteria>
  <done>/ask skill accepts --mode flag with three valid values, includes Step 1.5 triage classifier with four routes, checks decay on articles read, appends auto-stubs on wiki miss, and produces mode-conditional output (ephemeral=no writes, report=file, reconsolidate=file+wiki writeback)</done>
</task>

<task type="auto">
  <name>Task 2: Convert recommend.md to thin alias and add Auto-Stubs section to _queue.md</name>
  <files>
    .claude/commands/wiki/recommend.md
    wiki/_queue.md
  </files>
  <read_first>
    .claude/commands/wiki/recommend.md
    wiki/_queue.md
    .claude/commands/ask.md
  </read_first>
  <action>
    1. Replace the entire content of `.claude/commands/wiki/recommend.md` with this thin alias:

    ```markdown
    # /wiki:recommend — Alias for /ask --mode reconsolidate

    This command is an alias. It dispatches to `/ask` with `--mode reconsolidate`.

    The user's question is: $ARGUMENTS

    Execute the /ask skill with these arguments: `--mode reconsolidate $ARGUMENTS`

    Follow all steps in `.claude/commands/ask.md` with `--mode reconsolidate` set.
    Read `.claude/commands/ask.md` and execute it with the mode pre-set to reconsolidate.
    ```

    2. Add an `## Auto-Stubs` section to `wiki/_queue.md` as the LAST section (after "## Candidate Articles"):

    Append to the end of the file:
    ```markdown

    ## Auto-Stubs

    <!-- Auto-generated by /ask when wiki search returns zero results. Deduped by topic keyword. -->
    <!-- format: - [ ] <!-- auto-stub: <slug> --> wiki/concepts/<slug>.md — Auto-queued from /ask -->
    <!--         Query: "<query>" | Date: YYYY-MM-DD | Mode: <mode> -->
    ```
  </action>
  <verify>
    <automated>grep -c "reconsolidate" .claude/commands/wiki/recommend.md && grep -c "Auto-Stubs" wiki/_queue.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/wiki/recommend.md contains "Alias for /ask --mode reconsolidate"
    - .claude/commands/wiki/recommend.md contains "$ARGUMENTS"
    - .claude/commands/wiki/recommend.md does NOT contain "Step 1: Recall" (old content gone)
    - .claude/commands/wiki/recommend.md is fewer than 15 lines total
    - wiki/_queue.md contains "## Auto-Stubs"
    - wiki/_queue.md contains "auto-stub:" in a comment showing the dedup format
    - wiki/_queue.md "## Auto-Stubs" section appears after "## Candidate Articles"
    - `python3 -m pytest tests/ -x -q` exits 0 (no regressions)
  </acceptance_criteria>
  <done>/wiki:recommend is a thin alias dispatching to /ask --mode reconsolidate, and wiki/_queue.md has the ## Auto-Stubs section ready to receive coverage gap entries from /ask</done>
</task>

</tasks>

<verification>
- `.claude/commands/ask.md` contains all six steps (1, 1.5, 2, 3, 4, 5) with mode routing and classifier
- `.claude/commands/wiki/recommend.md` is a thin alias (< 15 lines)
- `wiki/_queue.md` has `## Auto-Stubs` section
- `python3 -m pytest tests/ -x -q` — full suite still passes (no regressions from skill file changes)
</verification>

<success_criteria>
- KNOW-01: /ask accepts --mode (ephemeral | report | reconsolidate); /wiki:recommend dispatches to /ask --mode reconsolidate
- KNOW-02: Step 1.5 triage classifier routes to wiki-only / wiki+MCP / deep / refuse with explicit heuristic rules; --force-route bypasses classifier
- WIKI-05: Auto-stub logic documented in ask.md Step 2; _queue.md has receiving section; fires on all modes when wiki search returns zero results
</success_criteria>

<output>
After completion, create `.planning/phases/01-knowledge-skill/01-02-SUMMARY.md`
</output>
