---
phase: 10-accelerator-artifact-type-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - raw/repos/fsi-dsp/MANIFEST.yaml
autonomous: true
requirements:
  - MAN-01
must_haves:
  truths:
    - "raw/repos/fsi-dsp/MANIFEST.yaml contains a capability entry with id 'accelerator/confluent-on-linuxone' and type 'accelerator'"
    - "The entry declares all 5 kustomize layers under apply_sequence with per-layer canon_key values"
    - "The entry declares build_command, dry_run_command, and apply_command strings"
    - "The fsi-dsp feature branch 'feat/manifest-accelerator-type' contains exactly one commit registering the entry"
    - "Existing fsi-dsp test/CI behavior is unchanged (no removed IDs, MANIFEST still parses)"
  artifacts:
    - path: "raw/repos/fsi-dsp/MANIFEST.yaml"
      provides: "MANIFEST entry for accelerator/confluent-on-linuxone"
      contains: "accelerator/confluent-on-linuxone"
  key_links:
    - from: "raw/repos/fsi-dsp/MANIFEST.yaml"
      to: "raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/"
      via: "path field on capability entry"
      pattern: "path: \"accelerators/confluent-on-linuxone\""
    - from: "raw/repos/fsi-dsp/MANIFEST.yaml"
      to: "raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/layers/01-rbac"
      via: "apply_sequence[0].path"
      pattern: "accelerators/confluent-on-linuxone/layers/01-rbac"
---

<objective>
Add `type: accelerator` to upstream fsi-dsp's MANIFEST.yaml by registering the `accelerators/confluent-on-linuxone/` tree as a first-class capability with a 5-layer `apply_sequence`. Work lands **inside the submodule** on a feature branch so the upstream PR can be opened by the user in plan 10-03 while cflt-ai-side validator work in 10-02 proceeds against the on-disk shape.

Purpose: Land the type contract that v2.1 Phase 11 (act-rail wiring) will consume. Per-layer `canon_key` placement is the single source of truth that prevents the G.2c-style fsi-dsp/cflt-ai duplication failure mode.

Output: One commit inside `raw/repos/fsi-dsp/` on branch `feat/manifest-accelerator-type` advancing the MANIFEST and an updated parent-pointer in cflt-ai (the parent-pointer bump is what 10-02 picks up).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-accelerator-artifact-type-registration/10-CONTEXT.md
@raw/repos/fsi-dsp/MANIFEST.yaml
@raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/DESIGN.md

<interfaces>
<!-- Existing MANIFEST.yaml schema shape (extracted from raw/repos/fsi-dsp/MANIFEST.yaml). -->
<!-- Every capability entry has at minimum: id, type, name, path, description. -->
<!-- Capabilities live under top-level `capabilities:` array, after version/schema header. -->

Current MANIFEST.yaml header:
```yaml
version: "1.1.0"
schema: "fsi-dsp/manifest/v1"

capabilities:
  - id: "role/cp_topic"
    type: ansible-role
    ...
```

Existing type values in use: ansible-role (9), terraform-module (2), scenario (6), adr (10), reference (10), script (7), observability (7).

Submodule on-disk shape (verified — Phase 9 bump landed):
- raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/{DESIGN.md, KNOWN-GAPS.md, MIGRATION.md, RUNBOOK.md, README.md, ATTRIBUTION.md, kustomization.yaml, base/, overlays/, layers/}
- layers/{01-rbac, 02-tls, 03-schema-governance, 04-audit, 05-flink}
- overlays/prod (kustomize build target)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register accelerator capability in raw/repos/fsi-dsp/MANIFEST.yaml on a feature branch</name>
  <files>raw/repos/fsi-dsp/MANIFEST.yaml</files>
  <read_first>
    - raw/repos/fsi-dsp/MANIFEST.yaml (entire file; understand existing entry shapes and where each type block lives so the new accelerator block is inserted at a logical position — recommend appending after the final `observability/metrics-mapping` entry on line ~374, as a new section)
    - .planning/phases/10-accelerator-artifact-type-registration/10-CONTEXT.md `<decisions>` block (locked schema shape — copy verbatim)
    - raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/DESIGN.md (first 20 lines — confirm description wording matches upstream's own framing of the accelerator)
  </read_first>
  <action>
    Step 1 — Branch inside the submodule:
    ```bash
    cd raw/repos/fsi-dsp
    git fetch origin
    git checkout -b feat/manifest-accelerator-type origin/main
    ```

    Step 2 — Append the new section to MANIFEST.yaml after line ~374 (the last observability entry `observability/metrics-mapping`). Insert verbatim from the locked CONTEXT.md decision block, with `description` lightly aligned to DESIGN.md framing if it reads better. Final block to append:

    ```yaml

      # ──────────────────────────────────────────────────
      # Accelerators (1)
      # ──────────────────────────────────────────────────
      - id: "accelerator/confluent-on-linuxone"
        type: accelerator
        name: "confluent-on-linuxone"
        path: "accelerators/confluent-on-linuxone"
        description: "Confluent Platform on IBM LinuxONE — FSI-hardened RBAC + mTLS + SR governance + audit + Flink layers via Kustomize Components over Mondics's upstream base"
        apply_sequence:
          - layer: "01-rbac"
            path: "accelerators/confluent-on-linuxone/layers/01-rbac"
            canon_key: "fsi.security.mds-rbac"
          - layer: "02-tls"
            path: "accelerators/confluent-on-linuxone/layers/02-tls"
            canon_key: "fsi.security.tls-fips"
          - layer: "03-schema-governance"
            path: "accelerators/confluent-on-linuxone/layers/03-schema-governance"
            canon_key: "fsi.schema.compatibility-full-transitive"
          - layer: "04-audit"
            path: "accelerators/confluent-on-linuxone/layers/04-audit"
            canon_key: "fsi.audit.events-retention"
          - layer: "05-flink"
            path: "accelerators/confluent-on-linuxone/layers/05-flink"
            canon_key: "fsi.flink.environment-mtls"
        build_command: "kustomize build overlays/prod"
        dry_run_command: "kustomize build overlays/prod | oc apply --dry-run=server -f -"
        apply_command: "kustomize build overlays/prod | oc apply -f -"
    ```

    **Why these values are locked here** (do not deviate):
    - `id: "accelerator/confluent-on-linuxone"` — prefix matches existing ID convention (`role/`, `module/`, `scenario/`, etc.); enforced by `test_ids_have_type_prefix` (must add `accelerator/` to that test in 10-02).
    - `apply_sequence` as ordered list — RBAC → audit → Flink is dependency order; the executor in Phase 11 walks top-to-bottom.
    - `canon_key` per-layer — single source of truth for layer→canon mapping; Phase 11's MODULE_TO_CANON_KEY is derived FROM this, not declared independently (G.2c cleanup lesson).
    - Three command fields explicit — future Helm/Terraform accelerators reuse the schema without cflt-ai changes.

    Step 3 — Do **not** edit fsi-dsp/CLAUDE.md. Verified during planning that file is auto-generated (no curated schema-contract section). Schema documentation lands in cflt-ai's CONTRIBUTING.md / tools/manifest-schema.md instead (10-02 Task 4) and in the upstream PR body (10-03).

    Step 4 — Commit inside the submodule:
    ```bash
    cd raw/repos/fsi-dsp
    git add MANIFEST.yaml
    git commit -m "$(cat <<'EOF'
    feat(MANIFEST): register accelerator artifact-type with apply_sequence schema

    Adds the first `type: accelerator` capability registering
    accelerators/confluent-on-linuxone/ as a five-layer kustomize artifact.

    Schema additions for the new type:
    - apply_sequence: ordered list of {layer, path, canon_key} objects;
      layers execute top-to-bottom (RBAC → TLS → SR → audit → Flink).
    - build_command / dry_run_command / apply_command: explicit per-artifact
      so future Helm/Terraform accelerators reuse the schema unchanged.

    Per-layer canon_key co-locates the layer→canon mapping with the artifact
    declaration — downstream consumers (cflt-ai's act rail) read this, not a
    duplicated map.

    Refs cflt-ai v2.1 MAN-01.
    EOF
    )"
    ```

    Step 5 — Return to cflt-ai parent and stage the submodule pointer bump (the actual `git add raw/repos/fsi-dsp` happens in 10-02 Task 1 alongside the validator commit; here, just verify the pointer moved):
    ```bash
    cd /Users/jhogan/cflt-ai
    git status raw/repos/fsi-dsp   # should report "new commits" (modified)
    git diff raw/repos/fsi-dsp     # should show the SHA advance
    ```

    Do NOT push the submodule branch yet. Do NOT open the upstream PR. Both happen in 10-03 with the human-action checkpoint.
  </action>
  <verify>
    <automated>cd /Users/jhogan/cflt-ai && python3 -c "
import yaml
m = yaml.safe_load(open('raw/repos/fsi-dsp/MANIFEST.yaml'))
caps = {c['id']: c for c in m['capabilities']}
acc = caps.get('accelerator/confluent-on-linuxone')
assert acc is not None, 'accelerator entry missing'
assert acc['type'] == 'accelerator'
assert acc['path'] == 'accelerators/confluent-on-linuxone'
assert isinstance(acc.get('apply_sequence'), list) and len(acc['apply_sequence']) == 5
expected_layers = ['01-rbac', '02-tls', '03-schema-governance', '04-audit', '05-flink']
expected_canon = ['fsi.security.mds-rbac', 'fsi.security.tls-fips', 'fsi.schema.compatibility-full-transitive', 'fsi.audit.events-retention', 'fsi.flink.environment-mtls']
for i, (layer, ck) in enumerate(zip(expected_layers, expected_canon)):
    assert acc['apply_sequence'][i]['layer'] == layer, f'layer {i}: got {acc[\"apply_sequence\"][i][\"layer\"]}'
    assert acc['apply_sequence'][i]['canon_key'] == ck, f'canon_key {i}: got {acc[\"apply_sequence\"][i][\"canon_key\"]}'
    assert acc['apply_sequence'][i]['path'] == f'accelerators/confluent-on-linuxone/layers/{layer}'
for cmd in ['build_command', 'dry_run_command', 'apply_command']:
    assert isinstance(acc.get(cmd), str) and acc[cmd], f'missing {cmd}'
print('OK: accelerator MANIFEST entry valid')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accelerator/confluent-on-linuxone" raw/repos/fsi-dsp/MANIFEST.yaml` returns `1`
    - `grep -c "type: accelerator" raw/repos/fsi-dsp/MANIFEST.yaml` returns `1`
    - All 5 canon_key strings present verbatim: `fsi.security.mds-rbac`, `fsi.security.tls-fips`, `fsi.schema.compatibility-full-transitive`, `fsi.audit.events-retention`, `fsi.flink.environment-mtls`
    - All 5 layer paths present verbatim: `accelerators/confluent-on-linuxone/layers/01-rbac` through `05-flink`
    - `build_command`, `dry_run_command`, `apply_command` strings all present and non-empty
    - `cd raw/repos/fsi-dsp && git log feat/manifest-accelerator-type --oneline -1` shows the new commit
    - `cd raw/repos/fsi-dsp && git diff origin/main --stat` shows only MANIFEST.yaml modified
    - `cd raw/repos/fsi-dsp && python3 -c "import yaml; yaml.safe_load(open('MANIFEST.yaml'))"` parses cleanly
  </acceptance_criteria>
  <done>
    raw/repos/fsi-dsp/MANIFEST.yaml has the verbatim accelerator entry committed to branch `feat/manifest-accelerator-type` inside the submodule. cflt-ai's working tree shows the submodule pointer advanced (uncommitted at parent level — 10-02 picks up the parent commit).
  </done>
</task>

</tasks>

<verification>
- Inside submodule: `python3 -c "import yaml; yaml.safe_load(open('raw/repos/fsi-dsp/MANIFEST.yaml'))"` parses successfully
- Inside submodule: `cd raw/repos/fsi-dsp && git log feat/manifest-accelerator-type --oneline -1` shows exactly one new commit
- Inside submodule: `cd raw/repos/fsi-dsp && python3 scripts/check-manifest-stability.py` (locally; will warn about origin/main being unreachable in a feature branch but must not flag removed IDs — exit 0 acceptable as "first introduction" path or proves no ID removal)
- Parent repo: `git status` shows `modified: raw/repos/fsi-dsp (new commits)`
</verification>

<success_criteria>
- accelerator/confluent-on-linuxone is a registered capability in upstream fsi-dsp's MANIFEST.yaml (submodule feature branch)
- All 5 layers + 5 canon_keys + 3 commands present verbatim per locked CONTEXT decision
- No existing IDs removed; existing entries unchanged
- cflt-ai's submodule pointer ready to bump (handed off to 10-02)
</success_criteria>

<output>
After completion, create `.planning/phases/10-accelerator-artifact-type-registration/10-01-SUMMARY.md`
</output>
