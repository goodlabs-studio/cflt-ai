---
phase: 12-wiki-ingest-of-linuxone-accelerator
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - .claude/commands/review.md
  - tests/golden/review/fixtures/linuxone-auditor-isolation-violation.md
  - tests/golden/review/fixtures/linuxone-fips-skip.md
  - tests/golden/review/fixtures/linuxone-connect-image-x86.md
  - tests/golden/review/fixtures/linuxone-sql-runner-skip.md
  - tests/golden/review/fixtures/linuxone-checkpoint-unencrypted.md
  - tests/golden/review/cases/review-linuxone-auditor-isolation-014.md
  - tests/golden/review/cases/review-linuxone-fips-skip-015.md
  - tests/golden/review/cases/review-linuxone-connect-image-x86-016.md
  - tests/golden/review/cases/review-linuxone-sql-runner-skip-017.md
  - tests/golden/review/cases/review-linuxone-checkpoint-unencrypted-018.md
  - tests/golden/ask/cases/linuxone-ref-arch-deploy-101.md
  - tests/golden/ask/cases/linuxone-cluster-linking-migration-102.md
  - tests/golden/ask/cases/linuxone-fips-at-install-103.md
  - tests/golden/ask/cases/linuxone-auditor-readonly-rbac-104.md
  - tests/golden/ask/cases/linuxone-s390x-image-build-105.md
  - tests/golden/ask/cases/linuxone-flink-fsi-jobs-106.md
  - tests/golden/ask/cases/linuxone-fsi-deploy-cross-cutting-107.md
  - tests/golden/ask/cases/linuxone-accelerator-dispatch-cross-cutting-108.md
  - tests/golden/ask/cases/linuxone-migration-evidence-cross-cutting-109.md
  - tests/golden/ask/cases/linuxone-fips-gap-g02-cross-cutting-110.md
autonomous: true
requirements: [WIKI-01, WIKI-03, WIKI-05]
must_haves:
  truths:
    - "User running /review against a doc claiming cluster-scoped DeveloperRead suffices for auditor isolation gets a flagged claim with the canonical correction citing wiki/patterns/auditor-readonly-rbac-payload-isolation.md"
    - "User running /ask 'How do I deploy Confluent Platform on IBM LinuxONE for FSI?' gets an answer citing wiki/patterns/linuxone-on-cfk-reference-architecture.md (WIKI-01)"
    - "Golden harness has ≥10 /ask cases tagged linuxone covering all 6 articles + 4 cross-cutting topics"
    - "Golden harness has ≥5 /review cases at EVAL-02 floor: 1 auditor-isolation + 4 trip-wire-as-claim violations (G-02, G-08, G-12, G-13)"
    - "pytest tests/golden/ask/test_golden_ask.py + tests/golden/review/test_golden_review.py pass with ≥15 new cases discovered"
  artifacts:
    - path: ".claude/commands/review.md"
      provides: "Auditor-readonly claim-flag rule injected as Step 4.1 — 3 paraphrase patterns + canonical correction text"
      contains: "DeveloperRead is consume-granting"
    - path: "tests/golden/review/cases/review-linuxone-auditor-isolation-014.md"
      provides: "Canonical /review case exercising the auditor-isolation claim-flag rule"
      contains: "auditor-isolation"
    - path: "tests/golden/ask/cases/linuxone-ref-arch-deploy-101.md"
      provides: "/ask case satisfying WIKI-01 — query matches the success criterion verbatim"
      contains: "linuxone-on-cfk-reference-architecture"
  key_links:
    - from: ".claude/commands/review.md"
      to: "wiki/patterns/auditor-readonly-rbac-payload-isolation.md"
      via: "canonical correction text cites the article path"
      pattern: "auditor-readonly-rbac-payload-isolation"
    - from: "tests/golden/review/cases/review-linuxone-auditor-isolation-014.md"
      to: "tests/golden/review/fixtures/linuxone-auditor-isolation-violation.md"
      via: "input_files: list in YAML frontmatter"
      pattern: "linuxone-auditor-isolation-violation"
    - from: "tests/golden/ask/cases/linuxone-ref-arch-deploy-101.md"
      to: "wiki/patterns/linuxone-on-cfk-reference-architecture.md"
      via: "required_claims grep target on article slug"
      pattern: "linuxone-on-cfk-reference-architecture"
---

<objective>
Wire the auditor-readonly payload-isolation claim-flag rule into the `/review` skill (`.claude/commands/review.md` as Step 4.1), author 5 golden review fixtures + 5 review cases, and add 10 golden ask cases covering the 6 new articles + 4 cross-cutting topics. After this plan, WIKI-01 is satisfied by the deploy case verbatim, WIKI-03 by the review claim-flag rule + auditor case, WIKI-05 by ≥15 new golden cases at EVAL-02 floor.

Purpose: Closes the largest scope of Phase 12 — the customer-facing user-journey requirements (WIKI-01 + WIKI-03) and the EVAL-02 quality bar (WIKI-05). Depends on Plan 12-01 (articles must exist for /ask cases to cite them and for the review rule's canonical correction to point to a real path).

Output: 1 skill spec edit + 5 review fixtures + 5 review cases + 10 ask cases = 21 new files + 1 edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-wiki-ingest-of-linuxone-accelerator/12-CONTEXT.md
@.planning/phases/12-wiki-ingest-of-linuxone-accelerator/12-01-SUMMARY.md

# /review skill spec (the file this plan modifies)
@.claude/commands/review.md

# Golden harness layout (mirror the existing patterns exactly)
@tests/golden/ask/test_golden_ask.py
@tests/golden/review/test_golden_review.py
@tests/golden/ask/cases/mcp-acks-config-001.md
@tests/golden/review/cases/review-bad-acks-001.md
@tests/golden/review/fixtures/bad-acks-producer.md

# Articles authored in Plan 12-01 (referenced as required_claims grep targets)
@wiki/patterns/linuxone-on-cfk-reference-architecture.md
@wiki/patterns/x86-to-linuxone-cluster-linking-migration.md
@wiki/concepts/fips-at-install-ocp-requirement.md
@wiki/patterns/auditor-readonly-rbac-payload-isolation.md
@wiki/concepts/s390x-custom-image-build-pipeline.md
@wiki/patterns/flink-on-cfk-fsi-example-jobs.md

# Source for trip-wire-as-claim fixtures
@raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/KNOWN-GAPS.md

<interfaces>
<!-- Existing /ask case frontmatter shape (from mcp-acks-config-001.md) -->
```yaml
 ---
id: <case-id>
query: "<the user's /ask query>"
expected_route: wiki+mcp | wiki | mcp | refuse
floor_model: haiku | sonnet
tags: [list, of, tags]
required_claims:
  - "<grep-target string the answer MUST contain>"
forbidden_claims:
  - "<string the answer MUST NOT contain>"
 ---
```

<!-- Existing /review case frontmatter shape (from review-bad-acks-001.md) -->
```yaml
 ---
id: <case-id>
input_files:
  - tests/golden/review/fixtures/<fixture-name>.md
expected_claims_min: <int>
floor_model: haiku | sonnet
tags: [list, of, tags]
required_report_sections:
  - "## Summary"
  - "## Claim Validation"
  - "## Canon Compliance"
  - "## Recommendations"
forbidden_content:
  - "<string>"
expected_verdict_contains:
  - "Corrected" | "Confirmed" | "Unverifiable"
premise_challenge_expected: true | false
overlay: null | <customer-name>
 ---
```

<!-- Required fields per test_golden_review.py L11-15 -->
REQUIRED_FIELDS = {
    "id", "input_files", "expected_claims_min", "floor_model",
    "tags", "required_report_sections", "forbidden_content"
}

<!-- /review skill spec injection point -->
Step 4 (line 125-130 of .claude/commands/review.md) is where to inject the auditor-readonly claim-flag rule. AFTER Step 3 (wiki cross-reference) but BEFORE Step 5 (canon compliance) — appropriate placement for claim-level grounding.

<!-- Canonical correction text — used as grep target by case 014 -->
"DeveloperRead is consume-granting at the topic-prefix scope it's bound to; auditor isolation requires topic-scoped binding to `confluent-audit-log-events` + SR subjects ONLY, explicitly NOT to `payments.*` business topics (per layer-01 RBAC pattern). See wiki/patterns/auditor-readonly-rbac-payload-isolation.md."

<!-- Three paraphrase patterns to detect -->
1. "DeveloperRead on the cluster is sufficient for auditor isolation"
2. "auditor binding can be at cluster scope"
3. "any read-only role can be used for audit access"
</interfaces>

<reference>
<!-- 10 /ask cases to author (≥10 per WIKI-05 EVAL-02 floor) -->

Article-specific (6):
| # | id | query | required_claim (grep target) | floor_model |
|---|-----|------|------------------------------|-------------|
| 1 | linuxone-ref-arch-deploy-101 | "How do I deploy Confluent Platform on IBM LinuxONE for FSI?" | linuxone-on-cfk-reference-architecture | haiku |
| 2 | linuxone-cluster-linking-migration-102 | "How do I migrate from x86 Confluent to LinuxONE Confluent with regulatory evidence?" | x86-to-linuxone-cluster-linking-migration | haiku |
| 3 | linuxone-fips-at-install-103 | "Why does spec.tls.fips.enabled have no effect on my OCP cluster?" | fips-at-install-ocp-requirement | haiku |
| 4 | linuxone-auditor-readonly-rbac-104 | "How do I configure an auditor RBAC role on Confluent for FSI without giving them access to business topics?" | auditor-readonly-rbac-payload-isolation | haiku |
| 5 | linuxone-s390x-image-build-105 | "How do I build a Confluent Connect image for s390x / LinuxONE?" | s390x-custom-image-build-pipeline | haiku |
| 6 | linuxone-flink-fsi-jobs-106 | "What FSI Flink jobs are included in the LinuxONE accelerator?" | flink-on-cfk-fsi-example-jobs | haiku |

Cross-cutting (4):
| # | id | query | required_claims | floor_model |
|---|-----|------|------------------|-------------|
| 7 | linuxone-fsi-deploy-cross-cutting-107 | "What are the FSI hardening controls in the LinuxONE Confluent accelerator?" | RBAC, mTLS, Schema Registry governance, audit logging, Flink | sonnet |
| 8 | linuxone-accelerator-dispatch-cross-cutting-108 | "Can I run /dsp:plan or /dsp:apply against the LinuxONE accelerator?" | accelerator/confluent-on-linuxone, MODULE_TO_CANON_KEY | sonnet |
| 9 | linuxone-migration-evidence-cross-cutting-109 | "What regulatory evidence should I collect when migrating Kafka workloads to LinuxONE?" | mirror lag, record-count, audit log continuity, RBAC posture, schema parity, 7y retention | sonnet |
| 10 | linuxone-fips-gap-g02-cross-cutting-110 | "Why is FIPS-at-install required for the LinuxONE accelerator's TLS layer?" | G-02, fips-at-install-ocp-requirement | haiku |

<!-- 5 /review cases — fixtures + cases -->

Case 014 (auditor-isolation): fixture is a customer LinuxONE deploy doc claiming "cluster-scoped DeveloperRead is sufficient for our auditor compliance" → review must Correct against canonical text from wiki/patterns/auditor-readonly-rbac-payload-isolation.md.

Cases 015-018 (trip-wire-as-claim): each fixture is a customer doc making a claim that CONTRADICTS a KNOWN-GAPS trip-wire status. Reviews must Correct.

| Case | Trip-wire | Contradicting claim in fixture |
|------|-----------|--------------------------------|
| 015 | G-02 | "We will enable FIPS on the existing OCP cluster after install — should be straightforward" |
| 016 | G-08 | "We can use the upstream confluentinc/confluent-kafka-connect:8.2.0 image directly on s390x without rebuilding" |
| 017 | G-12 | "FlinkApplications will start with the placeholder cp-flink image referenced in the manifest" |
| 018 | G-13 | "Flink checkpoint state on default OCP PVCs is fine — no encryption needed for transient state" |
</reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject auditor-readonly claim-flag rule (Step 4.1) into /review skill spec</name>
  <files>.claude/commands/review.md</files>
  <action>
Add a new sub-section to `.claude/commands/review.md` Step 4 (after the existing MCP validation guidance, BEFORE Step 5). The injection point: find the line `Record the validation result (Confirmed / Corrected / Unverifiable) for each claim checked` (around L130) and append AFTER it (with a blank line separator).

Insert this verbatim:

```markdown

#### Step 4.1: Auditor-readonly payload-isolation claim flag (WIKI-03)

If a claim or paraphrase matches ANY of the following patterns (case-insensitive,
whitespace-normalized), flag it as `Corrected` with the canonical correction text below:

**Trigger patterns:**
1. "DeveloperRead on the cluster is sufficient for auditor isolation" (and paraphrases: "cluster-scoped DeveloperRead suffices", "cluster-wide read-only role for auditors")
2. "auditor binding can be at cluster scope" (and paraphrases: "auditor role at cluster level", "cluster-bound auditor role")
3. "any read-only role can be used for audit access" (and paraphrases: "generic read role for compliance", "DeveloperRead is fine for auditors")

**Canonical correction text (use verbatim):**

> DeveloperRead is consume-granting at the topic-prefix scope it's bound to; auditor isolation requires topic-scoped binding to `confluent-audit-log-events` + SR subjects ONLY, explicitly NOT to `payments.*` business topics (per layer-01 RBAC pattern). See `wiki/patterns/auditor-readonly-rbac-payload-isolation.md`.

This rule grounds the WIKI-03 requirement: `/review` MUST flag any cluster-scoped DeveloperRead claim
for auditor roles as a payload-isolation violation, with the topic-scoped binding workaround as the
correction.
```

Verify the injection is well-formed:
- The heading `#### Step 4.1:` is one level deeper than Step 4 (which uses `###`)
- The blockquote (`>`) is preserved on the canonical correction line for visual emphasis
- Backticks around `confluent-audit-log-events`, `payments.*`, and the wiki article path are preserved

Do NOT renumber Step 5+ — the new section is `4.1`, leaving Step 5 stable.
  </action>
  <verify>
    <automated>grep -c "DeveloperRead is consume-granting" .claude/commands/review.md && grep -c "auditor-readonly-rbac-payload-isolation.md" .claude/commands/review.md && grep -c "Step 4.1" .claude/commands/review.md</automated>
  </verify>
  <done>`.claude/commands/review.md` contains the new Step 4.1 with all 3 trigger patterns, the verbatim canonical correction blockquote, and a reference to the wiki article path. Step 5+ headings unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Author 5 review fixtures + 5 review cases (014-018)</name>
  <files>tests/golden/review/fixtures/linuxone-auditor-isolation-violation.md, tests/golden/review/fixtures/linuxone-fips-skip.md, tests/golden/review/fixtures/linuxone-connect-image-x86.md, tests/golden/review/fixtures/linuxone-sql-runner-skip.md, tests/golden/review/fixtures/linuxone-checkpoint-unencrypted.md, tests/golden/review/cases/review-linuxone-auditor-isolation-014.md, tests/golden/review/cases/review-linuxone-fips-skip-015.md, tests/golden/review/cases/review-linuxone-connect-image-x86-016.md, tests/golden/review/cases/review-linuxone-sql-runner-skip-017.md, tests/golden/review/cases/review-linuxone-checkpoint-unencrypted-018.md</files>
  <action>
**Fixtures (5 files in tests/golden/review/fixtures/):**

Each fixture is a customer-style markdown deployment doc (Confluence-page or runbook tone) containing the contradicting claim. ~30-60 lines each. Style-match the existing `tests/golden/review/fixtures/bad-acks-producer.md` for tone.

1. `linuxone-auditor-isolation-violation.md` — Customer LinuxONE deploy plan. Must contain the claim: "We will grant our compliance team the `DeveloperRead` role at cluster scope — this gives them read-only access to all topics for audit purposes." (and at least one paraphrase). At least 3 extractable claims total: (a) auditor binding scope (the violation), (b) one canon-correct claim like `acks=all` to test that the harness extracts both correct + incorrect, (c) one neutral architectural claim.

2. `linuxone-fips-skip.md` — Customer doc claiming: "Our OCP cluster was installed without FIPS, but we plan to enable it post-install by editing the install-config.yaml. We'll also set `spec.tls.fips.enabled: true` on the CFK CRs." (contradicts G-02). ≥3 extractable claims.

3. `linuxone-connect-image-x86.md` — Customer doc claiming: "We will deploy the upstream `confluentinc/confluent-kafka-connect:8.2.0` image as-is on our LinuxONE workers without rebuilding for s390x." (contradicts G-08). ≥3 claims.

4. `linuxone-sql-runner-skip.md` — Customer doc claiming: "Layer 05 Flink will work out-of-the-box — we don't need to build the custom s390x SQL-runner image since the manifest references a placeholder image that the operator will resolve." (contradicts G-12). ≥3 claims.

5. `linuxone-checkpoint-unencrypted.md` — Customer doc claiming: "Our Flink checkpoint state is transient processing data, so encryption-at-rest is not required for the PVC backing `state.checkpoints.dir`." (contradicts G-13 + PCI-DSS 3.4 / GLBA). ≥3 claims.

Each fixture body MUST be plausible FSI engineer prose, NOT a list of one-liner claims. ~30-60 lines with sections, rationale prose, and the load-bearing claim embedded naturally.

**Cases (5 files in tests/golden/review/cases/):**

Each case is a YAML-frontmatter-only `.md` file with brief case-spec comments at end (match `review-bad-acks-001.md` exactly).

Case 014 (auditor-isolation):
```yaml
 ---
id: review-linuxone-auditor-isolation-014
input_files:
  - tests/golden/review/fixtures/linuxone-auditor-isolation-violation.md
expected_claims_min: 3
floor_model: haiku
tags: [rbac, mds, audit, linuxone, fsi, payload-isolation, wiki-03]
required_report_sections:
  - "## Summary"
  - "## Claim Validation"
  - "## Canon Compliance"
  - "## Recommendations"
forbidden_content:
  - "I don't know"
  - "cannot answer"
expected_verdict_contains:
  - "Corrected"
premise_challenge_expected: false
overlay: null
 ---

## Case: LinuxONE auditor-isolation violation (WIKI-03)

**What the review MUST find:**
- Claim that cluster-scoped DeveloperRead is sufficient for auditor isolation → expect Corrected verdict
- Canon Compliance row noting deviation from layer-01 RBAC payload-isolation pattern
- Recommendations section cites `wiki/patterns/auditor-readonly-rbac-payload-isolation.md`
- Canonical correction text contains "DeveloperRead is consume-granting" verbatim

**What the review MUST NOT contain:**
- Any statement endorsing cluster-scoped DeveloperRead for auditor isolation
- Any claim that the customer's approach is acceptable

**Negative-space trigger:** NO
```

Cases 015-018 follow the same skeleton, replacing tags + body comments + grep targets:

- **Case 015 (FIPS skip — G-02)**: `tags: [fips, ocp, linuxone, fsi, gap-g02, trip-wire, wiki-02]`. Body comments: review MUST cite "post-install FIPS unsupported by Red Hat" / "fips: true required in install-config.yaml" / KNOWN-GAPS G-02.
- **Case 016 (Connect image x86 — G-08)**: `tags: [connect, s390x, linuxone, gap-g08, trip-wire, image-build, wiki-02]`. Body: "docker buildx --platform linux/s390x required" / cite G-08.
- **Case 017 (SQL-runner skip — G-12)**: `tags: [flink, sql-runner, s390x, linuxone, gap-g12, trip-wire, wiki-02]`. Body: "FlinkApplications fail image pull without custom s390x image" / cite G-12.
- **Case 018 (checkpoint unencrypted — G-13)**: `tags: [flink, checkpoint, encryption, linuxone, gap-g13, trip-wire, pci-dss, wiki-02]`. Body: "PCI-DSS 3.4 + GLBA encryption-at-rest required" / "OCP encrypted StorageClass OR S3 SSE-KMS" / cite G-13.

All 5 cases use `floor_model: haiku`, `expected_claims_min: 3`, `expected_verdict_contains: [Corrected]`, `overlay: null`, `premise_challenge_expected: false`.

**Avoid:**
- ID collisions: 014-018 are next-free (latest existing is 016 — DOUBLE-CHECK with `ls tests/golden/review/cases/`; if 014/015/016 already exist, bump these to 017-021)
- Fixtures shorter than ~30 lines (harness needs ≥3 extractable claims)
- Body comments that mention pretend canon paths that don't exist (use `wiki/patterns/auditor-readonly-rbac-payload-isolation.md` — created in 12-01)
  </action>
  <verify>
    <automated>python -c "
import yaml
from pathlib import Path
review_cases = [
    'tests/golden/review/cases/review-linuxone-auditor-isolation-014.md',
    'tests/golden/review/cases/review-linuxone-fips-skip-015.md',
    'tests/golden/review/cases/review-linuxone-connect-image-x86-016.md',
    'tests/golden/review/cases/review-linuxone-sql-runner-skip-017.md',
    'tests/golden/review/cases/review-linuxone-checkpoint-unencrypted-018.md',
]
review_fixtures = [
    'tests/golden/review/fixtures/linuxone-auditor-isolation-violation.md',
    'tests/golden/review/fixtures/linuxone-fips-skip.md',
    'tests/golden/review/fixtures/linuxone-connect-image-x86.md',
    'tests/golden/review/fixtures/linuxone-sql-runner-skip.md',
    'tests/golden/review/fixtures/linuxone-checkpoint-unencrypted.md',
]
REQUIRED = {'id','input_files','expected_claims_min','floor_model','tags','required_report_sections','forbidden_content'}
for p in review_cases:
    assert Path(p).exists(), f'MISSING: {p}'
    c = Path(p).read_text()
    fm = yaml.safe_load(c[3:c.find('---', 3)])
    assert not (REQUIRED - set(fm.keys())), f'{p}: missing {REQUIRED - set(fm.keys())}'
    assert fm['floor_model'] == 'haiku'
    assert 'Corrected' in fm.get('expected_verdict_contains', [])
for f in review_fixtures:
    assert Path(f).exists(), f'MISSING: {f}'
    assert len(Path(f).read_text().splitlines()) >= 30
print('OK: 5 review cases + 5 fixtures valid')
"</automated>
  </verify>
  <done>5 review fixtures + 5 review cases on disk; all cases have REQUIRED frontmatter fields; all fixtures ≥30 lines with the contradicting claim embedded in plausible FSI prose; pytest discovers the new cases.</done>
</task>

<task type="auto">
  <name>Task 3: Author 10 golden /ask cases (101-110) covering 6 articles + 4 cross-cutting</name>
  <files>tests/golden/ask/cases/linuxone-ref-arch-deploy-101.md, tests/golden/ask/cases/linuxone-cluster-linking-migration-102.md, tests/golden/ask/cases/linuxone-fips-at-install-103.md, tests/golden/ask/cases/linuxone-auditor-readonly-rbac-104.md, tests/golden/ask/cases/linuxone-s390x-image-build-105.md, tests/golden/ask/cases/linuxone-flink-fsi-jobs-106.md, tests/golden/ask/cases/linuxone-fsi-deploy-cross-cutting-107.md, tests/golden/ask/cases/linuxone-accelerator-dispatch-cross-cutting-108.md, tests/golden/ask/cases/linuxone-migration-evidence-cross-cutting-109.md, tests/golden/ask/cases/linuxone-fips-gap-g02-cross-cutting-110.md</files>
  <action>
Create all 10 /ask cases per the `<reference>` table. Each follows the existing `mcp-acks-config-001.md` shape exactly.

**Case 101 — `linuxone-ref-arch-deploy-101.md`** (WIKI-01 verbatim — the success-criterion case):
```yaml
 ---
id: linuxone-ref-arch-deploy-101
query: "How do I deploy Confluent Platform on IBM LinuxONE for FSI?"
expected_route: wiki+mcp
floor_model: haiku
tags: [linuxone, ibm, s390x, cfk, ocp, fsi, accelerator, wiki-01, wiki-04]
required_claims:
  - "linuxone-on-cfk-reference-architecture"
  - "5-layer"
  - "Kustomize Component"
forbidden_claims:
  - "I cannot help"
  - "no information"
 ---

## Case: LinuxONE-on-CFK reference architecture deploy (WIKI-01)

**What the answer MUST contain:**
- Citation of wiki/patterns/linuxone-on-cfk-reference-architecture.md (path or article title)
- Mention of the 5-layer Kustomize Component composition (RBAC → mTLS → SR governance → audit → Flink)
- Reference to IBM Mondics base + GoodLabs FSI hardening layers

**What the answer MUST NOT contain:**
- Refusal to answer about LinuxONE deployment
- Suggestion to use a different platform (e.g. AWS) without acknowledging LinuxONE first

**Negative-space trigger:** NO
```

**Cases 102-106** — analogous shape, one per article:

| Case | id | query | required_claims | tags |
|------|----|----|------------------|------|
| 102 | linuxone-cluster-linking-migration-102 | "How do I migrate from x86 Confluent to LinuxONE Confluent with regulatory evidence?" | [x86-to-linuxone-cluster-linking-migration, mirror lag, evidence] | [linuxone, migration, cluster-linking, fsi, compliance, wiki-04] |
| 103 | linuxone-fips-at-install-103 | "Why does spec.tls.fips.enabled have no effect on my OCP cluster?" | [fips-at-install-ocp-requirement, post-install, install-config.yaml] | [fips, ocp, linuxone, gap-g02, wiki-04] |
| 104 | linuxone-auditor-readonly-rbac-104 | "How do I configure an auditor RBAC role on Confluent for FSI without giving them access to business topics?" | [auditor-readonly-rbac-payload-isolation, topic-scoped, confluent-audit-log-events] | [rbac, mds, audit, linuxone, fsi, wiki-03, wiki-04] |
| 105 | linuxone-s390x-image-build-105 | "How do I build a Confluent Connect image for s390x / LinuxONE?" | [s390x-custom-image-build-pipeline, docker buildx, linux/s390x] | [s390x, linuxone, docker-buildx, connect, gap-g08, wiki-04] |
| 106 | linuxone-flink-fsi-jobs-106 | "What FSI Flink jobs are included in the LinuxONE accelerator?" | [flink-on-cfk-fsi-example-jobs, tumbling, temporal] | [flink, linuxone, cmf, fsi, wiki-04] |

All cases 102-106: `floor_model: haiku`, `expected_route: wiki+mcp`, forbidden_claims = ["I cannot help", "no information"].

**Cases 107-110** — cross-cutting (sonnet floor for the 3 broader ones, haiku for the trip-wire-specific one):

| Case | id | query | required_claims | floor_model |
|------|----|----|------------------|-------------|
| 107 | linuxone-fsi-deploy-cross-cutting-107 | "What are the FSI hardening controls in the LinuxONE Confluent accelerator?" | [RBAC, mTLS, Schema Registry governance, audit logging, Flink] | sonnet |
| 108 | linuxone-accelerator-dispatch-cross-cutting-108 | "Can I run /dsp:plan or /dsp:apply against the LinuxONE accelerator?" | [accelerator/confluent-on-linuxone, MODULE_TO_CANON_KEY, layer] | sonnet |
| 109 | linuxone-migration-evidence-cross-cutting-109 | "What regulatory evidence should I collect when migrating Kafka workloads to LinuxONE?" | [mirror lag, record-count, audit log continuity, RBAC posture, schema parity, 7y] | sonnet |
| 110 | linuxone-fips-gap-g02-cross-cutting-110 | "Why is FIPS-at-install required for the LinuxONE accelerator's TLS layer?" | [G-02, fips-at-install-ocp-requirement, install-config.yaml] | haiku |

All 4 cross-cutting: `expected_route: wiki+mcp`, tags include `wiki-05` + relevant topic tags + `cross-cutting`.

**Avoid:**
- ID collisions: 101-110 should not exist (verify with `ls tests/golden/ask/cases/`). Existing IDs are 001-008 + deep-001..007 + negative-001..010 + ephemeral/mode/zero-results variants. 101-110 is the next-free 100-block.
- `forbidden_claims` listing strings that the answer might legitimately need to use to qualify uncertainty — keep forbidden_claims tight (just refusal patterns).
- `required_claims` listing strings that don't appear in the wiki article body (verify each grep target IS in the article from 12-01 before committing).
  </action>
  <verify>
    <automated>python -c "
import yaml
from pathlib import Path
cases = [
    'linuxone-ref-arch-deploy-101', 'linuxone-cluster-linking-migration-102',
    'linuxone-fips-at-install-103', 'linuxone-auditor-readonly-rbac-104',
    'linuxone-s390x-image-build-105', 'linuxone-flink-fsi-jobs-106',
    'linuxone-fsi-deploy-cross-cutting-107', 'linuxone-accelerator-dispatch-cross-cutting-108',
    'linuxone-migration-evidence-cross-cutting-109', 'linuxone-fips-gap-g02-cross-cutting-110',
]
REQUIRED = {'id','query','expected_route','floor_model','tags','required_claims','forbidden_claims'}
for slug in cases:
    p = Path(f'tests/golden/ask/cases/{slug}.md')
    assert p.exists(), f'MISSING: {p}'
    c = p.read_text()
    fm = yaml.safe_load(c[3:c.find('---', 3)])
    missing = REQUIRED - set(fm.keys())
    assert not missing, f'{slug}: missing {missing}'
    assert fm['id'] == slug, f'{slug}: id mismatch'
    assert isinstance(fm['required_claims'], list) and len(fm['required_claims']) >= 1
print('OK: 10 ask cases valid')
"</automated>
  </verify>
  <done>10 /ask cases on disk; each has REQUIRED frontmatter fields; each `required_claims` list non-empty; IDs 101-110 unique and follow naming convention.</done>
</task>

<task type="auto">
  <name>Task 4: Run golden harness to confirm structural discovery</name>
  <files>tests/golden/ask/test_golden_ask.py, tests/golden/review/test_golden_review.py</files>
  <action>
Run both golden harnesses to confirm:
1. All 10 new /ask cases are discovered (`ALL_CASES` glob picks them up)
2. All 5 new /review cases are discovered
3. Structural validations pass on every new case (REQUIRED_FIELDS present, floor_model in VALID_FLOOR_MODELS, fixtures exist for review cases)
4. Minimum-count gates pass: /review needs ≥15 (existing 16 + new 5 = 21); /ask should have ≥20 (existing baseline + new 10)

If any structural tests fail, fix the offending case file before completing this task. Common fixes:
- Missing required field → add to frontmatter
- `input_files` references a fixture that doesn't exist → verify Plan 12-03 Task 2 created it
- `floor_model` value other than `haiku`/`sonnet` → fix to one of those

Do NOT modify the harness code (`test_golden_ask.py`, `test_golden_review.py`) — they are byte-stable from H.2.
  </action>
  <verify>
    <automated>pytest tests/golden/ask/test_golden_ask.py tests/golden/review/test_golden_review.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>Both golden harnesses pass; total /review cases ≥21 (existing 16 + new 5); total /ask cases ≥ existing baseline + 10; no structural failures on new cases.</done>
</task>

</tasks>

<verification>
- `grep -c "DeveloperRead is consume-granting" .claude/commands/review.md` returns ≥1 (rule landed)
- `grep -c "auditor-readonly-rbac-payload-isolation.md" .claude/commands/review.md` returns ≥1 (path-to-article preserved)
- `ls tests/golden/review/cases/ | grep linuxone | wc -l` returns 5
- `ls tests/golden/ask/cases/ | grep linuxone | wc -l` returns 10
- `pytest tests/golden/` exits 0
- WIKI-01 verification: `linuxone-ref-arch-deploy-101.md` query string is the exact WIKI-01 success-criterion query
- WIKI-03 verification: the /review skill has Step 4.1 wired with all 3 paraphrase patterns + canonical correction; case 014 exercises it
- WIKI-05 verification: 10 + 5 = 15 new cases at EVAL-02 floor structurally satisfied (actual model-eval LLM-as-judge runs are deferred to CI per H.2 D-04 structural-only posture)
</verification>

<success_criteria>
1. `.claude/commands/review.md` has new Step 4.1 with 3 trigger patterns + verbatim canonical correction text + reference to `wiki/patterns/auditor-readonly-rbac-payload-isolation.md`
2. 5 review fixtures exist in `tests/golden/review/fixtures/` (each ≥30 lines with contradicting claim)
3. 5 review cases exist in `tests/golden/review/cases/` (014-018) with REQUIRED_FIELDS + `expected_verdict_contains: [Corrected]`
4. 10 ask cases exist in `tests/golden/ask/cases/` (101-110) covering 6 articles + 4 cross-cutting topics
5. WIKI-01 case (`linuxone-ref-arch-deploy-101`) query string is byte-identical to the ROADMAP WIKI-01 success criterion: `"How do I deploy Confluent Platform on IBM LinuxONE for FSI?"`
6. `pytest tests/golden/` passes (structural — no new failures)
7. WIKI-01, WIKI-03, WIKI-05 requirements structurally satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/12-wiki-ingest-of-linuxone-accelerator/12-03-SUMMARY.md` recording:
- Exact line range in `.claude/commands/review.md` where Step 4.1 was inserted
- Total review-case count before/after (16 → 21)
- Total ask-case count before/after
- Any trip-wire IDs that needed paraphrase tuning to land the contradicting claim in fixture prose
- Pointer to Plan 12-04 (carry-forward fix — closes the v2.0 hygiene debt and lands the final atomic commit)
</output>
</content>
</invoke>