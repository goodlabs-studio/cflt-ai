---
phase: 12-wiki-ingest-of-linuxone-accelerator
plan: 04
type: execute
wave: 3
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - wiki/concepts/observability-metrics-mapping.md
  - wiki/concepts/confluent-platform-broker-jmx.md
  - wiki/patterns/cfk-observability-baseline.md
  - wiki/patterns/cluster-linking-observability.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "No wiki article sources frontmatter contains raw/repos/fsi-dsp/... paths"
    - "All converted citations resolve against MANIFEST.yaml IDs (verified by test_all_citations_resolve_against_manifest)"
    - "pytest tests/test_wiki_citations.py::TestNoRawPaths::test_no_raw_fsi_dsp_paths_in_sources passes (was failing since commit bd7f967)"
    - "Phase 09 deferred-items.md test failure closed"
  artifacts:
    - path: "wiki/concepts/observability-metrics-mapping.md"
      provides: "Raw path converted: raw/repos/fsi-dsp/observability/metrics-mapping.md → fsi-dsp://observability/metrics-mapping"
      contains: "fsi-dsp://observability/metrics-mapping"
    - path: "wiki/concepts/confluent-platform-broker-jmx.md"
      provides: "Raw path converted: raw/repos/fsi-dsp/observability/grafana/jmx-exporter-config.yaml → fsi-dsp://observability/grafana"
      contains: "fsi-dsp://observability/grafana"
    - path: "wiki/patterns/cfk-observability-baseline.md"
      provides: "2 raw paths converted: jmx-exporter-config.yaml + README.md → fsi-dsp://observability/grafana (deduped to single citation)"
      contains: "fsi-dsp://observability/grafana"
    - path: "wiki/patterns/cluster-linking-observability.md"
      provides: "2 raw paths converted: dashboard-dr-readiness.json + README.md → fsi-dsp://observability/grafana (deduped to single citation)"
      contains: "fsi-dsp://observability/grafana"
  key_links:
    - from: "wiki/concepts/observability-metrics-mapping.md"
      to: "raw/repos/fsi-dsp/MANIFEST.yaml#observability/metrics-mapping"
      via: "fsi-dsp:// URI"
      pattern: "fsi-dsp://observability/metrics-mapping"
    - from: "wiki/concepts/confluent-platform-broker-jmx.md"
      to: "raw/repos/fsi-dsp/MANIFEST.yaml#observability/grafana"
      via: "fsi-dsp:// URI"
      pattern: "fsi-dsp://observability/grafana"
    - from: "wiki/patterns/cfk-observability-baseline.md"
      to: "raw/repos/fsi-dsp/MANIFEST.yaml#observability/grafana"
      via: "fsi-dsp:// URI"
      pattern: "fsi-dsp://observability/grafana"
    - from: "wiki/patterns/cluster-linking-observability.md"
      to: "raw/repos/fsi-dsp/MANIFEST.yaml#observability/grafana"
      via: "fsi-dsp:// URI"
      pattern: "fsi-dsp://observability/grafana"
---

<objective>
Close the Phase 09 carry-forward debt: convert all `raw/repos/fsi-dsp/...` source references in 4 pre-existing wiki articles to the stable `fsi-dsp://<id>` URI form per H.1 D-07 source-attestation discipline. After this plan, `pytest tests/test_wiki_citations.py::TestNoRawPaths::test_no_raw_fsi_dsp_paths_in_sources` passes (was failing since commit `bd7f967`), and Phase 12 closes both v2.1 wiki ingest AND the v2.0 hygiene debt in a single milestone.

Purpose: This plan has NO new REQ-ID — it's pure bookkeeping cleanup of pre-existing tech debt deferred by Phase 09 (see `.planning/phases/09-submodule-sync-canon-parity-unblock/deferred-items.md`). It's placed in Phase 12 because (a) source-attestation discipline aligns with Phase 12's ingest pattern, and (b) atomic-close discipline (mirroring Phases 09/10) means Phase 12 closes all of Phase 12's scope plus this carry-forward in one milestone.

Dependencies: Runs LAST (wave 3, depends on 12-01..03) so the carry-forward fix lands AFTER all new content. This way the final commit covers everything Phase 12 touched in one atomic landing.

Output: 4 wiki article edits (frontmatter `sources:` field only, no body content changed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-wiki-ingest-of-linuxone-accelerator/12-CONTEXT.md
@.planning/phases/09-submodule-sync-canon-parity-unblock/deferred-items.md

# The 4 articles to fix (read full frontmatter, NOT the body — saves context)
@wiki/concepts/observability-metrics-mapping.md
@wiki/concepts/confluent-platform-broker-jmx.md
@wiki/patterns/cfk-observability-baseline.md
@wiki/patterns/cluster-linking-observability.md

# The test that must go GREEN after this plan
@tests/test_wiki_citations.py

# MANIFEST.yaml entries used for ID lookup (verified at planning time)
@raw/repos/fsi-dsp/MANIFEST.yaml

<interfaces>
<!-- MANIFEST.yaml stable IDs available (verified at planning time via grep on raw/repos/fsi-dsp/MANIFEST.yaml) -->

Only TWO observability IDs exist in MANIFEST.yaml:
- `observability/metrics-mapping` (file-level: path = `observability/metrics-mapping.md`)
- `observability/grafana`          (folder-level: path = `observability/grafana`)

The 3 grafana subpaths (jmx-exporter-config.yaml, README.md, dashboard-dr-readiness.json) do NOT have individual MANIFEST entries. Per CONTEXT.md D-04: "if missing, fall back to most-specific existing reference ID" → all 3 collapse to `fsi-dsp://observability/grafana`.

<!-- Conversion map (all 6 violations from Phase 09 deferred-items.md) -->

| File | Raw path (violation) | Target ID |
|------|----------------------|-----------|
| wiki/concepts/observability-metrics-mapping.md | `raw/repos/fsi-dsp/observability/metrics-mapping.md` | `fsi-dsp://observability/metrics-mapping` |
| wiki/concepts/confluent-platform-broker-jmx.md | `raw/repos/fsi-dsp/observability/grafana/jmx-exporter-config.yaml` | `fsi-dsp://observability/grafana` |
| wiki/patterns/cfk-observability-baseline.md | `raw/repos/fsi-dsp/observability/grafana/jmx-exporter-config.yaml` | `fsi-dsp://observability/grafana` |
| wiki/patterns/cfk-observability-baseline.md | `raw/repos/fsi-dsp/observability/grafana/README.md` | `fsi-dsp://observability/grafana` (DEDUP — collapses to same target) |
| wiki/patterns/cluster-linking-observability.md | `raw/repos/fsi-dsp/observability/grafana/dashboard-dr-readiness.json` | `fsi-dsp://observability/grafana` |
| wiki/patterns/cluster-linking-observability.md | `raw/repos/fsi-dsp/observability/grafana/README.md` | `fsi-dsp://observability/grafana` (DEDUP) |

After conversion + dedup, the article sources lists shrink by 1 entry each in `cfk-observability-baseline.md` and `cluster-linking-observability.md` (two raw paths collapse to one fsi-dsp URI).

<!-- Citation resolution test (must PASS after this plan) -->
`test_all_citations_resolve_against_manifest` (tests/test_wiki_citations.py L84) reads every `fsi-dsp://` source string, strips the prefix, and checks against MANIFEST.yaml capability IDs. Both target IDs (`observability/metrics-mapping`, `observability/grafana`) are verified present in MANIFEST.yaml (raw/repos/fsi-dsp/MANIFEST.yaml L346, L370).

<!-- Note: deferred-items.md anticipated IDs like fsi-dsp://reference/observability-metrics-mapping (made-up). The ACTUAL MANIFEST IDs are fsi-dsp://observability/metrics-mapping and fsi-dsp://observability/grafana — use these. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert raw paths to fsi-dsp:// URIs in 4 wiki articles (frontmatter sources: field only)</name>
  <files>wiki/concepts/observability-metrics-mapping.md, wiki/concepts/confluent-platform-broker-jmx.md, wiki/patterns/cfk-observability-baseline.md, wiki/patterns/cluster-linking-observability.md</files>
  <action>
For each of the 4 articles, edit ONLY the `sources:` list in the YAML frontmatter (the `---` block at the top). Do NOT modify article body content.

**File 1 — `wiki/concepts/observability-metrics-mapping.md`:**

In the `sources:` list, replace:
```yaml
  - raw/repos/fsi-dsp/observability/metrics-mapping.md
```
with:
```yaml
  - fsi-dsp://observability/metrics-mapping
```

**File 2 — `wiki/concepts/confluent-platform-broker-jmx.md`:**

In the `sources:` list, replace:
```yaml
  - raw/repos/fsi-dsp/observability/grafana/jmx-exporter-config.yaml
```
with:
```yaml
  - fsi-dsp://observability/grafana
```

**File 3 — `wiki/patterns/cfk-observability-baseline.md`:**

In the `sources:` list, REPLACE both:
```yaml
  - raw/repos/fsi-dsp/observability/grafana/jmx-exporter-config.yaml
  - raw/repos/fsi-dsp/observability/grafana/README.md
```
WITH a SINGLE entry:
```yaml
  - fsi-dsp://observability/grafana
```
(Both raw paths collapse to the same MANIFEST ID — dedup is correct here. Net effect: sources list shrinks by 1 entry.)

**File 4 — `wiki/patterns/cluster-linking-observability.md`:**

In the `sources:` list, REPLACE both:
```yaml
  - raw/repos/fsi-dsp/observability/grafana/dashboard-dr-readiness.json
  - raw/repos/fsi-dsp/observability/grafana/README.md
```
WITH a SINGLE entry:
```yaml
  - fsi-dsp://observability/grafana
```

**Implementation discipline:**

- Use the `Read` tool on each file FIRST to see exact current frontmatter (necessary before `Edit`).
- Use the `Edit` tool with `old_string` matching the EXACT raw-path line (including leading 2 spaces + `- `) and `new_string` being the fsi-dsp URI line.
- After all 4 files: verify with `grep -r "raw/repos/fsi-dsp" wiki/` returns ZERO results (other than the article BODY content if any — but the test only checks frontmatter `sources:` list).
- Do NOT touch the `related:` field, `tags:` field, `last_validated:`, or any body content. Only `sources:` list entries change.
- Do NOT change `confidence:` field even if it was previously `medium` — that's an orthogonal decision deferred to a future `/wiki:validate` pass.

**Anti-patterns to avoid:**

- Do NOT add the upstream URL or HTTPS sources to compensate — the conversion is 1:1 (raw path → fsi-dsp URI), not a sources-list rewrite.
- Do NOT use the made-up IDs from `deferred-items.md` (`fsi-dsp://reference/observability-*`) — they don't exist in MANIFEST.yaml. The ACTUAL IDs are `fsi-dsp://observability/metrics-mapping` and `fsi-dsp://observability/grafana`.
- Do NOT use layer-scoped IDs like `fsi-dsp://observability/grafana:jmx-exporter-config` — MANIFEST has only the folder-level ID; the colon-suffix shape applies only to types with explicit per-layer/per-section subentries (e.g. accelerator apply_sequence).
  </action>
  <verify>
    <automated>! grep -r "raw/repos/fsi-dsp" wiki/ | grep -E "^\s*-\s+raw/repos/fsi-dsp" && pytest tests/test_wiki_citations.py::TestNoRawPaths::test_no_raw_fsi_dsp_paths_in_sources -v && pytest tests/test_wiki_citations.py::TestCitationResolution::test_all_citations_resolve_against_manifest -v</automated>
  </verify>
  <done>All 4 articles have `fsi-dsp://` URIs in their `sources:` list; zero `raw/repos/fsi-dsp` paths remain in any wiki frontmatter; `test_no_raw_fsi_dsp_paths_in_sources` PASSES (was failing since `bd7f967`); `test_all_citations_resolve_against_manifest` continues to PASS (the 2 new fsi-dsp IDs both resolve against MANIFEST.yaml).</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression check + Phase 12 closing verification</name>
  <files></files>
  <action>
Run the full pytest suite to confirm no regressions across Phase 12's scope:

```bash
pytest tests/ -v 2>&1 | tail -40
```

Confirm:
1. `test_no_raw_fsi_dsp_paths_in_sources` is now GREEN (Phase 09 deferred-items.md issue closed)
2. `test_all_citations_resolve_against_manifest` is GREEN (no regression from the 4 conversions)
3. Plan 12-01 outputs intact: `pytest tests/test_wiki_citations.py::TestLinuxOneArticle` should still pass (independent of this plan — the linuxone-kafka-integration article from `concepts/` is pre-existing, unrelated to Phase 12's new accelerator articles)
4. Plan 12-02 outputs intact: `pytest tests/test_wiki_lint_gap_drift.py` continues to pass
5. Plan 12-03 outputs intact: `pytest tests/golden/` continues to pass
6. `python tools/wiki-lint.py --full` exits 0; no new findings introduced by the carry-forward fix (the 4 converted articles should now show NO drift since they're not vendor-pinned — they use fsi-dsp:// URIs which `check_vendor_drift` correctly skips)

If ANY test that was previously green is now red, diagnose root cause before completing the task. Specifically watch for:
- Tests asserting a SPECIFIC number of sources per article (would break if dedup shrunk the list from 5 → 4 sources)
- Tests that grep for the raw paths INSIDE wiki body content (unrelated to this plan)

If a test was broken by Plan 12-01..03's changes (not 12-04's), that's a Plan 12-01..03 regression that needs to be fixed in those plans — NOT in 12-04. Surface the issue with a clear root-cause statement.
  </action>
  <verify>
    <automated>pytest tests/ -v 2>&1 | tail -10 && echo "---" && python tools/wiki-lint.py --full; echo "wiki-lint exit=$?"</automated>
  </verify>
  <done>Full test suite passes; `test_no_raw_fsi_dsp_paths_in_sources` now GREEN; no regression in any previously-green test; wiki-lint exits 0; Phase 12 is end-to-end green.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_wiki_citations.py::TestNoRawPaths::test_no_raw_fsi_dsp_paths_in_sources` passes (was failing since `bd7f967`)
- `pytest tests/test_wiki_citations.py::TestCitationResolution::test_all_citations_resolve_against_manifest` passes (no new unresolvable citations)
- `grep -r "raw/repos/fsi-dsp" wiki/ | grep -E "^\s*-\s+raw"` returns ZERO matches (no raw paths in frontmatter sources lists)
- `python tools/wiki-lint.py --full` exits 0
- Phase 09 deferred-items.md's pre-existing failure is RESOLVED
</verification>

<success_criteria>
1. All 4 articles have ZERO `raw/repos/fsi-dsp/...` paths in their `sources:` frontmatter list
2. All converted citations use either `fsi-dsp://observability/metrics-mapping` or `fsi-dsp://observability/grafana` (the 2 actual MANIFEST IDs)
3. `test_no_raw_fsi_dsp_paths_in_sources` PASSES (Phase 09 deferred debt closed)
4. `test_all_citations_resolve_against_manifest` PASSES (no broken citations introduced)
5. No regressions in any previously-green test
6. `tools/wiki-lint.py --full` exits 0
7. Phase 12 closes BOTH v2.1 wiki ingest (WIKI-01..05 via Plans 12-01..03) AND the v2.0 carry-forward hygiene debt (via this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/12-wiki-ingest-of-linuxone-accelerator/12-04-SUMMARY.md` recording:
- The exact 6 raw-path-to-fsi-dsp conversions performed (file:line:before → after)
- Confirmation that 2 dedups occurred in cfk-observability-baseline.md and cluster-linking-observability.md (sources lists each shrank by 1 entry)
- Phase 09 deferred-items.md status: RESOLVED in commit <SHA>
- Phase 12 closing status: All 5 WIKI-* requirements structurally satisfied + carry-forward debt closed
- Atomic-commit recommendation: Consider squashing Plans 12-01 + 12-02 + 12-03 + 12-04 commits into a single Phase 12 landing commit, mirroring Phase 09's atomic discipline (this is the executor's call at /gsd:execute-phase close time, not the planner's mandate)
</output>
</content>
</invoke>