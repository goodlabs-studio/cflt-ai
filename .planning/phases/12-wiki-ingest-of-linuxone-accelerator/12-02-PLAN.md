---
phase: 12-wiki-ingest-of-linuxone-accelerator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/vendor-sources.json
  - tools/wiki-lint.py
  - tests/test_wiki_lint_gap_drift.py
autonomous: true
requirements: [WIKI-02]
must_haves:
  truths:
    - "All 13 KNOWN-GAPS entries (G-01..G-13) exist in tools/vendor-sources.json as trip-wires"
    - "Each trip-wire has id, title, status, workaround, fsi_impact, source, source_id fields"
    - "tools/wiki-lint.py --full surfaces drift findings when upstream KNOWN-GAPS.md status changes"
    - "Drift findings are non-fatal (exit 0 per H.1 D-09 passive posture)"
    - "test_wiki_lint_gap_drift.py validates JSON shape + drift detection + non-fatal exit"
  artifacts:
    - path: "tools/vendor-sources.json"
      provides: "13 KNOWN-GAPS trip-wires under linuxone-accelerator-gaps key alongside existing confluent-agent-skills + streaming-skills-plugin entries"
      contains: "G-01"
    - path: "tools/wiki-lint.py"
      provides: "check_gap_drift() function reading trip-wires JSON, comparing status against upstream KNOWN-GAPS.md, emitting DRIFT/MALFORMED/MISSING-GAP findings"
      contains: "check_gap_drift"
    - path: "tests/test_wiki_lint_gap_drift.py"
      provides: "Trip-wire JSON shape validation + drift detection unit tests"
      contains: "def test_"
  key_links:
    - from: "tools/wiki-lint.py"
      to: "tools/vendor-sources.json"
      via: "json.loads on linuxone-accelerator-gaps key"
      pattern: "linuxone-accelerator-gaps|trip_wires"
    - from: "tools/wiki-lint.py"
      to: "raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/KNOWN-GAPS.md"
      via: "regex match on G-NN status table row"
      pattern: "KNOWN-GAPS.md"
    - from: "tests/test_wiki_lint_gap_drift.py"
      to: "tools/wiki-lint.py:check_gap_drift"
      via: "import + invoke with tmp_path fixtures"
      pattern: "from tools.wiki_lint|check_gap_drift|wiki-lint.py"
---

<objective>
Encode all 13 entries from `accelerators/confluent-on-linuxone/KNOWN-GAPS.md` (G-01..G-13) as trip-wires in `tools/vendor-sources.json`. Extend `tools/wiki-lint.py --full` with a `check_gap_drift()` function that surfaces DRIFT/MALFORMED/MISSING-GAP findings (non-fatal per H.1 D-09 passive posture). Ship tests validating the JSON shape and drift detection.

Purpose: This satisfies **WIKI-02** (13 KNOWN-GAPS trip-wires + lint drift surface) and mirrors the H.1 D-09 vendor-source-drift mechanism (already in place for `confluent-agent-skills@<sha>`). Trip-wires become living documentation: when upstream KNOWN-GAPS status changes (e.g. G-04 moves from "Needs verification" → "Resolved"), `tools/wiki-lint.py --full` surfaces the drift for routine `/wiki:validate` review.

Output: 1 JSON file extension + 1 Python module extension + 1 new pytest module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-wiki-ingest-of-linuxone-accelerator/12-CONTEXT.md

# H.1 vendor-source-drift mechanism (the pattern this plan extends)
@tools/vendor-sources.json
@tools/wiki-lint.py
@tests/test_wiki_lint_drift.py

# Upstream source (13 KNOWN-GAPS entries to encode)
@raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/KNOWN-GAPS.md

<interfaces>
<!-- Existing tools/vendor-sources.json shape (the file already exists with confluent-agent-skills + streaming-skills-plugin keys) -->
```json
{
  "confluent-agent-skills": { "upstream": "...", "commit": "...", "kind": "wiki-source", ... },
  "streaming-skills-plugin": { "upstream": "...", "commit": "...", "kind": "claude-plugin", ... }
}
```

<!-- This plan ADDS a new top-level key (NOT a wholesale rewrite) -->
```json
{
  "confluent-agent-skills": { ... },        // existing — DO NOT TOUCH
  "streaming-skills-plugin": { ... },        // existing — DO NOT TOUCH
  "linuxone-accelerator-gaps": {             // NEW — this plan
    "upstream": "https://github.com/goodlabs-studio/fsi-dsp",
    "vendored_path": "raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/KNOWN-GAPS.md",
    "ingested_at": "2026-05-23",
    "kind": "gap-register",
    "trip_wires": [
      {
        "id": "G-01",
        "title": "Fetch-by-SHA requires network access at activation",
        "status": "Open",
        "workaround": "Pre-fetch + artifact-cache OR mirror upstream OR OCP egress NetworkPolicy",
        "fsi_impact": "Air-gapped environments not directly supported — operator must mitigate",
        "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md",
        "source_id": "fsi-dsp://accelerator/confluent-on-linuxone"
      },
      // ... G-02 through G-13
    ]
  }
}
```

<!-- Existing wiki-lint.py extension points (from H.1 D-09) -->
- `load_vendor_pins(repo_root)` — already loads tools/vendor-sources.json (line 74)
- `check_vendor_drift(article_path, fm, vendor_pins)` — existing per-article drift check (line 97)
- `lint_wiki(root, full, fix)` — main loop (line 130); findings dict keys (line 132-143)
- `main()` — argparse + labels dict (line 217-252)

<!-- New extension this plan adds: check_gap_drift() -->
- Reads `vendor_pins["linuxone-accelerator-gaps"]["trip_wires"]` (skip silently if key absent — back-compat)
- Reads `raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/KNOWN-GAPS.md` via the repo_root passed into lint_wiki
- For each trip-wire, regex-match the corresponding `| G-NN | ... | <STATUS> |` table row in KNOWN-GAPS.md
- Emit findings:
  - `DRIFT-GAP`: declared status differs from upstream KNOWN-GAPS.md
  - `MISSING-GAP`: trip-wire ID has no corresponding row in upstream (gap was removed upstream)
  - `MALFORMED-GAP`: trip-wire JSON missing one of the 7 required fields
- All findings non-fatal (exit 0) per H.1 D-09 passive posture
</interfaces>

<reference>
<!-- 13 trip-wires to encode — sourced VERBATIM from KNOWN-GAPS.md status table L9-25 -->

| ID | Title (Gap column verbatim) | Status (Status column verbatim) |
|----|----|----|
| G-01 | Fetch-by-SHA requires network access at activation | Open |
| G-02 | FIPS only effective on FIPS-at-install OCP cluster | Open |
| G-03 | No native Dynatrace Kafka Connector | Workaround in place |
| G-04 | Control Center next-gen s390x image availability unverified | Needs verification |
| G-05 | SR bootstrap Job base image must be s390x | Workaround in place |
| G-06 | x86 node-agent sidecars (Splunk/DT) have QEMU overhead | Mitigated by design |
| G-07 | Flox manifest.lock stub must be regenerated | Open |
| G-08 | Custom s390x Connect image required | Open |
| G-09 | Cluster Linking schema sync (CP 8.2+) needs verification | Needs verification |
| G-10 | FKO + CMF are parallel Helm operators outside the kustomize build | Mitigated by design |
| G-11 | CFK 3.2.0+ on s390x officially supports only Flink Applications | Informational |
| G-12 | Custom s390x SQL-runner image required before FlinkApplications start | Open |
| G-13 | Flink checkpoint state encryption — cluster-dependent | Open |

Workaround text MUST come from the "Workaround" column of the same table (KNOWN-GAPS.md L12-25). For brevity in JSON, single-line summary is acceptable (e.g. G-01 collapses three workaround options into one comma-separated string).

`fsi_impact` SHOULD come from the "Impact" column verbatim where it fits in a single line, OR be a 1-sentence FSI-flavored synthesis (e.g. G-02: "Silent FSI compliance failure if FIPS not enabled at OCP install" — synthesized from L52-61 detail block).

All 13 trip-wires use the SAME `source_id`: `fsi-dsp://accelerator/confluent-on-linuxone` (the top-level accelerator ID; layer-specific IDs are not needed for gap tracking).
</reference>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author tests for trip-wire JSON shape + check_gap_drift (RED)</name>
  <files>tests/test_wiki_lint_gap_drift.py</files>
  <behavior>
- Test 1: `tools/vendor-sources.json` parses as JSON and contains `linuxone-accelerator-gaps` top-level key
- Test 2: `linuxone-accelerator-gaps.trip_wires` is a list of length 13
- Test 3: Each trip-wire has all 7 required fields: `id`, `title`, `status`, `workaround`, `fsi_impact`, `source`, `source_id`
- Test 4: Trip-wire IDs are exactly the set `{G-01, G-02, ..., G-13}` (no gaps, no extras)
- Test 5: All `source_id` values equal `fsi-dsp://accelerator/confluent-on-linuxone`
- Test 6 (`check_gap_drift` happy path): Synthetic trip-wires + synthetic KNOWN-GAPS.md table with matching status → no findings
- Test 7 (`check_gap_drift` drift): Trip-wire status="Open", upstream table status="Resolved" → emits `DRIFT-GAP: G-NN: declared=Open, upstream=Resolved`
- Test 8 (`check_gap_drift` missing upstream): Trip-wire ID has no matching row in upstream KNOWN-GAPS.md → emits `MISSING-GAP: G-NN`
- Test 9 (`check_gap_drift` malformed JSON): Trip-wire missing `status` field → emits `MALFORMED-GAP: G-NN: missing field 'status'`
- Test 10 (non-fatal exit): Running `python tools/wiki-lint.py --full` with synthetic drift returns exit code 0 (passive posture per H.1 D-09)
- Test 11 (back-compat): If `linuxone-accelerator-gaps` key is ABSENT from vendor-sources.json, `check_gap_drift` returns empty list silently (no exception, no warning)
- Test 12 (case-insensitive status compare): Trip-wire status "open" matches upstream "Open" (or vice versa) — case-insensitive, whitespace-stripped
  </behavior>
  <action>
Create `tests/test_wiki_lint_gap_drift.py` following the existing `tests/test_wiki_lint_drift.py` test style: module-level imports, fixture-based tmp_path repos with synthetic vendor-sources.json + KNOWN-GAPS.md, direct invocation of `check_gap_drift` function from `tools/wiki-lint.py`.

Existing test patterns to follow (verbatim from `tests/test_wiki_lint_drift.py`):
- Import wiki-lint.py via importlib (hyphenated module name): `spec = importlib.util.spec_from_file_location("wiki_lint", repo_root / "tools" / "wiki-lint.py")`
- Use `tmp_path` fixture for synthetic repos
- Helper function `write_vendor_pins(root, data)` for setting up vendor-sources.json
- Helper function `write_known_gaps(root, content)` for setting up the upstream KNOWN-GAPS.md mock

Test fixtures should include:
1. Synthetic vendor-sources.json with 13 well-formed trip-wires
2. Synthetic KNOWN-GAPS.md with the 13 status table rows
3. Variants: empty trip_wires list, missing key, malformed trip-wire, drifted status, missing upstream row

**Run tests at this point — they MUST fail** (RED phase). The trip-wires aren't in vendor-sources.json yet and `check_gap_drift` doesn't exist in wiki-lint.py. Commit message: `test(12-02): add failing tests for KNOWN-GAPS trip-wires + check_gap_drift`.
  </action>
  <verify>
    <automated>pytest tests/test_wiki_lint_gap_drift.py -v 2>&1 | tail -20 && echo '--- expected: failures (RED phase) ---'</automated>
  </verify>
  <done>Test file exists with ≥12 test cases per the behavior list; tests FAIL (RED) — confirming the trip-wires and `check_gap_drift` are not yet present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode 13 trip-wires in vendor-sources.json + add check_gap_drift to wiki-lint.py (GREEN)</name>
  <files>tools/vendor-sources.json, tools/wiki-lint.py</files>
  <behavior>
- After this task, all tests from Task 1 pass (GREEN)
- `tools/vendor-sources.json` retains existing `confluent-agent-skills` and `streaming-skills-plugin` keys byte-identical (verified via diff)
- `tools/vendor-sources.json` gains `linuxone-accelerator-gaps` top-level key with 13 trip-wires
- `tools/wiki-lint.py` gains `check_gap_drift(repo_root, vendor_pins)` function returning a list of finding strings
- `lint_wiki()` invokes `check_gap_drift` when `full=True` and populates 3 new findings keys: `gap_drift`, `missing_gap`, `malformed_gap`
- `main()` labels dict includes labels for the 3 new finding categories
- Exit code remains 0 when only gap-drift findings are present (passive posture)
  </behavior>
  <action>
**Step 1 — Extend `tools/vendor-sources.json`:**

Read the existing file. Add `linuxone-accelerator-gaps` as a third top-level key (sibling to the existing two). Use the data from `<reference>` block above:

```json
"linuxone-accelerator-gaps": {
  "upstream": "https://github.com/goodlabs-studio/fsi-dsp",
  "vendored_path": "raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/KNOWN-GAPS.md",
  "ingested_at": "2026-05-23",
  "kind": "gap-register",
  "trip_wires": [
    { "id": "G-01", "title": "Fetch-by-SHA requires network access at activation", "status": "Open", "workaround": "Pre-fetch + artifact-cache OR mirror upstream OR OCP egress NetworkPolicy allowing github.com", "fsi_impact": "Air-gapped FSI environments not directly supported — operator must mitigate", "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md", "source_id": "fsi-dsp://accelerator/confluent-on-linuxone" },
    { "id": "G-02", "title": "FIPS only effective on FIPS-at-install OCP cluster", "status": "Open", "workaround": "OCP must be installed with fips: true in install-config.yaml; post-install FIPS not supported by Red Hat", "fsi_impact": "Silent FSI compliance failure if spec.tls.fips.enabled set on non-FIPS OCP — verify before applying layer 02", "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md", "source_id": "fsi-dsp://accelerator/confluent-on-linuxone" },
    { "id": "G-03", "title": "No native Dynatrace Kafka Connector", "status": "Workaround in place", "workaround": "HTTP Sink connector (confluentinc/kafka-connect-http) POSTing to Dynatrace generic log ingest API v2; see layers/04-audit/connect-cr.yaml", "fsi_impact": "DT log processing pipelines must parse Confluent audit event schema manually (no first-party connector)", "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md", "source_id": "fsi-dsp://accelerator/confluent-on-linuxone" },
    { "id": "G-04", "title": "Control Center next-gen s390x image availability unverified", "status": "Needs verification", "workaround": "Use Control Center classic (included in CP 8.2.0 s390x images) until next-gen image is verified via docker manifest inspect", "fsi_impact": "FSI operators may need to fall back to C3 classic UI until verification completes", "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md", "source_id": "fsi-dsp://accelerator/confluent-on-linuxone" },
    { "id": "G-05", "title": "SR bootstrap Job base image must be s390x", "status": "Workaround in place", "workaround": "Use registry.access.redhat.com/ubi9/ubi-minimal:latest — UBI9 has s390x multi-arch manifests; verify with docker manifest inspect", "fsi_impact": "Air-gapped clusters need a verified s390x base image mirror", "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md", "source_id": "fsi-dsp://accelerator/confluent-on-linuxone" },
    { "id": "G-06", "title": "x86 node-agent sidecars (Splunk/DT) have QEMU overhead", "status": "Mitigated by design", "workaround": "Route audit events via Kafka Connect (Connect-based SIEM shipping) instead of node-agent sidecars; layers/04-audit/connect-cr.yaml implements this", "fsi_impact": "2-5x CPU penalty avoided by design — do not add x86 sidecars to CP pods", "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md", "source_id": "fsi-dsp://accelerator/confluent-on-linuxone" },
    { "id": "G-07", "title": "Flox manifest.lock stub must be regenerated", "status": "Open", "workaround": "On a machine with Flox installed, run flox activate in accelerators/confluent-on-linuxone/ to generate a real lock file; commit the result", "fsi_impact": "First-run developer experience requires manual lock-file generation; not blocking but breaks zero-friction onboarding", "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md", "source_id": "fsi-dsp://accelerator/confluent-on-linuxone" },
    { "id": "G-08", "title": "Custom s390x Connect image required", "status": "Open", "workaround": "docker buildx build --platform linux/s390x with Splunk Sink + HTTP Sink JARs pre-installed; base = confluentinc/confluent-kafka-connect:8.2.0", "fsi_impact": "FSI operator must own a private registry for s390x Connect image; cannot use upstream confluentinc registry directly", "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md", "source_id": "fsi-dsp://accelerator/confluent-on-linuxone" },
    { "id": "G-09", "title": "Cluster Linking schema sync (CP 8.2+) needs verification", "status": "Needs verification", "workaround": "Test schema sync in a staging environment; fall back to manual SR migration tool if needed; see MIGRATION.md §2.4", "fsi_impact": "Schema migration during x86→LinuxONE cutover may require manual SR migration tool until sync feature validated", "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md", "source_id": "fsi-dsp://accelerator/confluent-on-linuxone" },
    { "id": "G-10", "title": "FKO + CMF are parallel Helm operators outside the kustomize build", "status": "Mitigated by design", "workaround": "Install via RUNBOOK Step 1b: ansible-playbook ansible/playbooks/flink_operators.yml (readiness-gated); --check mode for audit-only", "fsi_impact": "kustomize build | oc apply is no longer self-sufficient — operators must be installed first or FlinkApplication CRs are rejected", "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md", "source_id": "fsi-dsp://accelerator/confluent-on-linuxone" },
    { "id": "G-11", "title": "CFK 3.2.0+ on s390x officially supports only Flink Applications", "status": "Informational", "workaround": "Layer 05 (Flink) is the officially supported s390x CFK use case; layers 01-04 are run-at-your-own-risk per Confluent's statement", "fsi_impact": "FSI operators must understand support boundary — layers 01-04 (Kafka, SR, Connect, C3) are not officially supported by Confluent on s390x in CFK 3.2.x", "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md", "source_id": "fsi-dsp://accelerator/confluent-on-linuxone" },
    { "id": "G-12", "title": "Custom s390x SQL-runner image required before FlinkApplications start", "status": "Open", "workaround": "docker buildx build --platform linux/s390x per layers/05-flink/sql-runner/README.md; replace all <PLACEHOLDER_CP_FLINK_SQL_RUNNER_IMAGE> references", "fsi_impact": "Pods fail image pull until SQL-runner image is built and pushed to FSI's private registry; layer 05 dead-letter without it", "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md", "source_id": "fsi-dsp://accelerator/confluent-on-linuxone" },
    { "id": "G-13", "title": "Flink checkpoint state encryption — cluster-dependent", "status": "Open", "workaround": "Use an OCP StorageClass with encryption: true OR S3-compatible endpoint with SSE-KMS; replace placeholder in layers/05-flink/flink-environment.yaml", "fsi_impact": "Regulatory finding under PCI-DSS 3.4 + GLBA if checkpoints unencrypted — sensitive in-flight data (transaction amounts, account numbers) at risk", "source": "accelerators/confluent-on-linuxone/KNOWN-GAPS.md", "source_id": "fsi-dsp://accelerator/confluent-on-linuxone" }
  ]
}
```

CRITICAL: do NOT modify the existing two keys (`confluent-agent-skills`, `streaming-skills-plugin`). Verify via `git diff tools/vendor-sources.json` that only additions appear, no modifications to existing entries.

**Step 2 — Extend `tools/wiki-lint.py`:**

Add a `check_gap_drift(repo_root, vendor_pins)` function (placement: after `check_vendor_drift`, before `lint_wiki`). The function:

```python
def check_gap_drift(repo_root: Path, vendor_pins) -> dict:
    """Surface drift between declared trip-wires and upstream KNOWN-GAPS.md.

    Per H.1 D-09 passive posture: findings are surfaced but lint exits 0.
    Returns dict with three keys: gap_drift, missing_gap, malformed_gap (each a list of strings).
    Silently returns empty dict if 'linuxone-accelerator-gaps' key absent (back-compat).
    """
    findings = {"gap_drift": [], "missing_gap": [], "malformed_gap": []}
    if vendor_pins is None:
        return findings
    gaps_entry = vendor_pins.get("linuxone-accelerator-gaps")
    if not gaps_entry:
        return findings  # back-compat: silent skip when key absent

    known_gaps_path = repo_root / "raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/KNOWN-GAPS.md"
    if not known_gaps_path.exists():
        return findings  # submodule not checked out — passive skip
    upstream_content = known_gaps_path.read_text()

    required_fields = {"id", "title", "status", "workaround", "fsi_impact", "source", "source_id"}
    for tw in gaps_entry.get("trip_wires", []):
        missing = required_fields - set(tw.keys())
        if missing:
            findings["malformed_gap"].append(
                f"MALFORMED-GAP {tw.get('id', '<no-id>')}: missing field(s) {sorted(missing)}"
            )
            continue

        gap_id = tw["id"]
        declared_status = str(tw["status"]).strip().lower()

        # Match the table row: | G-NN | <title> | <impact> | <workaround> | <STATUS> |
        # Simplest pattern: find a line starting with "| G-NN |" and extract last pipe-delimited field
        row_re = re.compile(rf"^\|\s*{re.escape(gap_id)}\s*\|.*\|\s*([^|]+?)\s*\|\s*$", re.MULTILINE)
        match = row_re.search(upstream_content)
        if not match:
            findings["missing_gap"].append(
                f"MISSING-GAP {gap_id}: no matching row in upstream KNOWN-GAPS.md"
            )
            continue

        upstream_status = match.group(1).strip().lower()
        if declared_status != upstream_status:
            findings["gap_drift"].append(
                f"DRIFT-GAP {gap_id}: declared={tw['status']!r}, upstream={match.group(1).strip()!r}"
            )
    return findings
```

**Step 3 — Wire into `lint_wiki()`:**

In `lint_wiki()` (around L130), extend the `findings` dict initializer with three new keys:
```python
findings = {
    "stubs": [], "broken_links": [], "orphans": [], "stale": [],
    "low_confidence": [], "unverified": [], "decayed": [],
    "drift": [], "malformed_source": [], "unknown_vendor": [],
    "gap_drift": [], "missing_gap": [], "malformed_gap": [],  # NEW from 12-02
}
```

After the per-article loop (just before the `if full:` orphan block), add:
```python
if full:
    gap_findings = check_gap_drift(root, vendor_pins)
    findings["gap_drift"].extend(gap_findings["gap_drift"])
    findings["missing_gap"].extend(gap_findings["missing_gap"])
    findings["malformed_gap"].extend(gap_findings["malformed_gap"])
```

**Step 4 — Extend `main()` labels:**

Add to the `labels` dict (L235):
```python
"gap_drift": "KNOWN-GAPS DRIFT (declared trip-wire status doesn't match upstream KNOWN-GAPS.md)",
"missing_gap": "MISSING-GAP (trip-wire ID has no matching row in upstream KNOWN-GAPS.md — gap removed?)",
"malformed_gap": "MALFORMED-GAP (trip-wire JSON missing required field)",
```

Exit code MUST remain 0 even when gap-drift findings are present — passive posture per H.1 D-09 (the existing code already exits 0 when total > 0; do NOT change that logic).

**Step 5 — Run tests (must PASS now):**

```bash
pytest tests/test_wiki_lint_gap_drift.py -v
```

All 12 tests from Task 1 should now pass. Commit: `feat(12-02): encode 13 LinuxONE KNOWN-GAPS trip-wires + check_gap_drift in wiki-lint`.

**Anti-patterns to avoid:**
- Do NOT make gap-drift findings fatal (exit ≠ 0) — passive posture per H.1 D-09
- Do NOT modify existing entries in vendor-sources.json
- Do NOT use `yaml.safe_load` on KNOWN-GAPS.md — it's Markdown, parse the table with regex
- Do NOT use `assert` in production code — raise informative errors or return findings
  </action>
  <verify>
    <automated>pytest tests/test_wiki_lint_gap_drift.py -v && python tools/wiki-lint.py --full; echo "exit=$?" && python -c "
import json
d = json.loads(open('tools/vendor-sources.json').read())
assert 'confluent-agent-skills' in d
assert 'streaming-skills-plugin' in d
assert 'linuxone-accelerator-gaps' in d
trips = d['linuxone-accelerator-gaps']['trip_wires']
assert len(trips) == 13
required = {'id','title','status','workaround','fsi_impact','source','source_id'}
for t in trips:
    assert required.issubset(set(t.keys())), f'missing fields: {required - set(t.keys())} in {t.get(\"id\")}'
ids = {t['id'] for t in trips}
expected = {f'G-{n:02d}' for n in range(1,14)}
assert ids == expected, f'IDs mismatch: extra={ids-expected}, missing={expected-ids}'
print('OK: 13 trip-wires with all 7 fields, IDs G-01..G-13')
"</automated>
  </verify>
  <done>All tests in `test_wiki_lint_gap_drift.py` PASS (GREEN); `tools/vendor-sources.json` contains 13 trip-wires under `linuxone-accelerator-gaps.trip_wires` with all 7 required fields each; existing 2 vendor entries unchanged; `python tools/wiki-lint.py --full` exits 0 (passive posture preserved).</done>
</task>

</tasks>

<verification>
- `pytest tests/test_wiki_lint_gap_drift.py` PASSES (all ≥12 tests green)
- `git diff tools/vendor-sources.json` shows ONLY additions (no modifications to existing `confluent-agent-skills` or `streaming-skills-plugin` blocks)
- `python tools/wiki-lint.py --full` exits 0 (passive posture preserved; ≥0 new findings expected since the trip-wires were sourced from upstream verbatim — no drift at ingest)
- WIKI-02 requirement structurally satisfied (13 trip-wires + lint drift surface)
</verification>

<success_criteria>
1. `tools/vendor-sources.json` has `linuxone-accelerator-gaps` key with 13 trip-wires (G-01..G-13), each with all 7 required fields
2. `tools/wiki-lint.py` has `check_gap_drift()` function that surfaces DRIFT/MISSING/MALFORMED gap findings non-fatally
3. `lint_wiki()` invokes `check_gap_drift` when `full=True` and populates 3 new findings keys
4. `main()` labels dict includes labels for the 3 new finding categories
5. Existing tests (`test_wiki_lint_drift.py`, `test_check_streaming_skills_drift.py`) STILL PASS — no regression in vendor-pin drift mechanism
6. New tests (`test_wiki_lint_gap_drift.py`) all pass
7. Exit code remains 0 in all cases (passive posture per H.1 D-09)
</success_criteria>

<output>
After completion, create `.planning/phases/12-wiki-ingest-of-linuxone-accelerator/12-02-SUMMARY.md` recording:
- Exact JSON shape committed to `tools/vendor-sources.json` (13 trip-wires)
- Function signatures added to `tools/wiki-lint.py` (check_gap_drift + lint_wiki extension points)
- Test count added in `tests/test_wiki_lint_gap_drift.py`
- Any deviations from CONTEXT.md decisions (e.g. case-insensitive status comparison, MISSING-GAP vs MISSING-VENDOR naming)
- Pointer to Plan 12-03 which references 4-5 trip-wires (G-02, G-08, G-12, G-13) as canonical claims in golden review cases
</output>
</content>
</invoke>