---
phase: 12-wiki-ingest-of-linuxone-accelerator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wiki/patterns/linuxone-on-cfk-reference-architecture.md
  - wiki/patterns/x86-to-linuxone-cluster-linking-migration.md
  - wiki/concepts/fips-at-install-ocp-requirement.md
  - wiki/patterns/auditor-readonly-rbac-payload-isolation.md
  - wiki/concepts/s390x-custom-image-build-pipeline.md
  - wiki/patterns/flink-on-cfk-fsi-example-jobs.md
  - wiki/_index.md
  - wiki/_graph.md
  - wiki/_queue.md
autonomous: true
requirements: [WIKI-04]
must_haves:
  truths:
    - "User running /ask about LinuxONE-on-CFK ref arch surfaces wiki/patterns/linuxone-on-cfk-reference-architecture.md"
    - "User running /ask about x86→LinuxONE migration surfaces wiki/patterns/x86-to-linuxone-cluster-linking-migration.md (with regulatory evidence checklist)"
    - "User running /ask about FIPS-at-install OCP requirement surfaces wiki/concepts/fips-at-install-ocp-requirement.md"
    - "User running /ask about auditor-readonly RBAC isolation surfaces wiki/patterns/auditor-readonly-rbac-payload-isolation.md with the topic-scoped-binding correction"
    - "User running /ask about custom s390x Connect/Flink images surfaces wiki/concepts/s390x-custom-image-build-pipeline.md"
    - "User running /ask about Flink-on-CFK FSI example jobs surfaces wiki/patterns/flink-on-cfk-fsi-example-jobs.md (tumbling-window + temporal join)"
    - "Each of the 6 new articles has confidence: high, last_validated: 2026-05-23, source: fsi-dsp://accelerator/confluent-on-linuxone in frontmatter"
    - "wiki/_index.md lists all 6 new articles under correct sections (Concepts/Patterns)"
    - "wiki/_graph.md has ≥1 inbound edge per new article"
  artifacts:
    - path: "wiki/patterns/linuxone-on-cfk-reference-architecture.md"
      provides: "LinuxONE-on-CFK ref arch (5-layer Kustomize Component composition, Mondics base + 5 FSI hardening layers, prerequisite topology)"
      min_lines: 80
      contains: "confidence: high"
    - path: "wiki/patterns/x86-to-linuxone-cluster-linking-migration.md"
      provides: "Pre-migration audit checklist + in-flight validation + rollback + post-migration evidence collection (7y retention)"
      min_lines: 90
      contains: "MirrorLag"
    - path: "wiki/concepts/fips-at-install-ocp-requirement.md"
      provides: "FIPS-at-install OCP constraint (post-install FIPS unsupported by Red Hat) + cert-manager FIPS-validated since ≥1.14.0"
      min_lines: 50
      contains: "fips_enabled"
    - path: "wiki/patterns/auditor-readonly-rbac-payload-isolation.md"
      provides: "DeveloperRead is consume-granting; topic-scoped binding to confluent-audit-log-events + SR subjects ONLY (explicitly NOT payments.*)"
      min_lines: 60
      contains: "DeveloperRead"
    - path: "wiki/concepts/s390x-custom-image-build-pipeline.md"
      provides: "docker buildx --platform linux/s390x for Connect (G-08) + Flink SQL-runner (G-12); UBI9 base; CPACF-native JVM"
      min_lines: 60
      contains: "linux/s390x"
    - path: "wiki/patterns/flink-on-cfk-fsi-example-jobs.md"
      provides: "Tumbling-window 1-min TPS + temporal stream-table enrichment join; mTLS + SR Avro-Confluent + audit integration"
      min_lines: 60
      contains: "FlinkApplication"
    - path: "wiki/_index.md"
      provides: "All 6 new articles listed in Concepts/Patterns sections"
      contains: "linuxone-on-cfk-reference-architecture"
    - path: "wiki/_graph.md"
      provides: "≥1 inbound graph edge per new article (per H.1 discipline)"
      contains: "linuxone-on-cfk-reference-architecture"
  key_links:
    - from: "wiki/patterns/auditor-readonly-rbac-payload-isolation.md"
      to: "wiki/patterns/fsi-canon-overlay-for-confluent-skills.md"
      via: "related: frontmatter field"
      pattern: "patterns/fsi-canon-overlay-for-confluent-skills"
    - from: "wiki/patterns/linuxone-on-cfk-reference-architecture.md"
      to: "wiki/concepts/fsi-data-streaming-platform.md"
      via: "related: frontmatter field"
      pattern: "concepts/fsi-data-streaming-platform"
    - from: "wiki/patterns/x86-to-linuxone-cluster-linking-migration.md"
      to: "wiki/concepts/cluster-linking-topology.md"
      via: "related: frontmatter field"
      pattern: "concepts/cluster-linking-topology"
    - from: "wiki/_index.md"
      to: "all 6 new articles"
      via: "markdown list link"
      pattern: "linuxone-on-cfk-reference-architecture|x86-to-linuxone-cluster-linking-migration|fips-at-install-ocp-requirement|auditor-readonly-rbac-payload-isolation|s390x-custom-image-build-pipeline|flink-on-cfk-fsi-example-jobs"
    - from: "wiki/_graph.md"
      to: "all 6 new articles"
      via: "source → target : relationship line"
      pattern: "→ patterns/linuxone-on-cfk-reference-architecture|→ patterns/x86-to-linuxone-cluster-linking-migration|→ concepts/fips-at-install-ocp-requirement|→ patterns/auditor-readonly-rbac-payload-isolation|→ concepts/s390x-custom-image-build-pipeline|→ patterns/flink-on-cfk-fsi-example-jobs"
---

<objective>
Author 6 wiki articles compiled from the upstream `accelerators/confluent-on-linuxone/` tree (DESIGN.md, KNOWN-GAPS.md, MIGRATION.md, layers/*/README.md). Wire all 6 into `wiki/_index.md` and `wiki/_graph.md` with ≥1 inbound edge each per H.1 discipline. Mark previously stubbed `wiki/_queue.md` entries as ingested where applicable.

Purpose: This satisfies **WIKI-04** (the ≥6 articles requirement) and is the foundation for WIKI-01 (provenance citation), WIKI-03 (auditor-readonly review pattern grounded in article 4), and WIKI-05 (golden eval cases cite these articles). Plans 12-02 and 12-03 depend on these articles existing.

Output: 6 new wiki articles + 3 metadata file updates (`_index.md`, `_graph.md`, `_queue.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-wiki-ingest-of-linuxone-accelerator/12-CONTEXT.md

# Wiki structure + ingest patterns from H.1
@wiki/_index.md
@wiki/_graph.md
@wiki/_queue.md
@wiki/patterns/fsi-canon-overlay-for-confluent-skills.md
@wiki/concepts/linuxone-kafka-integration.md
@wiki/concepts/linuxone-platform-foundations.md

# Upstream LinuxONE accelerator source files (ALL articles must cite these by fsi-dsp:// ID)
@raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/DESIGN.md
@raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/KNOWN-GAPS.md
@raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/MIGRATION.md
@raw/repos/fsi-dsp/accelerators/confluent-on-linuxone/README.md

<interfaces>
<!-- Wiki article frontmatter shape — adopt verbatim from H.1 D-07 convention -->
<!-- Tags are comma-separated flow sequence (H.1 STATE rule) -->
<!-- Source IS the stable fsi-dsp:// ID; H.1 D-09 drift mechanism reads it via tools/wiki-lint.py -->

Frontmatter shape (verbatim from H.1 articles, e.g. wiki/concepts/avro-schema-source-directory.md):
```yaml
 ---
title: <Sentence Case Title>
tags: [tag1, tag2, tag3]   # flow sequence with commas (NOT space-separated)
sources:
  - fsi-dsp://accelerator/confluent-on-linuxone
  # additional MCP/upstream sources optional
related:
  - concepts/<slug>
  - patterns/<slug>
confidence: high
last_updated: 2026-05-23
last_validated: 2026-05-23
source: confluent-agent-skills@91d1871ef8c320be92bca955c8e42492a2778cb4   # OMIT for accelerator articles — they're fsi-dsp-sourced, not agent-skills-sourced
 ---
```

For accelerator-sourced articles, the H.1 `source: <vendor>@<sha>` field does NOT apply (vendor-pin is for agent-skills only). Use `sources: [fsi-dsp://accelerator/confluent-on-linuxone]` in frontmatter — this is the H.1 D-09 vendor-drift mechanism path (`source: fsi-dsp://...` is non-vendored and is skipped by `check_vendor_drift()` per wiki-lint.py line 110).

MANIFEST.yaml stable IDs available (verified at planning time):
- `fsi-dsp://accelerator/confluent-on-linuxone`           (top-level accelerator)
- `fsi-dsp://accelerator/confluent-on-linuxone:01-rbac`   (per-layer; format derived from MANIFEST apply_sequence)
- `fsi-dsp://accelerator/confluent-on-linuxone:02-tls`
- `fsi-dsp://accelerator/confluent-on-linuxone:03-schema-governance`
- `fsi-dsp://accelerator/confluent-on-linuxone:04-audit`
- `fsi-dsp://accelerator/confluent-on-linuxone:05-flink`
- `fsi-dsp://adr/009`                                      (LinuxONE deployment guidance ADR — referenced by article 1)
- `fsi-dsp://adr/005`                                      (CL over MRC — referenced by article 2)

Existing graph format (wiki/_graph.md):
```
source/article → target/article : relationship description
```
NOT the YAML block format — append new lines in this convention (per H.3a STATE rule).
</interfaces>

<reference>
<!-- Source-line mapping for each article — saves the executor a re-read pass -->

Article 1 (linuxone-on-cfk-reference-architecture):
- DESIGN.md L41-69 (directory structure + Kustomize Component composition)
- DESIGN.md L93-103 (Reuse Map)
- DESIGN.md L107-200 (5 layer details)
- README.md L11-21 (Provenance table — IBM upstream vs GoodLabs)

Article 2 (x86-to-linuxone-cluster-linking-migration):
- MIGRATION.md Sections 1-4 (all five sub-sections + rollback + evidence)

Article 3 (fips-at-install-ocp-requirement):
- KNOWN-GAPS.md G-02 (full detail block L49-61)
- DESIGN.md L126-141 (layer 02 mTLS + FIPS + s390x caveat)

Article 4 (auditor-readonly-rbac-payload-isolation):
- DESIGN.md "Decisions locked" #2 (L37-41 — D-02 verbatim)
- DESIGN.md L108-124 (layer 01 detail — 6 FSI roles, auditor-readonly binding)

Article 5 (s390x-custom-image-build-pipeline):
- KNOWN-GAPS.md G-05 (UBI9 base for SR bootstrap)
- KNOWN-GAPS.md G-08 (Connect image build)
- KNOWN-GAPS.md G-12 (SQL-runner image build)

Article 6 (flink-on-cfk-fsi-example-jobs):
- DESIGN.md L176-200 (layer 05 detail block — CMF compute env, FlinkApplications, mTLS, RBAC, audit integration)
- README.md L96-115 (layer 05 rationale + prerequisites)
</reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 6 wiki articles + frontmatter</name>
  <files>wiki/patterns/linuxone-on-cfk-reference-architecture.md, wiki/patterns/x86-to-linuxone-cluster-linking-migration.md, wiki/concepts/fips-at-install-ocp-requirement.md, wiki/patterns/auditor-readonly-rbac-payload-isolation.md, wiki/concepts/s390x-custom-image-build-pipeline.md, wiki/patterns/flink-on-cfk-fsi-example-jobs.md</files>
  <action>
Create all 6 wiki articles from the upstream accelerator source (per CONTEXT.md locked decisions and the `<reference>` source-line mapping above).

Each article MUST have:
1. **Frontmatter (YAML)** per the H.1 D-07 convention:
   - `title:` Sentence Case article title
   - `tags:` flow sequence with commas (e.g. `tags: [kafka, linuxone, ibm, s390x, fsi]`) — NOT space-separated
   - `sources:` list — first entry MUST be `fsi-dsp://accelerator/confluent-on-linuxone` (or layer-scoped `:NN-layer-name` for layer-specific articles); additional MCP/upstream URLs allowed
   - `related:` list — at least 3 related wiki articles (concepts/foo, patterns/bar form, NO `wiki/` prefix, NO `.md` suffix)
   - `confidence: high`
   - `last_updated: 2026-05-23`
   - `last_validated: 2026-05-23`
   - Do NOT add `source: <vendor>@<sha>` — that field is reserved for `confluent-agent-skills@<sha>` vendor-pin (H.1 D-09) and does NOT apply to fsi-dsp-sourced articles

2. **Body** — each article ≥50 lines, grounded ONLY in the cited upstream source lines (`<reference>` block above) plus existing wiki cross-references. Inline `⚠️ unverified` markers per H.1 D-08 for any claim NOT directly grounded in DESIGN.md / KNOWN-GAPS.md / MIGRATION.md / README.md.

**Per-article specifics:**

**Article 1 — `wiki/patterns/linuxone-on-cfk-reference-architecture.md`**
- Source: `fsi-dsp://accelerator/confluent-on-linuxone`
- Body covers:
  - Provenance (IBM Mondics base CP 8.2.0 + 5 GoodLabs FSI hardening layers)
  - Composition mechanism: `kind: Component` overlays with explicit layer ordering (01-rbac → 02-tls → 03-schema-governance → 04-audit → 05-flink)
  - Reuse Map (existing repo assets wired in — `ansible/roles/cp_rbac`, `cp_mtls`, `cfk_operator`, `observability/{splunk,dynatrace}`)
  - Per-layer one-line responsibility summary (RBAC=MDS+LDAP; TLS=mTLS+FIPS; SR=FULL_TRANSITIVE; audit=7y `confluent-audit-log-events`; Flink=CMF+FlinkApps)
  - CFK CRD JSON-merge gotcha (each layer owns a distinct CR field to avoid collision)
- `related:` MUST include `concepts/fsi-data-streaming-platform`, `concepts/linuxone-kafka-integration`, `concepts/linuxone-platform-foundations`, `patterns/auditor-readonly-rbac-payload-isolation`, `patterns/flink-on-cfk-fsi-example-jobs`

**Article 2 — `wiki/patterns/x86-to-linuxone-cluster-linking-migration.md`**
- Source: `fsi-dsp://accelerator/confluent-on-linuxone` + `fsi-dsp://adr/005`
- Body covers all 4 MIGRATION.md sections:
  - §1 Pre-migration audit (RBAC posture, TLS ≥1.2, SR compatibility ≥BACKWARD, audit log on source, regulatory data scope)
  - §2 Cluster Link establishment + in-flight validation (mirror lag → 0, record-count parity via end-offsets, schema parity — explicit "SR DOES NOT replicate via Cluster Linking" trip-wire)
  - §3 Rollback procedure (re-point consumers, demote mirrors, RTO 15-30 min)
  - §4 Post-migration evidence collection (mirror-lag log, record-count comparison, audit log continuity, RBAC parity, schema parity, 7y retention)
- `related:` MUST include `concepts/cluster-linking-topology`, `patterns/dr-cluster-linking`, `patterns/dr-mirrormaker2`, `synthesis/adr-index`

**Article 3 — `wiki/concepts/fips-at-install-ocp-requirement.md`**
- Source: `fsi-dsp://accelerator/confluent-on-linuxone:02-tls`
- Body covers:
  - `spec.tls.fips.enabled: true` is a no-op on non-FIPS OCP installations
  - Red Hat does NOT support post-install FIPS conversion — must be at install (`fips: true` in `install-config.yaml`)
  - Detection: `oc get node -o json` + `/proc/sys/crypto/fips_enabled=1` check
  - cert-manager Operator for OpenShift FIPS-validated since ≥1.14.0 (s390x-supported)
  - FSI consequence: silent compliance failure — verify before applying layer 02
- `related:` MUST include `patterns/auditor-readonly-rbac-payload-isolation`, `concepts/fsi-compliance`, `patterns/linuxone-on-cfk-reference-architecture`

**Article 4 — `wiki/patterns/auditor-readonly-rbac-payload-isolation.md`** (CANONICAL article for WIKI-03)
- Source: `fsi-dsp://accelerator/confluent-on-linuxone:01-rbac`
- Body MUST contain — VERBATIM in body text (used as grep target by Plan 12-03's review-engine canonical correction):
  - "DeveloperRead is consume-granting at the topic-prefix scope it's bound to"
  - "auditor isolation requires topic-scoped binding to `confluent-audit-log-events` + SR subjects ONLY"
  - "explicitly NOT to `payments.*` business topics"
- Cover:
  - The 6 FSI roles from layer 01 (platform-admin, topic-admin, producer-only, consumer-only, schema-admin, auditor-readonly)
  - DESIGN.md D-02 decision (verbatim quote allowed under fair-use for committed-design-record context)
  - Why cluster-scoped DeveloperRead fails: it grants consume on all PREFIXED topics under that binding scope
  - Validation: `confluent iam rbac role-binding list` + topic-prefix scope inspection
- `related:` MUST include `patterns/fsi-canon-overlay-for-confluent-skills`, `concepts/fsi-compliance`, `patterns/linuxone-on-cfk-reference-architecture`

**Article 5 — `wiki/concepts/s390x-custom-image-build-pipeline.md`**
- Source: `fsi-dsp://accelerator/confluent-on-linuxone:04-audit` + `:05-flink`
- Body covers:
  - `docker buildx build --platform linux/s390x` as the canonical command shape
  - Connect image (G-08): base `confluentinc/confluent-kafka-connect:8.2.0` + Splunk Sink + HTTP Sink JARs (arch-neutral Java)
  - SQL-runner image (G-12): base `confluentinc/cp-flink:1.20.0-cp1` + `flink-sql-connector-kafka:3.2.0-1.20` + `flink-sql-avro-confluent-registry:1.20.0` + GoodLabs `fsi-sql-runner.jar`
  - SR bootstrap Job (G-05): UBI9 minimal as the s390x-supported base
  - Why x86 sidecars are anti-pattern (G-06): QEMU binary translation 2-5x CPU overhead
- `related:` MUST include `concepts/linuxone-kafka-integration`, `concepts/linuxone-platform-foundations`, `patterns/linuxone-on-cfk-reference-architecture`

**Article 6 — `wiki/patterns/flink-on-cfk-fsi-example-jobs.md`**
- Source: `fsi-dsp://accelerator/confluent-on-linuxone:05-flink`
- Body covers:
  - Layer 05 architecture (FlinkEnvironment + CMFRestClass CRs, resources-only Component — no patches)
  - Two example jobs: 1-minute transaction-volume tumbling window; account-transaction temporal stream-table enrichment join
  - mTLS via cert-manager Certificate CRs signed by `confluent-ca-issuer` (registered by layer 02)
  - SR Avro-Confluent format over `https://` (layer 02 mTLS'd SR)
  - RBAC: `flink-developer` (LDAP group → FlinkEnvironmentAdmin); three `flink-job-runtime-*` service-account bindings (DeveloperRead source, DeveloperWrite sink, DeveloperRead SR)
  - Audit integration: Flink's Kafka access automatically audited via ConfluentServerAuthorizer (layer 01 → layer 04) — no layer-04 change needed
  - Prerequisites: FKO + CMF Helm operators (RUNBOOK Step 1b); custom s390x SQL-runner image (G-12)
- `related:` MUST include `patterns/flink-runtime-models`, `concepts/flink-checkpointing`, `concepts/linuxone-platform-foundations`, `patterns/linuxone-on-cfk-reference-architecture`

**Avoid:**
- Speculation about per-CFU sizing or throughput numbers — `⚠️ unverified` if needed
- Quoting more than ~3 consecutive paragraphs verbatim from upstream (fair-use boundary)
- Cross-references to articles that don't exist (verify via `ls wiki/{concepts,patterns}/`)
- Tags as space-separated string (`tags: [a b c]`) — MUST be comma-separated flow sequence
  </action>
  <verify>
    <automated>python -c "
import yaml
from pathlib import Path
articles = [
    'wiki/patterns/linuxone-on-cfk-reference-architecture.md',
    'wiki/patterns/x86-to-linuxone-cluster-linking-migration.md',
    'wiki/concepts/fips-at-install-ocp-requirement.md',
    'wiki/patterns/auditor-readonly-rbac-payload-isolation.md',
    'wiki/concepts/s390x-custom-image-build-pipeline.md',
    'wiki/patterns/flink-on-cfk-fsi-example-jobs.md',
]
for p in articles:
    path = Path(p)
    assert path.exists(), f'MISSING: {p}'
    content = path.read_text()
    assert content.startswith('---'), f'NO FRONTMATTER: {p}'
    end = content.find('---', 3)
    fm = yaml.safe_load(content[3:end])
    assert fm.get('confidence') == 'high', f'BAD CONFIDENCE in {p}: {fm.get(\"confidence\")}'
    assert str(fm.get('last_validated')) == '2026-05-23', f'BAD last_validated in {p}: {fm.get(\"last_validated\")}'
    sources = fm.get('sources', [])
    assert any('fsi-dsp://accelerator/confluent-on-linuxone' in str(s) for s in sources), f'NO ACCELERATOR SOURCE in {p}: {sources}'
    assert len(content.splitlines()) >= 50, f'TOO SHORT in {p}: {len(content.splitlines())} lines'
print('OK: all 6 articles have valid frontmatter + ≥50 lines + accelerator source')
"</automated>
  </verify>
  <done>All 6 article files exist; each has `confidence: high`, `last_validated: 2026-05-23`, `sources:` containing `fsi-dsp://accelerator/confluent-on-linuxone` (or layer-scoped variant); ≥50 lines each; auditor-readonly article body contains the 3 grep-target strings for Plan 12-03's canonical correction.</done>
</task>

<task type="auto">
  <name>Task 2: Wire articles into wiki/_index.md and wiki/_graph.md</name>
  <files>wiki/_index.md, wiki/_graph.md, wiki/_queue.md</files>
  <action>
**Update `wiki/_index.md`:**

Add the 4 patterns articles under the `## Patterns` section (in the existing alphabetical-ish ordering — append to end is fine if existing order is mixed). Add the 2 concepts articles under `## Concepts`. Use the existing entry format verbatim:

```
[Title](path/to/article.md) — one-line summary — #tag1 #tag2
```

Entries to add:

Under `## Concepts`:
```
[FIPS-at-install OCP Requirement](concepts/fips-at-install-ocp-requirement.md) — Trip-wire: spec.tls.fips.enabled is no-op on non-FIPS OCP; Red Hat does not support post-install FIPS conversion — #linuxone #ibm #s390x #ocp #fips #fsi
[s390x Custom Image Build Pipeline](concepts/s390x-custom-image-build-pipeline.md) — docker buildx --platform linux/s390x for CFK Connect (G-08) and Flink SQL-runner (G-12); UBI9 base for SR bootstrap; arch-neutral Java JARs — #linuxone #ibm #s390x #docker-buildx #connect #flink #fsi
```

Under `## Patterns`:
```
[LinuxONE-on-CFK Reference Architecture](patterns/linuxone-on-cfk-reference-architecture.md) — 5-layer Kustomize Component composition (RBAC → mTLS → SR governance → audit → Flink) on IBM Mondics CP 8.2.0 base; FSI-hardened CFK on OCP-on-LinuxONE — #linuxone #ibm #s390x #cfk #ocp #fsi #accelerator
[x86 → LinuxONE Cluster Linking Migration](patterns/x86-to-linuxone-cluster-linking-migration.md) — Pre-migration audit + in-flight validation (mirror-lag, end-offset parity, schema parity) + rollback + 7y regulatory evidence collection — #linuxone #cluster-linking #migration #fsi #compliance
[Auditor-Readonly RBAC Payload Isolation](patterns/auditor-readonly-rbac-payload-isolation.md) — DeveloperRead is consume-granting; auditor isolation requires topic-scoped binding to confluent-audit-log-events + SR subjects ONLY, NOT to payments.* business topics — #rbac #mds #audit #fsi #linuxone
[Flink-on-CFK FSI Example Jobs](patterns/flink-on-cfk-fsi-example-jobs.md) — Tumbling-window TPS + temporal stream-table enrichment join on CMF-managed Flink with mTLS, SR Avro-Confluent, audit integration — #flink #cmf #cfk #linuxone #fsi #s390x
```

**Update `wiki/_graph.md`:**

Append a new block after the existing backlinks (mirror the line-based format used throughout the file — H.3a STATE rule). At minimum, each new article MUST have ≥1 inbound edge:

```
patterns/linuxone-on-cfk-reference-architecture → concepts/fsi-data-streaming-platform : platform context the accelerator targets
patterns/linuxone-on-cfk-reference-architecture → concepts/linuxone-kafka-integration : LinuxONE Kafka offload context
patterns/linuxone-on-cfk-reference-architecture → concepts/linuxone-platform-foundations : s390x foundations
patterns/linuxone-on-cfk-reference-architecture → patterns/auditor-readonly-rbac-payload-isolation : layer 01 RBAC pattern
patterns/linuxone-on-cfk-reference-architecture → patterns/flink-on-cfk-fsi-example-jobs : layer 05 Flink applications

patterns/x86-to-linuxone-cluster-linking-migration → concepts/cluster-linking-topology : CL mechanism underlying the migration
patterns/x86-to-linuxone-cluster-linking-migration → patterns/dr-cluster-linking : DR pattern using same CL primitive
patterns/x86-to-linuxone-cluster-linking-migration → synthesis/adr-index : ADR-005 CL-over-MRC decision

concepts/fips-at-install-ocp-requirement → patterns/linuxone-on-cfk-reference-architecture : layer 02 TLS+FIPS context
concepts/fips-at-install-ocp-requirement → concepts/fsi-compliance : PCI-DSS / FFIEC encryption-in-transit context

patterns/auditor-readonly-rbac-payload-isolation → patterns/fsi-canon-overlay-for-confluent-skills : FSI RBAC overlay on upstream skills
patterns/auditor-readonly-rbac-payload-isolation → concepts/fsi-compliance : SOX/FFIEC/PCI-DSS least-privilege
patterns/auditor-readonly-rbac-payload-isolation → patterns/linuxone-on-cfk-reference-architecture : layer 01 RBAC source

concepts/s390x-custom-image-build-pipeline → concepts/linuxone-kafka-integration : LinuxONE Kafka context
concepts/s390x-custom-image-build-pipeline → concepts/linuxone-platform-foundations : s390x platform foundations
concepts/s390x-custom-image-build-pipeline → patterns/linuxone-on-cfk-reference-architecture : layer 04 Connect + layer 05 SQL-runner image consumers

patterns/flink-on-cfk-fsi-example-jobs → patterns/flink-runtime-models : CMF is one of three Flink runtime options
patterns/flink-on-cfk-fsi-example-jobs → concepts/flink-checkpointing : checkpoint encryption context (G-13)
patterns/flink-on-cfk-fsi-example-jobs → concepts/linuxone-platform-foundations : s390x foundations
patterns/flink-on-cfk-fsi-example-jobs → patterns/linuxone-on-cfk-reference-architecture : layer 05 in the 5-layer composition
```

Also add inbound edges TO the new articles (each new article needs ≥1 inbound — per H.1 STATE pattern). Add:

```
concepts/fsi-data-streaming-platform → patterns/linuxone-on-cfk-reference-architecture : LinuxONE deployment model
concepts/linuxone-kafka-integration → patterns/linuxone-on-cfk-reference-architecture : CFK-on-OCP-on-LinuxONE pattern
concepts/linuxone-platform-foundations → concepts/s390x-custom-image-build-pipeline : image build for s390x
patterns/dr-cluster-linking → patterns/x86-to-linuxone-cluster-linking-migration : migration variant of CL
patterns/fsi-canon-overlay-for-confluent-skills → patterns/auditor-readonly-rbac-payload-isolation : RBAC payload-isolation rule
patterns/flink-runtime-models → patterns/flink-on-cfk-fsi-example-jobs : CMF FSI examples
```

**Update `wiki/_queue.md`:**

No new stubs to mark — the 6 articles are net-new (not previously queued). If `auditor-readonly`, `linuxone-on-cfk-ref-arch`, or any of the 6 article slugs DO appear in `## Stubs to Create` or `## Candidate Articles`, mark them `[x]` with the ingest note: "compiled 2026-05-23 via Phase 12 plan 12-01". Otherwise no changes required to `_queue.md`.
  </action>
  <verify>
    <automated>python -c "
from pathlib import Path
idx = Path('wiki/_index.md').read_text()
graph = Path('wiki/_graph.md').read_text()
articles = ['linuxone-on-cfk-reference-architecture', 'x86-to-linuxone-cluster-linking-migration', 'fips-at-install-ocp-requirement', 'auditor-readonly-rbac-payload-isolation', 's390x-custom-image-build-pipeline', 'flink-on-cfk-fsi-example-jobs']
for a in articles:
    assert a in idx, f'MISSING from _index.md: {a}'
    # ≥1 inbound edge means article appears as target (RHS of →)
    inbound = [line for line in graph.splitlines() if f'→ ' in line and a in line.split('→', 1)[1]]
    assert len(inbound) >= 1, f'MISSING inbound graph edge for: {a}'
print('OK: all 6 articles indexed and have ≥1 inbound graph edge')
"</automated>
  </verify>
  <done>All 6 articles listed in `wiki/_index.md` under correct sections (Concepts/Patterns); each has ≥1 inbound edge in `wiki/_graph.md`; `wiki/_queue.md` updated if any of the 6 slugs were previously queued as stubs.</done>
</task>

</tasks>

<verification>
- `python tools/wiki-lint.py --full` runs without new errors against the 6 new articles. Warnings about `UNKNOWN VENDOR` for `fsi-dsp://...` sources are EXPECTED (these are non-vendored per H.1 D-09 — `check_vendor_drift()` returns empty for sources without `@` separator; verified at wiki-lint.py L110).
- `pytest tests/test_wiki_citations.py::TestCitationResolution::test_all_citations_resolve_against_manifest` PASSES — every `fsi-dsp://` citation in the 6 new articles resolves against MANIFEST.yaml IDs (verified: `accelerator/confluent-on-linuxone`, `adr/005`, `adr/009` all exist in MANIFEST.yaml).
- `pytest tests/test_wiki_citations.py::TestNoRawPaths::test_no_raw_fsi_dsp_paths_in_sources` will STILL fail on the 4 pre-existing observability articles — that's plan 12-04's responsibility (carry-forward fix). Do NOT touch those 4 files in this plan.
</verification>

<success_criteria>
1. 6 new wiki articles exist on disk with H.1-compliant frontmatter (`confidence: high`, `last_validated: 2026-05-23`, `sources:` cites `fsi-dsp://accelerator/confluent-on-linuxone` or layer-scoped variant)
2. Each article has ≥50 lines of body content grounded in the cited upstream source
3. `wiki/_index.md` lists all 6 articles under correct sections
4. `wiki/_graph.md` has ≥1 inbound edge per new article
5. Article 4 (auditor-readonly) body contains the 3 verbatim strings used as grep targets by Plan 12-03's review-engine canonical correction
6. `python tools/wiki-lint.py --full` runs without NEW failures (pre-existing carry-forward findings on observability articles remain — Plan 12-04 closes those)
7. WIKI-04 requirement is structurally satisfied (the ≥6-articles bar)
</success_criteria>

<output>
After completion, create `.planning/phases/12-wiki-ingest-of-linuxone-accelerator/12-01-SUMMARY.md` recording:
- The 6 article paths created with one-line summaries
- The exact `sources:` field on each article (for downstream plans to grep)
- Total lines added to `wiki/_index.md` and `wiki/_graph.md`
- Any deviations from the locked CONTEXT.md decisions (with rationale)
- Pointer to: Plan 12-02 (trip-wires) + Plan 12-03 (eval cases + review pattern) depend on these articles existing
</output>
</content>
</invoke>