---
phase: 02-review-skill
plan: "03"
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - tests/golden/review/__init__.py
  - tests/golden/review/test_golden_review.py
  - tests/golden/review/README.md
  - tests/golden/review/cases/
  - tests/golden/review/fixtures/
autonomous: true
requirements: [REVW-01, REVW-02, REVW-03, REVW-04, REVW-05, REVW-06]

must_haves:
  truths:
    - "tests/golden/review/ contains >= 15 golden case files with valid YAML front matter"
    - "At least one case has premise_challenge_expected: true (exercises REVW-02)"
    - "At least one case has overlay set to a non-null value (exercises REVW-06)"
    - "At least one case has multiple input_files entries (exercises REVW-04)"
    - "At least one case has expected_verdict_contains with Corrected (exercises correction accuracy)"
    - "test_golden_review.py structural tests all pass via pytest"
  artifacts:
    - path: "tests/golden/review/test_golden_review.py"
      provides: "Structural test runner for review golden cases (mirrors test_golden_ask.py)"
      contains: "class TestGoldenReviewStructure"
    - path: "tests/golden/review/__init__.py"
      provides: "Python package marker"
    - path: "tests/golden/review/README.md"
      provides: "Case format specification and coverage requirements"
      contains: "REVW-05"
    - path: "tests/golden/review/cases/"
      provides: ">= 15 golden case .md files"
    - path: "tests/golden/review/fixtures/"
      provides: "Synthetic test documents (15-50 lines each)"
  key_links:
    - from: "tests/golden/review/test_golden_review.py"
      to: "tests/golden/review/cases/"
      via: "CASES_DIR glob for *.md files"
      pattern: "CASES_DIR"
    - from: "tests/golden/review/cases/*.md"
      to: "tests/golden/review/fixtures/"
      via: "input_files field references fixture paths"
      pattern: "input_files"
---

<objective>
Build the golden test harness for the /review skill with >= 15 structural cases covering all requirement dimensions.

Purpose: REVW-05 requires a golden test harness with >= 15 cases from sanitized (synthetic) documents. The harness structurally validates case file format and coverage -- it does NOT execute the review skill (LLM evaluation deferred to Phase 4, same as /ask harness). Additionally, it validates that cases exist for REVW-02 (premise-challenge), REVW-04 (multi-doc), and REVW-06 (customer overlay).

Output: `tests/golden/review/` directory with test runner, 15+ case files, and synthetic fixture documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-review-skill/02-CONTEXT.md
@.planning/phases/02-review-skill/02-RESEARCH.md
@.planning/phases/02-review-skill/02-01-SUMMARY.md
@.planning/phases/02-review-skill/02-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From tests/golden/ask/test_golden_ask.py (harness pattern to mirror):

Key constants and functions the executor must replicate for /review:
- CASES_DIR = Path(__file__).parent / "cases"
- REQUIRED_FIELDS = set of required YAML front matter field names
- VALID_FLOOR_MODELS = {"haiku", "sonnet"}
- load_case(path) parses YAML front matter between triple-dash delimiters
- ALL_CASES = sorted glob of *.md in CASES_DIR
- TestGoldenHarnessStructure class with parametrized per-case tests
- TestFloorModelDistribution class with haiku/sonnet count checks

Review case YAML front matter required fields:
  id, input_files, expected_claims_min, floor_model, tags,
  required_report_sections, forbidden_content

Optional fields:
  expected_verdict_contains (list of verdict strings),
  premise_challenge_expected (bool),
  overlay (string or null)

Each case file has YAML front matter (between triple-dash lines) followed by
markdown body describing what the review must find/not find.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test runner and README (RED phase)</name>
  <files>tests/golden/review/__init__.py, tests/golden/review/test_golden_review.py, tests/golden/review/README.md</files>
  <read_first>
    - tests/golden/ask/test_golden_ask.py (exact pattern to mirror -- load_case, ALL_CASES, parametrize, TestGoldenHarnessStructure)
    - tests/golden/ask/README.md (README format to replicate)
    - tests/golden/ask/__init__.py (package marker)
    - tests/golden/__init__.py (parent package marker -- already exists from Phase 1)
  </read_first>
  <behavior>
    - test_minimum_case_count: asserts len(ALL_CASES) >= 15
    - test_premise_challenge_case_exists: asserts at least 1 case has premise_challenge_expected == True
    - test_overlay_case_exists: asserts at least 1 case has overlay != null
    - test_multi_doc_case_exists: asserts at least 1 case has len(input_files) > 1
    - test_correction_case_exists: asserts at least 1 case has Corrected in expected_verdict_contains
    - test_case_id_unique: all case IDs are unique
    - test_case_has_required_fields (parametrized): each case has all REQUIRED_FIELDS
    - test_case_has_valid_floor_model (parametrized): floor_model in haiku or sonnet
    - test_input_files_is_list (parametrized): input_files is a list
    - test_required_report_sections_is_list (parametrized): required_report_sections is a list
    - test_fixture_files_exist (parametrized): each file in input_files exists on disk
  </behavior>
  <action>
Create three files:

**tests/golden/review/__init__.py** -- empty file (package marker)

**tests/golden/review/test_golden_review.py** -- Mirror tests/golden/ask/test_golden_ask.py exactly, with these changes:

Module-level constants:
- CASES_DIR = Path(__file__).parent / "cases"
- FIXTURES_DIR = Path(__file__).parent / "fixtures"
- REQUIRED_FIELDS = {"id", "input_files", "expected_claims_min", "floor_model", "tags", "required_report_sections", "forbidden_content"}
- VALID_FLOOR_MODELS = {"haiku", "sonnet"}

load_case(path) function -- identical to ask version: parse YAML front matter between triple-dash delimiters using yaml.safe_load

ALL_CASES -- sorted glob of *.md in CASES_DIR, empty list if dir missing

TestGoldenReviewStructure class (REVW-05):
- test_cases_directory_exists: assert CASES_DIR.exists()
- test_fixtures_directory_exists: assert FIXTURES_DIR.exists()
- test_minimum_case_count: assert len(ALL_CASES) >= 15
- test_premise_challenge_case_exists (REVW-02): at least 1 case with premise_challenge_expected == True
- test_overlay_case_exists (REVW-06): at least 1 case with overlay not None
- test_multi_doc_case_exists (REVW-04): at least 1 case with len(input_files) > 1
- test_correction_case_exists: at least 1 case with "Corrected" in expected_verdict_contains list
- test_case_id_unique: no duplicate IDs
- Parametrized per-case tests: test_case_has_required_fields, test_case_has_valid_floor_model, test_input_files_is_list, test_required_report_sections_is_list, test_forbidden_content_is_list, test_fixture_files_exist

For test_fixture_files_exist: resolve each input_files path relative to the repo root:
  full = Path(__file__).resolve().parent.parent.parent.parent / fpath

TestFloorModelDistribution class:
- test_haiku_cases_exist: assert >= 5 haiku-floor cases
- test_sonnet_cases_exist: assert >= 5 sonnet-floor cases

**tests/golden/review/README.md** -- Document case format spec, required fields, minimum thresholds (15 cases, 1+ premise-challenge, 1+ overlay, 1+ multi-doc), fixture conventions. Mirror tests/golden/ask/README.md structure.

After creating these files, run tests -- they MUST fail (RED phase) because cases/ and fixtures/ directories do not exist yet. Commit the failing test infrastructure.
  </action>
  <verify>
    <automated>python3 -m pytest tests/golden/review/test_golden_review.py -v 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/golden/review/__init__.py exists (empty file)
    - tests/golden/review/test_golden_review.py exists and contains "class TestGoldenReviewStructure"
    - tests/golden/review/test_golden_review.py contains "def test_minimum_case_count" with assertion >= 15
    - tests/golden/review/test_golden_review.py contains "def test_premise_challenge_case_exists"
    - tests/golden/review/test_golden_review.py contains "def test_overlay_case_exists"
    - tests/golden/review/test_golden_review.py contains "def test_multi_doc_case_exists"
    - tests/golden/review/test_golden_review.py contains "def test_fixture_files_exist"
    - tests/golden/review/test_golden_review.py contains REQUIRED_FIELDS with id, input_files, expected_claims_min, floor_model, tags
    - tests/golden/review/README.md exists and contains "REVW-05"
    - Running pytest on the file produces FAILURES (RED phase -- cases dir missing)
  </acceptance_criteria>
  <done>
    Test runner and README exist. Tests fail because no cases or fixtures exist yet (confirmed RED phase). Commit: test(02-03): add golden review harness pytest runner and README (RED phase)
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 15+ golden cases and fixture documents (GREEN phase)</name>
  <files>tests/golden/review/cases/, tests/golden/review/fixtures/</files>
  <read_first>
    - tests/golden/review/test_golden_review.py (REQUIRED_FIELDS and structural assertions to satisfy)
    - tests/golden/ask/cases/mcp-acks-config-001.md (case format example from /ask harness)
    - .claude/commands/review.md (review process after Plan 01 -- understand claim categories, premise-challenge, multi-doc flow)
    - canon/base/defaults.yaml (canon values for bad config fixture documents)
    - canon/industry/fsi/overrides.yaml (FSI overrides for premise-challenge cases)
    - canon/customer/acme-bank/overrides.yaml (customer overlay for overlay cases -- from Plan 02)
  </read_first>
  <action>
Create tests/golden/review/cases/ and tests/golden/review/fixtures/ directories, then author all fixture documents and case files.

**FIXTURE DOCUMENTS** (in tests/golden/review/fixtures/):
Create 8 synthetic documents, each 15-50 lines, exercising specific review behaviors:

1. **bad-acks-producer.md** (~20 lines) -- Producer config doc recommending acks=1 and enable.idempotence=false. Contains at least 3 extractable claims. Triggers corrections on acks and idempotence.

2. **correct-consumer-config.md** (~25 lines) -- Consumer config doc following all canon defaults (auto.offset.reset=earliest, enable.auto.commit=false, one group per app). All claims should be Confirmed.

3. **fsi-latency-assumptions.md** (~30 lines) -- Architecture doc assuming sub-10ms is sufficient for market data. Contains unstated premises about network latency. Triggers premise-challenge under FSI context (market_data tier is sub-millisecond).

4. **multi-doc-deck.md** (~20 lines) -- Slide-style content describing a Kafka architecture with Schema Registry using JSON Schema. Contains 2-3 claims.

5. **multi-doc-tfvars.tfvars** (~15 lines) -- Terraform variables file with partition_count = 3, replication_factor = 2, compression_type = "gzip". Contains config claims that deviate from canon.

6. **multi-doc-runbook.md** (~20 lines) -- Operational runbook referencing the deck and tfvars, with instructions about consumer group management. Contains 2-3 claims.

7. **overlay-compression-doc.md** (~20 lines) -- Doc recommending compression.type=lz4 for producers. Under base canon this is Aligned; under acme-bank overlay (which requires zstd) this becomes Deviates.

8. **schema-evolution-review.md** (~25 lines) -- Doc about schema evolution using FULL compatibility mode. Contains architecture choices and a comparison claim about Avro vs Protobuf.

**GOLDEN CASE FILES** (in tests/golden/review/cases/):
Create 16 case files (exceeds 15 minimum with buffer). Each uses YAML front matter with all REQUIRED_FIELDS.

Case distribution plan:

| # | Case ID | Fixture(s) | Key Test | floor_model | premise_challenge | overlay |
|---|---------|-----------|----------|-------------|-------------------|---------|
| 1 | review-bad-acks-001 | bad-acks-producer.md | Correction accuracy | haiku | false | null |
| 2 | review-bad-acks-premise-002 | bad-acks-producer.md | Premise challenge | sonnet | true | null |
| 3 | review-correct-consumer-003 | correct-consumer-config.md | All Confirmed | haiku | false | null |
| 4 | review-fsi-latency-004 | fsi-latency-assumptions.md | Premise challenge | sonnet | true | null |
| 5 | review-fsi-overlay-latency-005 | fsi-latency-assumptions.md | Overlay differential | sonnet | true | acme-bank |
| 6 | review-multi-doc-006 | deck + tfvars + runbook | Multi-doc merge | sonnet | false | null |
| 7 | review-overlay-compression-007 | overlay-compression-doc.md | Base aligned | haiku | false | null |
| 8 | review-overlay-compression-diff-008 | overlay-compression-doc.md | Overlay differential | haiku | false | acme-bank |
| 9 | review-schema-evolution-009 | schema-evolution-review.md | Architecture claims | sonnet | false | null |
| 10 | review-schema-premise-010 | schema-evolution-review.md | Premise challenge | sonnet | true | null |
| 11 | review-config-values-011 | bad-acks-producer.md | config_value category | haiku | false | null |
| 12 | review-behavior-assertion-012 | correct-consumer-config.md | behavior_assertion | haiku | false | null |
| 13 | review-metrics-sla-013 | fsi-latency-assumptions.md | metric_sla category | haiku | false | null |
| 14 | review-multi-doc-contradiction-014 | deck + tfvars | Cross-doc contradiction | sonnet | false | null |
| 15 | review-full-pipeline-015 | overlay-compression-doc.md | All sections present | sonnet | true | acme-bank |
| 16 | review-empty-premise-016 | correct-consumer-config.md | No premise challenges | haiku | false | null |

Each case file follows this format (YAML front matter between triple-dash lines, then markdown body):

Front matter fields (all required by REQUIRED_FIELDS):
- id: unique case identifier
- input_files: list of fixture paths (relative to repo root)
- expected_claims_min: minimum number of claims expected
- floor_model: "haiku" or "sonnet"
- tags: list of topic tags
- required_report_sections: list of section headings that must appear
- forbidden_content: list of strings that must NOT appear

Optional fields:
- expected_verdict_contains: list of verdict strings (e.g., "Corrected", "Confirmed")
- premise_challenge_expected: boolean
- overlay: customer overlay name or null

Markdown body:
- What the review MUST find (expected behaviors)
- What the review MUST NOT contain (negative assertions)
- Negative-space trigger indicator

For multi-doc cases, input_files lists multiple fixture paths. For overlay cases, overlay is "acme-bank". For premise-challenge cases, premise_challenge_expected is true and required_report_sections includes "## Premise Challenge".

After creating all files, run the test suite -- all structural tests MUST pass (GREEN phase).
  </action>
  <verify>
    <automated>python3 -m pytest tests/golden/review/test_golden_review.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/golden/review/cases/ directory contains >= 15 .md files
    - tests/golden/review/fixtures/ directory contains >= 6 fixture files
    - At least 1 case file has premise_challenge_expected set to true
    - At least 1 case file has overlay set to acme-bank (non-null)
    - At least 1 case file has multiple entries in input_files
    - At least 1 case file has Corrected in expected_verdict_contains
    - Every case file has all REQUIRED_FIELDS: id, input_files, expected_claims_min, floor_model, tags, required_report_sections, forbidden_content
    - Every input_files entry references a file that exists on disk
    - All case IDs are unique
    - python3 -m pytest tests/golden/review/test_golden_review.py -v exits 0 with all tests passing
    - Best-effort: python3 -m pytest tests/ -x -q passes (no regressions), but not a blocking gate — scoped pass requirement is tests/golden/review/ only
  </acceptance_criteria>
  <done>
    Golden review harness has 16 case files and 8 fixture documents. All structural tests pass (GREEN phase). Distribution covers:
    - 4 premise-challenge cases (REVW-02)
    - 3 overlay cases including acme-bank (REVW-06)
    - 2 multi-doc cases (REVW-04)
    - 2 correction accuracy cases (tests REVW-01 reproducibility anchors)
    - 8 haiku-floor + 8 sonnet-floor cases (balanced distribution)
    Commit: feat(02-03): author 16 golden review cases and 8 fixture documents
  </done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/golden/review/test_golden_review.py -v` exits 0
- `python3 -m pytest tests/golden/review/ -v` exits 0 (scoped to review harness)
- `ls tests/golden/review/cases/*.md | wc -l` returns >= 15
- `ls tests/golden/review/fixtures/ | wc -l` returns >= 6
- `grep -l "premise_challenge_expected: true" tests/golden/review/cases/*.md | wc -l` returns >= 1
- Best-effort: `python3 -m pytest tests/ -x -q` exits 0 (full suite, no regressions)
</verification>

<success_criteria>
The golden review harness structurally validates all REVW requirements through case coverage: REVW-01 (structured claims with expected counts), REVW-02 (premise-challenge cases), REVW-04 (multi-doc cases), REVW-05 (>= 15 cases), REVW-06 (overlay cases). The harness mirrors the Phase 1 /ask pattern exactly -- same load_case utility, same parametrize approach, same YAML front matter + markdown body format. Review harness tests pass with no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-review-skill/02-03-SUMMARY.md`
</output>
