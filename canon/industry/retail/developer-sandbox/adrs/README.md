# Retail Developer-Sandbox Canon ADRs

**Status: template (pre-ADR).** Overrides in `../overrides.yaml` cite
`retail-sandbox://context/<dimension>` placeholders — deliberate dev-tier departures
from the prod retail overlay (`../../overrides.yaml`), mirroring the FSI
developer-sandbox pattern at `canon/industry/fsi/developer-sandbox/adrs/`.

## Promotion Path

After one retail engagement using the `developer/sandbox` profile validates these
dev-tier values in practice, promote each context-sourced override to a formal ADR
(`adr-001.md`, ...). Until then, this README is the canonical justification.

## See Also

- `../../overrides.yaml` — prod retail overlay (the bifurcation point)
- `canon/industry/fsi/developer-sandbox/` — the FSI analog this mirrors
