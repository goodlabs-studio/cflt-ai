# Activity Log — 2026-05

## 2026-05-04T20:00:00Z
**Skill:** ad-hoc validation + authoring
**Overlay:** base + fsi + linuxone
**Input:** `LinuxOneValidation .docx`, `UpdatedKafkaTuningSuggestions.docx` (root of repo)
**Output:**
- `wiki/patterns/linuxone-validation-suite.md` (new)
- `wiki/patterns/linuxone-kafka-tuning.md` (new)
- `wiki/patterns/linuxone-flink-validation-tuning.md` (new)
- `wiki/patterns/fsi-l1-reference-architecture.md` (new)
- `wiki/_index.md` (updated)
**Canon stack:** base + fsi + linuxone

**Validation findings against canon + live Confluent docs:**
- Source docs treated ZooKeeper as live (legacy); CP 8.0+ is KRaft-only — corrected
- `linger.ms` default: source said 0; actual is 5 since Kafka 4.0 — corrected
- `acks` default: source said 1; actual is `all` since Kafka 3.0 — corrected
- `enable.idempotence` default: not addressed; actual is `true` since Kafka 3.0 — added
- `send.buffer.bytes` framed as "OS default"; Kafka has its own 128 KB default — corrected
- Confluent Platform for Apache Flink s390x support GA Dec 2025; IBM-bundled support model called out
- FIPS 140-2 source claim updated to FIPS 140-3 (NIST sunset 140-2 in 2026; CEX8S supports 140-3)
- Added LinuxONE-specific tests not in canonical suites: HiperSockets latency floor, SMC-D vs HiperSockets vs OSA, Crypto Express FIPS throughput, NNPA co-residency
- Telum II (corrected user spelling "Tellum") integration via zDLC ONNX → JNI for sub-ms anomaly detection in Flink UDFs

**Reference architecture:**
- L1 operational plane: Mongo, Cockroach, Postgres, Redis, Neo4j, Confluent, CMF (all L1-certified)
- Off-platform analytics via CL → CC Tableflow → Databricks; same Iceberg pattern fans out to Snowflake / S3 / Splunk / Dynatrace
- SLA tier mapping per [SLA Tiers](../concepts/sla-tiers.md)

## 2026-05-05T19:30:00Z
**Skill:** ad-hoc MCP-validation pass (post `/mcp` repair)
**Overlay:** base + fsi + linuxone
**Input:** four wiki articles authored 2026-05-04; live MCP servers `confluent-docs` (mcpdoc → docs.confluent.io/llms.txt) and `context7` (Apache Kafka 4.2.0 javadoc, Apache Flink 2.2 docs)
**Output:**
- `wiki/patterns/linuxone-validation-suite.md` (validated; MCP notes added)
- `wiki/patterns/linuxone-kafka-tuning.md` (validated; defaults confirmed; OpenJDK/Semeru caveat added)
- `wiki/patterns/linuxone-flink-validation-tuning.md` (Flink version range corrected: 1.20+ → CMF 2.3.x with Apache Flink 1.18.x – 2.1.x; Flink 2.x state backend class noted)
- `wiki/patterns/fsi-l1-reference-architecture.md` (Tableflow corrected: Iceberg + Delta Lake; CockroachDB clarified: not in CC fully-managed catalog, native changefeed mechanism)
**Canon stack:** base + fsi + linuxone

**MCP findings (no canon drift on the high-stakes claims):**
- ✅ `linger.ms` default = 5 (changed in Kafka 4.0)
- ✅ `acks` default = all
- ✅ `enable.idempotence` default = true (since 3.0)
- ✅ `send.buffer.bytes` default = 131072 (128 KB)
- ✅ `receive.buffer.bytes` consumer default = 65536 (64 KB); producer default = 32768 (32 KB) — distinction noted
- ✅ s390x support posture verbatim from Confluent docs
- ✅ KRaft only in CP 8.0+
- ✅ FIPS available in CP

**Corrections applied:**
- Flink version range narrowed: 1.20+ → CMF 2.3.x ↔ CP 8.2.x ↔ Flink 1.18.x – 2.1.x (release 2026-04-10)
- Tableflow output formats: Iceberg → Iceberg + Delta Lake
- CockroachDB connector posture: clarified as Hub self-managed only (not CC fully-managed); native changefeed is the canonical mechanism
- PostgreSQL CDC V1 EOL date verified at 2026-03-31
- Added JDK posture note: IBM Semeru / OpenJ9 not on Confluent's supported JDK list — workload-specific trade-off for IBMJCE access

**MCP infrastructure note:** Repaired `.mcp.json` (replaced dead `@langchain/mcpdoc-server` package with `uvx --from mcpdoc mcpdoc`). Removed user-scope MCP duplicates that were creating "conflicting scopes" warnings. Added `enabledMcpjsonServers: ["context7", "confluent-docs"]` to project `settings.local.json` to enable per-project approval at session start.

## 2026-05-06T16:00:00Z
**Skill:** chore (submodule pointer maintenance)
**Overlay:** base
**Input:** fsi-dsp main HEAD post-merge of PR #1 (squash commit `8200723`)
**Output:**
- `raw/repos/fsi-dsp` submodule pointer: `5721f9a` → `8200723`
**Canon stack:** base

**Side effect:** Resolves the pre-existing `check-citations` CI failure on this repo. The previously-recorded SHA `5721f9a` was committed locally to fsi-dsp but never pushed, so CI's `actions/checkout@v4` submodule fetch failed with "not our ref" on every wiki-touching PR. The new SHA `8200723` is the squash-merge commit of fsi-dsp PR #1 and is fetchable from `origin/main`.
