## 2026-08-05T20:51:24Z
**Skill:** /review
**Overlay:** base + fsi
**Input:** wiki/concepts/confluent-cloud-gateway.md (reviewed under its original filename; renamed to wiki/concepts/confluent-gateway.md in a follow-up action immediately after this review completed)
**Output:** outputs/reports/confluent-cloud-gateway-review-2026-08-05.md
**Canon stack:** base + industry/fsi
**Skills consulted:** kafka-streams-programming, kafka-schema-registry (spurious keyword match, out-of-scope)

## 2026-08-05T21:01:27Z
**Skill:** manual patch (follow-up to /review above)
**Overlay:** base + fsi
**Input:** wiki/patterns/dr-application-routing.md — reverified the Confluent Gateway restart requirement against `confluent-docs` (`gateway-migrate.html` Client Switchover considerations, `gateway-custom-domains.html` restart step, `co-gateway-deploy.html` CFK CR reference) before patching; restart-on-switchover holds for both Docker and CFK deployment paths, with no documented rolling/hot-reload path for CFK's multi-replica HA case
**Output:** wiki/patterns/dr-application-routing.md patched in place — corrected the "no restart required, 5–30s RTO" claim to reflect the mandatory gateway restart (Confluent Gateway specifically; Kroxylicious/ORKA left unverified rather than assumed identical), corrected the decision-matrix RTO/restart-required cells, corrected the superseded "1.5–2× broker CPU" sizing heuristic to the vendor-published instance sizing table, bumped `last_updated`/`last_validated` to 2026-08-05
**Canon stack:** base + industry/fsi
**Skills consulted:** none

## 2026-08-06T14:42:59Z
**Skill:** /ask --mode report
**Overlay:** base + fsi
**Input:** "Is Confluent Gateway controlled by Terraform? ..." (initial ephemeral turn) + "point my Confluent Gateway (K8s) to my Confluent Cloud cluster" + "HA with auto switchover for pod death / region death" (follow-up, saved as report on request)
**Output:** outputs/reports/confluent-gateway-iac-cc-connectivity-ha-2026-08-06.md
**Canon stack:** base + industry/fsi
**Skills consulted:** none — self-corrected a prior in-conversation claim after MCP fetch of `cloud/current/cp-component/gateway/overview.html` and `gateway-migrate.html` revealed Confluent Cloud Gateway is a separately-doc-treed edition, not merely an image-tag variant. Confirmed the restart-required Client Switchover behavior is identical word-for-word across both editions. Confirmed no automatic region-level failover exists in either edition (pod-level HA is ordinary K8s behavior; region-level is always operator-initiated). Two new gaps auto-stubbed to `wiki/_queue.md`.

## 2026-08-06T14:52:59Z
**Skill:** manual wiki update (follow-up to /ask report above, on explicit request)
**Overlay:** base + fsi
**Input:** wiki/concepts/confluent-gateway.md, wiki/concepts/private-networking.md — request to split the Confluent Gateway editions into separate pages and add strong disambiguation notation throughout so readers can't conflate the three "gateway" products
**Output:** wiki/concepts/confluent-cloud-gateway.md created (net-new article — the CC-backend edition's delta from confluent-gateway.md: image/license/doc-tree/CC-Schema-Registry-governance-target, pointing a route at a CC cluster, the SASL/OAUTHBEARER-passthrough-after-failover gotcha). wiki/concepts/confluent-gateway.md updated: expanded "Naming and disambiguation" into a three-way comparison table (Confluent Private Cloud Gateway / Confluent Cloud Gateway / Confluent Cloud Ingress PrivateLink Gateway), added a Terraform-naming-trap note, updated front matter and Related. wiki/concepts/private-networking.md updated: added a top-of-article disambiguation callout (previously had zero cross-reference to the proxy product despite owning the `confluent_gateway` Terraform resource name) and a Related entry. wiki/_index.md, wiki/_graph.md updated for the new article. wiki/_queue.md: resolved `confluent-gateway-terraform-iac` and `confluent-cloud-gateway-edition-vs-private-cloud` stubs; `gateway-ha-pod-vs-region-failover` left open (out of scope for this edit).
**Canon stack:** base + industry/fsi
**Skills consulted:** none

## 2026-08-06T15:04:32Z
**Skill:** manual wiki update (reversal of the split above, on explicit request)
**Overlay:** base + fsi
**Input:** request to reorganize so the proxy product (both backend flavors) lives on one wiki page, and the networking product stays fully separate — the only real difference between the self-managed and Confluent Cloud proxy flavors is which image to deploy, so a separate article was overkill and risked the same drift problem already fixed once this session in dr-application-routing.md
**Output:** wiki/concepts/confluent-cloud-gateway.md deleted; its CC-specific content (image/license table, CR example for pointing at a CC cluster, auth-swap-vs-passthrough guidance, SASL/OAUTHBEARER-passthrough-after-failover gotcha, CC Schema Registry governance target) merged into wiki/concepts/confluent-gateway.md as a new "Backend flavor: connecting to a Confluent Cloud cluster" subsection. confluent-gateway.md's disambiguation section reverted from a three-row to a two-row table (Confluent Gateway — both flavors — vs. the unrelated PrivateLink Gateway). wiki/concepts/private-networking.md, wiki/_index.md, wiki/_graph.md, wiki/_queue.md updated to remove all references to the deleted article.
**Canon stack:** base + industry/fsi
**Skills consulted:** none

## 2026-08-06T15:18:31Z
**Skill:** manual wiki update (rename + dedup verification, on explicit request)
**Overlay:** base + fsi
**Input:** rename wiki/concepts/private-networking.md → wiki/concepts/confluent-cloud-private-networking.md for clarity, and verify no other wiki article duplicates the gateway/networking content rather than legitimately referencing it
**Output:** git-renamed the file; updated every live cross-reference (link paths and `related:` front-matter slugs) across wiki/_graph.md, wiki/_index.md, wiki/patterns/flink-coe-aws-privatelink-refarch.md, wiki/patterns/aks-kafka-tuning.md, wiki/patterns/terraform-cicd-confluent-private-networking.md, wiki/patterns/flink-coe-managed-cc-overview.md, wiki/concepts/confluent-cloud-cluster-sku-selection.md, wiki/concepts/oic-kafka-integration.md, wiki/concepts/azure-connection-management.md, wiki/concepts/confluent-gateway.md; left historical mentions in wiki/activity/*.md and the 2026-05-18 wiki/_queue.md ingest entry untouched as point-in-time record. Dispatched a background sweep to confirm no other article substantially duplicates Confluent Gateway or PrivateLink Gateway content rather than just cross-referencing it — result pending.
**Canon stack:** base + industry/fsi
**Skills consulted:** none

## 2026-08-06T15:48:52Z
**Skill:** /review
**Overlay:** base + fsi
**Input:** wiki/concepts/confluent-gateway.md
**Output:** outputs/reports/confluent-gateway-review-2026-08-06.md
**Canon stack:** base + industry/fsi
**Skills consulted:** kafka-streams-programming

## 2026-08-06T15:55:43Z
**Skill:** manual patch (follow-up to /review above, on explicit request)
**Overlay:** base + fsi
**Input:** wiki/concepts/confluent-gateway.md — apply the three fixes from the 2026-08-06 review
**Output:** Core Capabilities auth-swapping row corrected to list mTLS as a supported client-side swap mechanism (resolves a finding carried unaddressed since the 2026-08-05 review, now sharper as a self-contradiction against the article's own mTLS swap example) and notes passthrough is unavailable for mTLS clients. Added an FSI SLA-tier caveat to Use Case 2 stating "sub-minute" RTO does not satisfy the risk (<10ms) or compliance (<100ms) tiers. Added an FSI callout to the "Backend flavor: connecting to a Confluent Cloud cluster" section flagging the worked `SASL_SSL`+`PLAIN` API-key example as non-compliant with the `mTLS + RBAC` / never-username-password canon default, with the `SASL_SSL`+`OAUTHBEARER` alternative as the FSI-correct substitution. Bumped sources/last_updated/last_validated.
**Canon stack:** base + industry/fsi
**Skills consulted:** none

## 2026-08-07T00:00:00Z
**Skill:** /review
**Overlay:** base + fsi
**Input:** ~/GoodLabs/confluent-terraform-modules/README.md, ~/GoodLabs/confluent-terraform-modules/CLAUDE.md (external repo — Confluent Cloud Terraform module set, reviewed against Confluent docs + this project's wiki for gateway/YAML schema consistency and client usability)
**Output:** outputs/reports/confluent-terraform-modules-review-2026-08-07.md — found the repo's own docs incorrectly claim PNI (Private Network Interface) works on Dedicated clusters in three places (Confluent's AWS Private Network Feature Support matrix and this project's own wiki/concepts/network-connectivity-by-tier.md both confirm PNI is Enterprise/Freight-only), with no plan-time guard on that path unlike the two PrivateLink modules added earlier this session; also found a stale provider-version mention (`~> 2.47.0`) left over from the same version bump, and two stale tree diagrams/a "placeholder" line predating the networking modules' implementation. One claim (Confluent Cloud Schema Registry contexts) queued to _queue.md as unverifiable this session.
**Canon stack:** base + industry/fsi
**Skills consulted:** none
